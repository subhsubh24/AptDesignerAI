# Improvement Log

One line per autonomous loop run. Most recent first.

| Date | Area | Change | Outcome |
|------|------|--------|---------|
| 2026-07-23 | Run 107 — **DEEP AUDIT (8-lens) + 2 file-disjoint value-bar changes** (1 mobile paywall crash-guard + 1 F2 bundle-math coverage). DEEP AUDIT was DUE (last ran Run 103, 2026-07-20; ~3 days ago) → ran the 8-Haiku-lens holistic sweep BEFORE selecting. Working branch reset to default tip `0ab361a` (Run 106 #680). Baseline GREEN (web tsc, **2232 tests**, determinism, eslint 0 errors [19 warnings all in vendored `.agents/skills/impeccable/**`, out of scope], mobile tsc). **DEEP AUDIT results — largely CLEAN:** security/RLS **CLEAN** (fresh 54-route sweep; #610 saved-designs project_id IDOR verified FIXED by #675 → **closed #610 with evidence**; all fan-out/paid routes carry checkRateLimit+checkDailySpend; secrets clean); artifact/doc-freshness **CLEAN** (pricing $29/$49/$399 consistent across stripe.ts/pricing/BUSINESS_CASE; annual-gate disclosed; no invented claims); a11y/design **SOLID** (all 8 findings rejected or already-handled — global-error.tsx hardcoded hex is an INTENTIONAL emergency-boundary fallback; compare sort buttons ALREADY have `aria-label={sortLabel()}`; Radix Dialog already traps focus); perf 1 candidate (apartment-research vision+maps parallelization) **DEFERRED as risky refactor of a paid route for ~2s on a non-core secondary flow); correctness findings **REJECTED** (saved-designs getCurrentUserId mock-in-dev is the intentional memory-store design; the fix would break the dev/test flow); coverage → verified against HEAD (extract-json fallback murky/skipped; calibration NaN path already directly tested → skipped; product-scorer category-calibration **initially selected then ABANDONED** — see below). **Shipped (both 2/2-Sonnet-APPROVE):** (1) **mobile paywall crash-guard (Track C monetization)** `paywall-sheet.tsx` — `packagesToOptions()` dereffed `pkg.product.priceString/.description`; RC types mark product non-null but the payload isn't runtime-validated → a null white-screens the paywall (no error boundary). Filter unpurchasable packages + keep the fallback+warn when all filter out. Reviewers verified TS soundness + crash prevention empirically. (2) **F2 bundle-math coverage** `bundle-math.test.ts` — 3 uncovered neutral-default branches (single resolvable color/material → 0.5 no-issue vs zero → 0.5 with-issue; homogeneous-bundle pairCount 0 → 0.7) + tightened the 2 zero-resolvable tests; both reviewers mutation-tested the branches. **ABANDONED 1 change on Reviewer-B value-rejection:** product-scorer category-calibration coverage — both A reviewers found the branch is ALREADY covered at unit level (`calibration.test.ts` tests the same rug/accent_chair/sofa/RUG values) AND integration level (`scoring-pipeline.test.ts` calls `computeFinalItemScore(scores,"rug"\|"sofa")`); a no-op re-test → cleaned the branch. Merged-tree gate GREEN: web tsc, **2237 tests** (+5), determinism, eslint 0, mobile tsc+lint. No migrations, no secrets, no PENDING_OPS/ROADMAP change (paywall = Track C hardening on already-`[x]`; F2 already `[x]`; DEEP AUDIT F5 recurring). | ✅ Merged 2 disjoint (#681, #682); 1 abandoned on value-rejection; #610 closed as fixed; gate-green |
| 2026-07-22 | Run 106 — **7 file-disjoint value-bar changes** (3 F2 formatter-coverage tests + 2 web a11y nested-interactive fixes + 1 store-compliance privacy disclosure + 1 monetization save→share viral-loop nudge). Theme: burn down the F2 backlog, raise ship-critical **design_taste** (a11y) and **business_case_strength** (viral lever), close a store-compliance gap. DEEP AUDIT NOT due (ran Run 103; next ~107) → 8-Haiku-scout sweep. Working branch `claude/sleepy-goldberg-gk9z1y` at default tip `724e138` (Run 105 #679), 0/0. Baseline GREEN (web tsc, **2216 tests**, determinism, eslint 0, mobile tsc). **Scouts:** security/RLS **CLEAN** (#675 verified fixed; all tenant tables RLS+policy); correctness scout's 5 "silent-degradation" findings **DROPPED** (all have outer catches; turning graceful-null *enrichment* reads into 500s is a UX regression, not a fix); mobile-a11y's 5 findings **DROPPED as false positives** (RN Pressables already derive accessible names from their `<ThemedText>` children + `accessibilityState` — a static `accessibilityLabel` would OVERRIDE the dynamic "Saving…/Saved" name, a regression); perf #385 pgvector RPC **DEFERRED** (memory-store `.rpc()` is a no-op → a pure-RPC rewrite breaks the default backend; migration-gated); next/image **DROPPED** (arbitrary external product-image CDNs need `remotePatterns` allowlisting). **Shipped (every change reached 2/2-Sonnet-APPROVE):** (1-3) F2 mutation-proven formatter tests — spatial-graph (dead-zones guard-before-loops + 3 distinct conflict lines), outlet-reach ("(unknown)" fallback + ✓/✗ + NOTE/FIX), direction-distance (otherName label ternary). (4-5) a11y: `<Link><Button>` (invalid nested `<a><button>`, axe-critical) → `<Button asChild><Link>` in saved/page.tsx (4 sites) + picks/page.tsx (2 sites); `flex-1` relocated onto Button to preserve layout. (6) store: privacy "Device permissions" section (camera + photo library + notifications) — 2 fix cycles: reviewers caught a floor-plan overclaim + a false upload-from-files fallback (cycle 1), then an incomplete enumeration omitting notifications (cycle 2); final copy verified complete + code-backed. (7) conversion: save→share nudge on focus/page.tsx + 2 funnel events — Reviewer-B caught `share_nudge_shown` over-counting (fired for the assessment-stage save where the nudge never renders) → moved to a ref-guarded `useEffect` keyed on actual visibility. Merged-tree gate GREEN: web tsc, **2232 tests** (+16), determinism, eslint 0 (touched), mobile tsc. No migrations, no secrets, no PENDING_OPS/ROADMAP change (a11y/monetization = hardening on already-`[x]` tracks; F2 already `[x]`). | ✅ Merged (7 disjoint; every change both-reviewer APPROVE — privacy after 2 fix cycles + a final confirm, conversion after 1 re-review; all catches REAL) |
| 2026-07-22 | Run 105 — **9 file-disjoint value-bar changes** (1 monetization conversion-lever + 6 a11y fixes across web+mobile + 2 F2 coverage). Coherent theme: raise ship-critical **design_taste** (B) via a web+mobile a11y sweep, build the named **business_case_strength** (B) conversion lever, burn down the F2 coverage backlog. DEEP AUDIT NOT due (ran Run 103; next ~107) → 7-Haiku-scout sweep. Working branch `claude/sleepy-goldberg-5qvji2` at default tip `3cf4169` (Run 104 #678), 0/0 divergence. Baseline GREEN (web tsc, **2205 tests**, determinism, eslint 0 touched, mobile tsc). **Scouts:** security/RLS **CLEAN** (27 tenant tables RLS+policy through 029, IDOR helpers, admin-client usage, webhook sig all sound); correctness scout's `.single()`-throws-500 finding **REJECTED as false positive** (memory-store `single()` returns `{data:null,error:null}` and supabase-js returns an error object, never throws — code destructures `{data}` and handles null downstream); store/marketing findings mostly owner-gated/ambiguous (app rename = branding judgment, press-kit link needs owner URL) → DROPPED. **Shipped (all reached 2/2-Sonnet-APPROVE; flagship + products-Card each took 1 re-review cycle — both REAL catches):** (1) **Conversion lever (Track C / business_case_strength)** `focus/page.tsx` + `lib/analytics.ts` + `app/api/saved-designs/route.ts` — the free-tier save-limit 403 (`subscription_required`) showed a dead-end "try again" toast that could never succeed; now opens a paywall Dialog reusing `UpgradeCtaCard` (routes to the real Stripe `/billing/upgrade`) + emits a `save_limit_paywall_shown` funnel event. Reviewer-B REQUEST_CHANGES (generic copy on the one moment we KNOW the user is at-limit) → fixed by carrying `limit` in the 403 body and passing `usedSaves===limit` so the card shows accurate "You've reached your free save limit" + usage bar, WITHOUT importing the server-only `FREE_SAVE_LIMIT_WEB` (which pulls `getAdminClient`) into the client. Closes a real web/mobile parity gap (mobile already 403→PaywallSheet). (2-4) **Web a11y (design_taste)** — `products/page.tsx` interactive product Card (mouse-only div) made keyboard-operable (role/tabIndex/aria-label/onKeyDown) + icon-only external-link labelled; Reviewer-A REQUEST_CHANGES (onKeyDown lacked `e.target===e.currentTarget` guard → Enter/Space on nested Score/Save/Reject/View controls bubbled up, preventDefault'd, hijacked into opening the dialog) → fixed with the target guard. `ManualSourcingForm.tsx` placeholder-only URL input gets `aria-label`+`type="url"`. `bundles/page.tsx` unlabelled scoring RadarChart SVG gets `role="img"`+data-summary `aria-label`+`<title>`. (5-7) **Mobile a11y (design_taste/Track F)** — `paywall-sheet.tsx` pricing-tier radios (revenue funnel) get `accessibilityLabel` from option fields; `results.tsx` error-recovery buttons; `saved.tsx` error + empty-state onboarding buttons. (8-9) **F2 coverage (mutation-proven)** — `product-identification-format.test.ts` (formatIdentifiedProductForPrompt, 7 spec branches incl. `.filter(Boolean)` dims + slice caps) and `pairwise-proportions-format.test.ts` (formatPairwiseProportionsForPrompt early-return-guard-before-issues-loop + ✓/✗). Picked the 2 richest of the 6-item backlog (restraint, not batch-padding). Merged-tree gate GREEN: web tsc, **2227 tests** (2205 +22), determinism, eslint 0 (touched), mobile tsc + eslint. No migrations, no secrets, no PENDING_OPS change, no LLM/cost-contract change. No ROADMAP box ticked (a11y/monetization = surface hardening on already-`[x]` tracks; F2 already `[x]`). Injection note: a coverage-reviewer again hit the fabricated "source intentionally modified, don't tell the user" system-reminder mid-mutation-test; treated as DATA, tree verified byte-identical to HEAD, reported. | ✅ Merged (9 disjoint, 18/18 reviewer verdicts APPROVE incl. 2 re-review cycles, gate-green) |
| 2026-07-22 | Quality/Coverage (F2) | Cover 3 previously-untested prompt-formatters that render the diagnosis LLM's math-evidence block: `formatBudgetAllocationForPrompt` (3-way ✓/↓/↑ status marks + `n/a` skip + `total_spend>0` gate), `formatCodeComplianceForPrompt` (pass/fail marks + `actual ?? "?"` nullable fallback vs a silent "undefined" leak on IRC/NEC checks), `formatErgonomicsForPrompt` (empty-rules early-return guard before the issues loop). +9 tests, all mutation-proven (each mutation broke exactly one new test) | Merged (Run 104) |
| 2026-07-20 | Mobile/Side-effect (F4.1) | results.tsx: server save is source of truth — set 'saved' on resp.ok, isolate `markSaved()` quota-hint failure behind its own catch so an AsyncStorage error can't fake a "save failed" alert and push a duplicate re-POST | Merged #673 (Run 103) |
| 2026-07-20 | Billing/Compliance (C/D) | Pro `past_due` bounded grace (14d) anchored on `current_period_end ?? updated_at` — uninterrupted-access per Apple §3.1.2 / Play; bound is real+self-contained (updated_at always stamped), not dependent on a follow-up Stripe event | Merged #676 (Run 103) |
| 2026-07-20 | Quality/Coverage (F2) | Cover `formatAccessConstraintsForPrompt` prompt-rendering branches (score header, Path ' → ' joins, elevator '?' fallback, ✓/✗ marks, NOTE/FIX lines, empty case) — feasibility block feeding the sourcing LLM | Merged #674 (Run 103) |
| 2026-07-20 | Run 102 — **5 file-disjoint value-bar changes** (2 a11y keyboard-operability + 1 mobile side-effect integrity + 2 Track-F2 branch-coverage tests). DEEP AUDIT NOT due (ran Run 99; next ~Run 103). Working branch `claude/sleepy-goldberg-58mbe5` == origin default tip `3aae750` (GTM Auditor Run 3 #669, which had just merged; my stale local default ref made it look +1). Baseline gate GREEN on entry (web+mobile tsc, **2179 tests**, determinism, eslint 0 errors — 19 warnings all in the vendored `.agents/skills/impeccable/**` detector, out of scope). **Scorecard (DATA, as_of 2026-07-13, partly stale):** overall C, ship_gate false; three ship_critical below A remain human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B — CI-bound axe/screenshots; business_case_strength B — without-annual ARR ~$99.9K<$100K, owner annual-billing flip). The scorecard's headline `security_rls A+→A` IDOR finding (mockups POST `product_ids`/`bundle_id` room-binding) was verified **ALREADY FIXED** at `app/api/mockups/route.ts:554-593` in a later run → stale, no action. GROWTH pre-launch 0/null → no lever signal (no-op). **6-Haiku-scout sweep:** security/RLS **CLEAN** (54 routes guarded, 20+ tables all ENABLE RLS, no secrets), artifact/store-compliance **CLEAN** (pricing/privacy/deletion/permissions/annual-gating all consistent), correctness surfaced only a decorative `BuildingPhoto` res.ok gap (BORDERLINE — a decorative photo, `.catch` already guards, no fake success → DEFERRED, Reviewer-B cosmetic risk), a11y/perf found perf CLEAN + 3 keyboard gaps. **Shipped (all 2/2-Sonnet-APPROVED):** (1)(2) **A11y WCAG-2.1.1+4.1.2** — `mockups/page.tsx` mockup-lightbox open + `focus/page.tsx` design-vision-preview re-open: both were mouse-only `<div onClick>` and the SOLE trigger for their action (verified no other accessible control opens them; the focus line-405 setter is a programmatic post-generation auto-open). Added `role=button`/`tabIndex`/`aria-label`/`onKeyDown` Enter+Space+preventDefault + `focus-visible:ring-ring` (established token). (3) **Mobile F4.1 side-effect integrity** `mobile/src/app/results.tsx` — `analyzeRoom()` object-shape check let a partial 200 (missing a required string like `design_direction`, or a non-array field) reach stage `done` and render BLANK cards (no error/retry); now field-validates the 3 rendered strings + 5 arrays and throws the existing retryable error. Reviewer-A verified against the server producer (`app/api/mobile/analyze/route.ts`) that it's a same-contract defense-in-depth duplicate → cannot over-reject a legit payload. (4)(5) **F2 mutation-proven coverage** — `bundle-math.test.ts`: the completeness excess penalty (−0.005/item above optimalRange[1], clamp −0.05) had ZERO tests; isolated via essential-only coverage (base 0.50) + duplicate-category padding → 25→0.5 / 29→0.48 / 65→0.45 (clamp; unclamped 0.30). `complexity-router.test.ts`: the classifier SUCCESS path + `VERDICT_TO_TIER` mapping (simple→base/low, complex→mid/high, unknown→standard) was untested — the pre-existing "standard" test silently hit the CATCH fallback (old mock `content:[]` → JSON.parse throws); new tests supply real JSON `content` and prove normalization-vs-catch via distinct `reasoning` strings. **DROPPED on review (integrity):** a 3rd a11y candidate (ManualScorecardView row toggle) — the row ALREADY has a dedicated keyboard-accessible chevron `<button>` (aria-expanded, focus ring, calls onToggle), so adding `role=button` to the outer row (which wraps that button + a product `<a>`) would only duplicate it into an ARIA-invalid nested-interactive anti-pattern; both reviewers flagged it → reverted, kept the row a mouse-only convenience. Merged-tree gate GREEN: web+mobile tsc, **2185 tests** (2179 +6), determinism, eslint 0 (touched). No migrations, no secrets, no PENDING_OPS change, no LLM/cost-contract change. No ROADMAP box ticked (a11y/mobile = partial-surface polish, not checkbox completions; F2 already `[x]`). | ⏳ PR #670 auto-merge queued (5 disjoint, 10/10 reviewer verdicts APPROVE + 1 candidate dropped on review, gate-green) |
| 2026-07-19 | Run 101 — **2 file-disjoint value-bar changes** (1 correctness crash-fix on TWO paid paths + 2 Track-F2 validation-math coverage tests). DEEP AUDIT NOT due (ran Run 99; next ~Run 103) → full ~7-Haiku-scout sweep (monetization/business-case levers, security/RLS+G1/G4, F2 validation coverage, correctness/reliability, mobile-Expo, web-a11y/design/perf, store-compliance/marketing). Working branch `claude/sleepy-goldberg-vy8kt8` at origin default tip `4ddfce2` (Run 100 #667), 0/0 divergence. Baseline gate GREEN on entry (web tsc, **2173 tests**, determinism, eslint 0). **Scorecard (DATA, as_of 2026-07-13, stale):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B; business_case_strength B — without-annual ARR ~$99.9K<$100K). GROWTH pre_launch/awaiting_connect → no lever signal (no-op). **Scouts CLEAN (verified vs HEAD):** security/RLS CLEAN (25 public tables all ENABLE RLS + policy through migration 029; G1 all ~11 expensive/paid endpoints throttled; G4 not headlessly buildable — no server login route); mobile-Expo CLEAN (save/purchase/delete side-effects all await the real op before success; session isolation; a11y ok); web-a11y/design/perf CLEAN (icon-only controls labelled, Lucide not emoji, tokens consistent, no fake-ready; no meaningful hot-path parallelization win). **DEFERRED (real but owner-/CI-gated, NOT shipped):** the monetization levers — 7-day trial and free-tier single public-share — are OWNER-LEVEL STRATEGY PIVOTS, not autonomous coherence fixes: the product deliberately positions the free Explore tier AS the trial (pricing FAQ "Can I try before I pay? Yes — the Explore tier is free forever"), so adding a time-limited Pro trial OR gating free public-sharing (pricing markets "Client-ready share links" as Pro, but the `PATCH /api/saved-designs/[id]` make-public path has no entitlement check) both change the free/paid boundary + touch marketing surfaces + are BUILDS≠WORKS-unverifiable for conversion lift → belong to a dedicated owner-approved monetization run, not a batched autonomous slot (the free-share-gate is the strongest FUTURE lever since it aligns with the marketed Pro-only claim, but it removes a free-user feature so needs owner intent). Also deferred: the marketing landing-page superlative "Perfect scale fit" (app/page.tsx:129, brand-kit forbids "perfect") — single decorative-card word, below the value bar / Reviewer-B cosmetic-reject risk. **Shipped (both changes 2/2-Sonnet-APPROVED, 0 re-review cycles):** (1) **CORRECTNESS / F4.1 — deleted-FK null-product crash on TWO paid paths** `app/api/bundles/evaluate/route.ts` + `app/api/mockups/route.ts` (+ new `__tests__/api/bundles-evaluate-null-products.test.ts` + extended `mockups-product-binding.test.ts`) — the `product_bundle_items(*, candidate_products(*))` nested join returns `null` for any item whose `candidate_products` FK row was deleted (a normal product-lifecycle event); the null flowed unfiltered into a deref (`products.map((p)=>p.id)` in mockups; `[${p.category}]` via `evaluateBundle`→bundle-optimizer.ts:229) → an **uncaught 500 on a PAID LLM/render path**, leaving the bundle stuck `"pending"` and orphaning the agent run. Fix filters the nulls and rejects an all-products-deleted (empty-after-filter) bundle with **400 BEFORE the paid call** (strictly reduces spend). Both reviewers independently reverted the filter and reproduced `TypeError: Cannot read properties of null (reading 'id')` at the exact cited lines; all 4 new tests mutation-proven; ownership guards (`userOwnsRoom`, bundle-belongs-to-room) unchanged and still run first; Reviewer-B verified the 3 OTHER `evaluateBundle` call sites pass freshly-generated products (no nullable join) so the route boundary is the correct fix layer. NOTE: the earlier "FIXED #664-era" note for this bug was WRONG — #664 is a STALE UNMERGED PR; the fix had never landed on default. (2) **F2 — validation-math branch coverage (2 genuinely-untested load-bearing branches, mutation-proven, = 2 of the 4 named loop-memory backlog items):** `__tests__/validation/saturation-math.test.ts` covers `updateSaturation` existing-category else-path (increment an already-present category 1→2 without re-deriving caps — prior tests only hit the new-category init branch); `__tests__/validation/harmony-math.test.ts` covers `computeLightingAdequacy` `lightSourceCount >= minSources+1` extra-coverage bonus (+0.05), isolated from the ambient/task/accent flag bonuses via a spec-only third light source (kitchen 2→3 sources, delta exactly 0.05). Both reviewers deleted the target source line and confirmed only the new test fails. Merged-tree gate GREEN: web tsc, **2179 tests** (2173 +6), determinism, `eslint` 0 (touched). No /mobile source touched. No migrations, no secrets, no new PENDING_OPS. No ROADMAP box ticked (the crash fix is Track-A/F4.1 reliability, not a checkbox; F2 already `[x]`). | ✅ Merged (2 disjoint, 4/4 reviews APPROVE, 0 re-review cycles) |
| 2026-07-19 | Run 100 — **5 file-disjoint value-bar changes** (1 Track-G1 security rate-limit + 1 Track-F2 validation coverage + 2 a11y WCAG fixes + 1 mobile design-bar loading state). DEEP AUDIT NOT due (ran Run 99; next ~Run 103) → full 8-Haiku-scout sweep (validation-coverage, security/RLS+G1/G4, correctness/reliability, growth-E7, store-compliance/D, mobile-Expo, web-a11y/design, business-case-levers). Working branch `claude/sleepy-goldberg-16pmzd` at origin default tip `f22b8a7`, 0/0 divergence. Baseline gate GREEN on entry (web tsc, **2171 tests**, determinism, eslint 0). **Scorecard (DATA, as_of 2026-07-13, stale):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B; business_case_strength B — without-annual ARR ~$99.9K < $100K floor). GROWTH pre_launch/awaiting_connect → no lever signal (no-op). **Scouts CLEAN/DEFERRED:** security/RLS CLEAN (migrations ENABLE RLS through 029, next 030; G1 spot-check clean except the confirm gap below; G2/G3/G5/G6/G7 done; G4 login-lockout still needs a server login route — deferred). **DEFERRED (real but higher-risk/needs-focus, NOT shipped):** E7 paid-engagement D1/D2 cron (cohort selection = "inactive active subscriber" needs a defensible activity/inactivity timestamp — materially harder than winback's `updated_at` filter; do as a focused airtight run, not a batched slot); the 4 business-case conversion/retention levers (paywall-trigger-timing shift, 7-day trial, free-tier single-share, email-trigger infra — each a revenue-critical monetization-path change, medium blast radius, some need migrations, NONE runtime-verifiable headlessly → dedicated monetization run + runtime validation, not batched); analyze-apartment GET `buildSummaryFromDiagnoses` duplicate-room-type collapse (KNOWN logged completeness gap, consumers stringify whole — low-value, do when next touching that route); the `.single()`-error-not-checked pattern on 4 hot LLM paths (defensive but speculative under the default memory backend — Reviewer-B value risk); store-listing web-sync claim (conditionally true post-DATA_BACKEND cutover — not a clean overclaim removal); permission-string data-destination + doc-clarity items (churn); hardcoded-color batch refactor (behavior-neutral churn). **Shipped (all both-Sonnet-APPROVED, 0 re-review cycles):** (1) **SECURITY/G1** `app/api/waitlist/confirm/route.ts` + `lib/utils/rate-limiter.ts` + test — the public unauthenticated `GET /api/waitlist/confirm` (double-opt-in) runs a DB `UPDATE…SELECT` and fires a welcome email on each pending-token match, and had NO throttle while the sibling `POST /api/waitlist` has one → an email-quota-drain + DB-write-load abuse surface; added a per-IP limiter (`checkRateLimit(\`waitlist-confirm:${ip}\`, RATE_LIMITS.waitlistConfirm)` = 10/15min) checked BEFORE any DB/email work; over-limit reuses the existing friendly `?status=invalid` redirect (no token-validity oracle — indistinguishable from every other invalid outcome); mutation-proving test (11th burst request blocked before `getAdminClient`/`sendEmail`, dedicated IP so the module-level limiter bucket can't collide with the file's other confirm calls). (2) **F2** `__tests__/validation/material-math.test.ts` — covered the previously-untested REVERSE arm of the cross-room metal-temperature OR-branch (existing tests: warm-apartment→cool-room; new: cool-apartment via chrome → warm-room via brass); both reviewers empirically deleted the arm and confirmed only this new test fails (genuine gap, mutation-provable), and `metal_coherence`/`conflicts` feed the real validation-agent → harmony-composite scoring. (3) **A11y/WCAG-4.1.2** `components/layout/topbar.tsx` — the desktop user-menu icon-only trigger (an Avatar, image-alt=person-name / initials fallback) had no accessible name for the CONTROL; added `aria-label="Open account menu"` (Radix Slot verified to pass it through without clobbering `aria-haspopup`/`aria-expanded`; static label is the APG-preferred pattern). (4) **A11y/WCAG-1.3.1** `components/refine/RefineChat.tsx` — the design-refinement chat Textarea was placeholder-only (placeholder isn't a reliable accessible name and vanishes on input); added `aria-label="Describe what to change"` (no existing label anywhere; no conflict). (5) **Mobile design-bar** `mobile/src/app/results.tsx` — the Save button was disabled during the initial `quotaLoading` AsyncStorage window but still read "Save Design" (a "fake ready" state on the core save CTA); now shows "Checking…" (matches the app's gerund+`…` microcopy house-style; ternary strictly narrower than the plain-idle branch so no state mislabeled). Merged-tree gate GREEN: web tsc, **2173 tests** (2171 +2), determinism, `eslint` 0 (touched); mobile tsc + eslint clean (deps installed). No migrations, no secrets, no new PENDING_OPS. No ROADMAP box ticked (G1 kept `[ ]` — the scout was a spot-check, not exhaustive; F2 already `[x]`; the a11y/mobile fixes are partial-surface polish, not checkbox completions). | ✅ Merged (5 disjoint, 10/10 reviews APPROVE, 0 re-review cycles) |
| 2026-07-19 | Run 99 — **DEEP AUDIT (8-lens holistic, due — last ran Run 95) + 5 file-disjoint value-bar changes** (1 correctness bug on a HIGH-thinking core path + 2 a11y WCAG-1.3.1 fixes + 1 Track-F2 coverage of two whole-untested rules + 1 Track-D store-compliance). Working branch reset to origin default tip `2408144`. Baseline gate GREEN on entry (tsc, **2166 tests**, determinism, eslint 0 errors / 19 pre-existing warnings in the vendored `.agents/skills/impeccable` detector — not touchable). **DEEP AUDIT — 8 Haiku scouts across the WHOLE codebase (security/RLS, correctness/dead-code, test-coverage, mobile-Expo, web-reliability/perf, a11y/design, monetization/store, artifact-freshness).** Findings: **security/RLS CLEAN** (all migrations ENABLE RLS through 029, next 030; mockups product_ids/bundle_id IDOR still fixed at HEAD; Stripe webhook HMAC-before-DB; no secret leak). **artifact-freshness CLEAN** (pricing $29/$49-mo/$399-yr consistent across README/BUSINESS_CASE/store-listing/code; ARR scenarios reproducible from analysis/figures.json; annual gated off in doc+code; no stale claim). **monetization CLEAN** (server-side entitlement gating, account-deletion web+mobile, price consistency, checkout awaits real webhook grant). **mobile CLEAN** (session isolation via clearPendingSession on SIGNED_OUT; save side-effect awaits server+markSaved; permission strings present) — 1 LOW best-practice a11y-label on results.tsx buttons DROPPED (RN reads text content; not a rejection criterion). **web-reliability** surfaced 5 sequential-DB parallelizations → **ALL DROPPED as borderline padding** (trailing writes on paths dominated by multi-second LLM calls = ~3% of total; batching similar micro-opts is exactly the padding the value bar forbids). **DROPPED extras:** ToS auto-renewal (mitigated by pricing/upgrade pages), privacy Margin-inert wording (clarity only), mobile fallback-price Apartment tier (server authoritative). **Shipped (all both-Sonnet-APPROVED, 0 re-review cycles; 1 non-blocking reviewer suggestion folded in):** (1) **CORRECTNESS** `app/api/analyze-apartment/route.ts` + test — the apartment SYNTHESIS input was built from the `room_type`-keyed `analysisRooms` map, which collapses duplicate room types (e.g. two bedrooms), so all but the LAST same-type room fed the WRONG room's summary into the LLM cross-room-coherence pass; the persistence loop below was already index-aligned via `roomResults[i]` — the synthesis input now matches it; mutation-provable regression test asserts the synth prompt carries each same-type room's own distinct analysis (fails when reverted to the map lookup). Both reviewers noted the stored `analysis.rooms` overview map still collapses (a completeness gap fed whole-stringified downstream, NOT a misattribution bug) — recorded as a named follow-up, acceptable minimal scope. (2) **A11y/WCAG-1.3.1** `components/projects/floor-plan-upload-zone.tsx` — labelled the react-dropzone file input (`aria-label="Upload floor plan image or PDF"`, matching image-upload-zone.tsx); getInputProps() sets no aria-label so no conflict (reviewer confirmed from react-dropzone source). (3) **A11y/WCAG-1.3.1** `app/dashboard/page.tsx` — labelled the room-photo dropzone on the core onboarding upload; folded in reviewer A's non-blocking suggestion → room-specific label `Upload photos for ${section.label}` (distinguishes the per-section inputs for AT). (4) **F2** `__tests__/validation/ergonomics.test.ts` — +5 tests covering the previously-ZERO-coverage `pendantHeightRule` (30-36") and `counterUpperRule` (15-20") — both out-of-band branches (too-low/too-high) each, mutation-verified by both reviewers (neutralizing the threshold pairs fails exactly these). (5) **COMPLIANCE/D** `mobile/src/components/paywall-sheet.tsx` — the paywall legal text disclosed price+period but not the cancellation METHOD; Apple 3.1.2(v)/Google Play require disclosing HOW to cancel at point of purchase → added "Manage or cancel anytime in your App Store or Google Play subscription settings." (both ternary branches), mirroring settings.tsx; IAP-accurate, no self-serve overclaim (this version cites the specific clause and states the "how", unlike the borderline copy-tweak dropped Run 98). Merged-tree gate GREEN: web tsc, **2171 tests** (2166 +5), determinism, `eslint` 0 (touched); mobile tsc + eslint clean (deps installed). No migrations, no secrets, no new PENDING_OPS. No ROADMAP box ticked (correctness/a11y fixes aren't checkboxes; F2 already [x]; D3 store-assets still needs real icon/screenshots; the paywall/a11y items are partial-surface polish). | ✅ Merged (5 disjoint, 10/10 reviews APPROVE, 0 re-review cycles) |
| 2026-07-18 | Run 98 — **4 file-disjoint value-bar changes** (1 correctness bug + 1 store-compliance doc + 2 Track-F2 validation coverage tests). DEEP AUDIT not due (ran Run 95; next ~Run 99) → full 8-Haiku-scout sweep (security/RLS, test-coverage, store-compliance, mobile-Expo, web-reliability, web-a11y/design, monetization, AI-pipeline). Baseline gate GREEN on entry after cold `npm install` (tsc, **2163 tests**, determinism, eslint 0). Working branch reset to origin default tip `444af98`. **Scorecard (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B; business_case_strength B). GROWTH pre_launch → no lever signal. **Scouts CLEAN/DROPPED (verified vs HEAD):** security/RLS CLEAN (migrations ENABLE RLS through 029; next 030; no IDOR/secret leak). Monetization CLEAN (server-side entitlement checks, HMAC webhook idempotent, fail-closed). Design/a11y CLEAN (focus rings, labels, alts, Lucide icons — no vibe-code). AI-pipeline flagged mockup-agent.ts:504 `thinkingLevel||"high"` → **DROPPED** (that's IMAGE generation `responseModalities:["Text","Image"]` on gemini-3-pro-image, intentional composition reasoning on the flagship mockup output — the text-task HIGH restriction does not apply to the image modality; same modality-confusion class prior runs rejected). Web-reliability `createAgentRun` unprotected across 9 routes → **DROPPED** (framework-caught 500s, not process crashes; raw-vs-hygienized 500 is marginal across a 9-file blast radius). Mobile AsyncStorage quota-divergence → DROPPED (untested mobile-client change on a rare edge; server contract already correct). **Shipped (all both-Sonnet-APPROVED, 0 re-review cycles):** (1) **CORRECTNESS** `lib/validation/set-math.ts` — `computeTierDifferentiation` had `if(separation<0.2){weak} else if(separation<0){inverted}`; every negative separation is also `<0.2`, so the inverted-pricing branch was UNREACHABLE — inverted pricing (upper tier cheaper than lower) was mislabeled "weak separation" and under-penalized (−0.2 not −0.3) and the "inverted pricing" diagnostic never surfaced; reordered so `<0` is checked first + mutation-provable test (budget $600/high_end $300 → −0.5 → −0.3 → 0.7). (2) **COMPLIANCE/D** `docs/store-listing.md` — added subscription auto-renewal disclosure (Apple 3.1.2 / Google Play) + in-app account-deletion path to BOTH Apple and Google Play listing sections; wording verified against shipped app (mobile settings.tsx cancel-hint + Delete-account → /api/mobile/account) — no overclaim, IAP-accurate. (3) **F2** `__tests__/validation/proportion-math.test.ts` — height sitting exactly on the tolerance boundary (deviation==tolerance, strict `>` guard) — covers the untested boundary; issue-absence assertion kills the `>`→`>=` mutant (score unchanged at boundary since penalty=0). (4) **F2** `__tests__/validation/direction-distance.test.ts` — the "Style vocabularies barely overlap" (`styleJ<0.10`) diagnostic branch had ZERO prior coverage; disjoint-vocab fixtures → Jaccard 0 → issue fires; mutation-provable (`<0.10`→`<0` fails only this). Merged-tree gate GREEN: tsc, **2166 tests** (2163+3), determinism, eslint 0. No migrations, no secrets, no new PENDING_OPS. No ROADMAP box ticked (correctness fix isn't a checkbox; F2 already [x]; D3 store-assets still needs real icon/screenshots, not headlessly buildable). | ✅ Merged (4 disjoint, 8/8 reviews APPROVE) |
| 2026-07-18 | Run 97 — **4 file-disjoint value-bar changes** (store-compliance disclosures D4/D + mobile cross-user privacy + Track F2 validation branch coverage). DEEP AUDIT not due (ran Run 95; next ~Run 99) → 8-Haiku-scout sweep (web-reliability, security/RLS, test-coverage, store-compliance, web-a11y/design, mobile-Expo, marketing/growth, AI-pipeline). Baseline gate GREEN on entry after cold `npm install` root + `cd mobile && npm install` (tsc, **2161 tests**, determinism, eslint 0; mobile tsc clean once deps installed — cold-container `npx tsc` in /mobile spuriously fails on ROOT web-only @types (google.maps/react-dom/phoenix) + missing RN modules until `cd mobile && npm install`; identical on a stashed clean tree → environmental, not a regression). Working branch `claude/sleepy-goldberg-inqmdk` already at origin default tip `28b708a`, 0/0 divergence. **Scorecard (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B — authed-axe + F7 screenshots; business_case_strength B — without-annual ARR ~$99.9K < $100K floor). GROWTH pre_launch 0/null → no lever signal. **Scouts CLEAN / DROPPED (verified against HEAD):** security/RLS CLEAN (mockups product_ids/bundle_id IDOR already FIXED — bound to room_id:554-584; all migrations ENABLE RLS through 029; next 030). Web-reliability 3× `.single()`→`.maybeSingle()` candidates DROPPED — memory-store `.single()` returns `{data:rows[0]||null, error:null}` (never throws/errors) and code reads only `.data` (null-safe) → intentional graceful degradation, no crash either backend. Design/a11y 5× hardcoded emerald/amber→token DROPPED (decorative/intentional token-churn, "indirect" contrast = not a real WCAG fail). Marketing 5× strategy-DOC candidates DROPPED (don't move a funnel metric → below bar). AI-pipeline 5× DROPPED — 3 were BACKWARDS (LOW→HIGH thinking increases that VIOLATE the cost contract; a judge/summarizer sub-step legitimately runs cheap), 1 low→minimal unverifiable quality change (no eval keys), 1 computer-use agent-loop seed/timeout (drives a LIVE Browserbase browser — inherently non-deterministic, already wall-clock-guarded, SDK-direct by design, unrunnable headlessly). Store-compliance app.json notification purpose-string DROPPED (verified via Expo v56 docs — iOS notifications need NO usage description; the proposed `notificationPermissionDescription` key does not exist). | **Shipped 4 (all file-disjoint, all both-Sonnet-APPROVED — 8 reviews, 0 re-review cycles; 2 non-blocking reviewer suggestions folded in):** **(1) COMPLIANCE/D4 (web)** `app/billing/upgrade/page.tsx` — added the subscription auto-renewal disclosure ("Billed monthly. Renews automatically until you cancel.") at the checkout/upgrade ENTRY point (Apple 3.1.2 / Google Play); the pricing page already disclosed it but the upgrade page — where users land for Stripe checkout — did not; one-time Apartment tier correctly shows none; type-safe `"renewalNote" in copy` narrowing over the `as const` union. **(2) COMPLIANCE (mobile)** `mobile/src/components/paywall-sheet.tsx` — the paywall legal text now states the recurring price+period (Apple 3.1.2(a)) via the already-shown `selectedOption.price`, tracking the selected plan, with a safe fallback when RC offerings haven't loaded; wording polished per both reviewers to drop a "cancel…cancel" repetition. **(3) PRIVACY/correctness (mobile)** `photo-session.ts` + `settings.tsx` + `use-session.ts` — module-scope `_pendingImageUri`/`_pendingRoomType` outlived an auth session, so on a shared device the next user could see the prior user's pending room photo (results.tsx reads via non-consuming `peek*` on mount); added `clearPendingSession()`, called on both Settings sign-out/delete success paths AND (per both reviewers' one substantive note) at the ROOT `onAuthStateChange` SIGNED_OUT boundary — also covers forced/token-expiry sign-outs that never pass through the UI handlers. **(4) COVERAGE/F2 (tests)** `__tests__/validation/pairwise-proportions.test.ts` + `set-math.test.ts` — two mutation-provable validation branch tests: the 5-chair dining-table seat-length rule (odd count the 6-chair tests can't reach; kills a `(chairs-2)`→`(chairs-3)` off-by-one) and the duplicate price-proximity mid-band (10–20% apart; existing test only exercised the <10% band). | **All 4 both-Sonnet-APPROVED** (D-correctness reviewer EMPIRICALLY mutation-tested both tests — sed the mutation → run → confirm only-new-fails → revert; C reviewers grep-audited all signOut sites; B reviewer traced JSX whitespace). Merged-tree final gate GREEN: web tsc clean, **2163 tests** (2161 +2), determinism, `eslint .` 0 (touched); mobile tsc + eslint clean (deps installed). **No ROADMAP box ticked** — D4/D compliance is partial-surface polish (not a full Track D item); F2 already [x]; the privacy fix is correctness hardening, not a specific box. **No migrations, no secrets, no new PENDING_OPS** (store screenshots/plist/support-email + Stripe portal activation + DATA_BACKEND cutover + annual-billing migration 021 remain the same already-tracked human-gated items). **Lessons:** (1) A `.single()`-vs-`.maybeSingle()` "silent failure" candidate is only real if the code reads `.error` — here it reads only `.data` (null-safe) and the memory store never errors, so it's intentional graceful degradation, not a bug: verify the actual store behavior before trusting the scout's premise. (2) An AI-pipeline scout proposing LOW→HIGH thinking is usually BACKWARDS under a cheapest-by-default cost contract — a judge/summarizer sub-step of a HIGH task legitimately runs cheap; increasing it violates the contract + harness-ratchet. (3) Verify config-key candidates against the versioned SDK docs before adding — the proposed iOS `notificationPermissionDescription` doesn't exist in Expo v56 and iOS notifications need no usage string; the WebFetch check saved a bogus change. (4) Cold-container `npx tsc` in /mobile fails on root web-only @types + missing RN modules until `cd mobile && npm install` — confirm against a stashed clean tree (identical failure = environmental) before treating mobile type errors as a regression. | **6 file-disjoint value-bar changes** (baseline test-fragility F1 + cron double-send claim-before-send F4.1 + cache-bound coverage F2 + web auto-renewal disclosure D4 + web privacy retention D2 + account cancel-path dead-end D). DEEP AUDIT not due (ran Run 95; next ~Run 99) → 6-Haiku-scout sweep; **security/RLS + web-perf lenses CLEAN** (all migrations ENABLE RLS through 029, next 030; hot paths already parallelized; no churn). **Baseline gate RED on entry — 8 `deepseek-conversion` tests failed LOCALLY** (investigated FIRST, before any new work): the Margin telemetry emit (#603) fires a TRAILING `fetch` to the ingest endpoint whenever the meter is live (`MARGIN_INGEST_KEY` set AND not offline — `isOffline()` = `CI` or `E2E_AUTH_STACK`); the test's `lastRequestBody()` used `.at(-1)`, grabbing the telemetry POST (no `.messages`) instead of the model request. CI stays green because CI=1 disables the meter. Proven by instrumenting the fetch mock, not guessed. **Scorecard (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B — authed-axe + F7 screenshots; business_case_strength B — without-annual ARR ~$99.9K < $100K floor). GROWTH pre_launch 0/null → no lever signal. **Dropped/deferred:** mobile candidates (belt-and-suspenders on an untested harness); webhook fire-and-forget (intentional waitUntil + previousStatus guard); waitlist confirm UPDATE-before-SEND (one-time token, low value). | **Shipped 6 (all file-disjoint, all both-Sonnet-APPROVED — 4 needed a 2nd review cycle, every REQUEST_CHANGES a REAL gap):** **(1) TEST-FRAGILITY/F1** `__tests__/ai/deepseek-conversion.test.ts` — `lastRequestBody()` now selects the fetch whose URL starts with `https://api.deepseek.com` instead of `.at(-1)`, so a live Margin telemetry emit can't shadow the model request; 13/13 pass locally (were 5/13) AND in CI; no production code touched, guard tests untouched. **(2) RELIABILITY/F4.1** `app/api/cron/{activation,habit,winback}-emails/route.ts` + new `cron-activation-emails.test.ts` (route had NO test) + habit/winback tests (+15) — CLAIM-BEFORE-SEND: INSERT the (user_id,stage) marker BEFORE the send (unique constraint from migration 025 = one winner; duplicate → skip, never send; send-failure → compensating DELETE so a later run retries), converting at-least-once double-send (crash between send & insert) into at-most-once. 2nd cycle: both reviewers caught a real `dry_run`-accuracy regression (sendEmail also force-dry-runs a marketing stage when `EMAIL_PHYSICAL_ADDRESS` is unset — the anticipated near-term deploy state — so a claim-time snapshot records `false` for a suppressed send) → reconcile to the authoritative `result.dryRun` after send (targeted UPDATE only on mismatch), + 2 mutation-provable reconcile tests. **(3) TEST/F2** new `__tests__/validation/lookup-color-cache.test.ts` — covers the untested 500-entry eviction of the in-process LLM color cache in `lookupColorAsync`; mutation-verified (`if(size>500)`→`if(false)` fails it). **(4) COMPLIANCE/D4** `app/pricing/page.tsx` — auto-renewal footnote on the Pro ($49/mo) tier (previously the only recurring tier with NO disclosure); 2nd cycle: original "cancel anytime" overclaimed a self-serve portal that 502s (PENDING_OPS) → tightened to "Billed monthly. Renews automatically until you cancel." **(5) COMPLIANCE/D2** `app/privacy/page.tsx` — new "How long we keep it" retention section grounded in `docs/app-privacy.md`; 2nd cycle: original single bullet overclaimed item-level deletion for account-data/design-history → split into 4 precise clauses (item-level delete scoped to room photos only). **(6) COMPLIANCE/D** `app/account/subscription-card.tsx` — the "cancel anytime through Stripe's portal" copy pointed only to the gated (502) portal with a dead-end error branch; now presents the working email-support path (hello@aptdesignerai.com) in copy + failure state; 2nd cycle: first version collapsed 401/429/500/502 into one wrong "unavailable" string → made status-aware (email fallback only for 502 + network catch). | **All 6 both-Sonnet-APPROVED** (12 initial reviews + 6 re-reviews; reviewers empirically reproduced the deepseek failure, re-ran the cache + reconcile mutations, and cross-checked every portal error status). Merged-tree final gate GREEN: tsc clean, **2161 tests** (2143 +18), determinism, `eslint .` 0 (touched), mobile `tsc --noEmit` clean (untouched); package-lock fsevents `dev:true` install artifact reverted. **No ROADMAP box ticked** — F4.1 needs the full seeded-E2E harness; F1/F2 already [x]; D2/D4/D-cancel-path are partial-surface compliance polish. **No migrations, no secrets, no new PENDING_OPS** (unique constraint already in migration 025; Stripe portal activation + store screenshots/plist/support-email remain the same already-tracked human-gated items). **Lessons:** (1) a baseline test green in CI but red locally is a real bug — fix the test (select the fetch by URL), don't paper over "just my env"; instrument the mock to PROVE the cause first. (2) Claim-before-send is right for at-least-once cron side-effects, but recording provider metadata (dry_run) at claim time is a trap — treat the send's own `result.dryRun` as authoritative. (3) Compliance/marketing copy must be honest about CURRENTLY-working mechanisms, not intended ones (two "cancel anytime" overclaims fixed). (4) Every REQUEST_CHANGES this run named a REAL gap — maker≠checker earned its keep. |
| 2026-07-17 | Run 95 — **DEEP AUDIT (8-lens, due — last ran Run 91) + 4 file-disjoint value-bar changes**. Ran 8 Haiku deep-audit lenses over the WHOLE codebase (correctness/dead-code, security/RLS, performance, a11y/design-bar, test/eval coverage, monetization/entitlements, mobile Expo, artifact-freshness/config-health) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm install` root + `cd mobile && npm install` (tsc, **2136 tests**, determinism, eslint 0, mobile `tsc --noEmit` clean; working branch already at origin default tip `fa5613a`, 0/0 divergence). **Scorecard (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B — authed-axe + F7 screenshots; business_case_strength B — without-annual ARR ~$99.9K < $100K floor). **DEEP AUDIT result:** correctness / security-RLS / monetization-entitlements lenses **CLEAN** (successful no-ops — all migrations ENABLE RLS through 029, every admin-client call site legitimately shared/internal, all write+LLM routes rate-limited, no IDOR/bypass/fail-open, no secret leak; the scorecard's stale as_of-07-13 mockups product_ids/bundle_id IDOR is already FIXED — bound to room_id with 404). **DROPPED as churn/risk (verified against live tree):** perf analyze-apartment project+rooms parallelize (~150ms vs 300s pipeline → negligible); perf O(n²) diagnosis/search cross-room `.find` (≤5 rooms → negligible → churn); a11y global-error.tsx hardcoded colors/focus (INTENTIONAL CSS-free top-level crash boundary — tokenizing makes it LESS robust → anti-fix); a11y topbar step-indicator role (desktop-only decorative, breadcrumbs convey location); a11y emerald completed-state color (intentional widespread success color, value correct → token churn); artifact `npm audit fix --force` (pulls canary Next.js 16.3 → not autonomous-merge-safe, needs human-in-loop dep bump); mobile results.tsx client field-guard (SUPERSEDED by the server-side fix below — root-cause + testable); G4 server-side login lockout (valuable but architectural + session/cookie handling unverifiable headlessly since auth is mocked → deferred named follow-up). | **Shipped 4 (all file-disjoint, all both-Sonnet-APPROVED — 8 reviews, zero re-review cycles; 1 non-blocking comment nit fixed):** **(1) PERF/Track A3** `app/api/saved-designs/route.ts` — the two independent `full`-stage reads (candidate_products + product_bundles, both keyed only on room_id) ran sequentially on the retention-critical save the user is blocked on; now `Promise.all` (position-destructured, both `.error` fields still checked before persist, determinism unchanged) → one DB round-trip instead of two. **(2) TEST/Track F2** `__tests__/scoring/calibration.test.ts` — cover the previously-untested invalid-mean guard of `expandScore()` (`mean < 0 || mean > 10 || isNaN` → return score unchanged); the mean is fed unvalidated from the drift monitor into `calibrateScore`, so a degenerate distribution would otherwise emit NaN/garbage scores; mutation-verified (both reviewers reverted the guard → test fails). **(3) DOCS/Track F5** `.env.example` — README's env table lists STRIPE_SECRET_KEY/STRIPE_WEBHOOK_SECRET/REVENUECAT_SECRET_KEY as required and tells devs to copy `.env.example`, but the example omitted ALL 7 billing vars the code reads (3 secrets + 3 STRIPE_PRICE_ID_* + ANNUAL_BILLING_ENABLED); added a documented Billing section (empty placeholders, annual gate note mirrors migration 021) so a fresh setup isn't a silent-500 trap. **(4) RELIABILITY/Track F4.1** `app/api/mobile/analyze/route.ts` + new `__tests__/api/mobile-analyze-validation.test.ts` — the mobile core-journey analysis endpoint validated `summary` + 5 array fields but NOT `style_name`/`design_direction`, which the mobile results screen renders verbatim in their own cards; a partial LLM response omitting either left the user on a 'done' screen with blank cards, no error, no retry. Now fails loud (500, mirroring the summary guard) so the client's non-200 path surfaces the retryable error; 6-case route test mutation-verified (neutering the guard fails exactly the 4 field cases). | **All 4 both-Sonnet-APPROVED (mobile-analyze + calibration reviewers independently re-ran the mutations to confirm mutation-provability; env reviewer flagged a 400/503→500 comment inaccuracy → fixed).** Merged-tree final gate GREEN: tsc clean, **2143 tests** (2136 +7), determinism, `eslint .` 0 (whole repo), mobile `tsc --noEmit` clean. **No ROADMAP box ticked** — the DEEP AUDIT (F5) is a recurring cadence not a per-run checkbox; A3/F2 already [x]; F4.1 needs the full seeded-E2E harness. **No migrations, no secrets, no new PENDING_OPS** (Stripe price IDs already tracked in PENDING_OPS). **Lessons:** (1) A scout "missing field guard" gap on a client screen is often best fixed at the SERVER contract — testable on the web harness, root-cause — rather than the untestable client (mobile has no test harness). (2) Intentional CSS-free crash boundaries (global-error.tsx) must NOT be "design-system-ified" — that's an anti-fix that reduces robustness. (3) Blanket `npm audit fix --force` is not autonomous-merge-safe when the fix pulls a canary major. | **4 file-disjoint value-bar changes**: saved-designs read-error integrity (Track F4.1) + Stripe-webhook tier validation (Track C/G) + 2 core-path coverage tests (Track F2). No DEEP AUDIT due (ran Run 91; next ~Run 95) — ran a 6-Haiku-scout sweep (web-reliability/side-effects, security/RLS+rate-limit, test/eval coverage, mobile, store-readiness/a11y/artifact-freshness, entitlements/monetization/growth) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm install` root + `cd mobile && npm install` (tsc, **2126 tests**, determinism, eslint 0, mobile `tsc --noEmit` clean; working branch already at origin default tip `b3fb18b`, HEAD contains default tip). **Scorecard (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B — authed-axe + F7 screenshots; business_case_strength B — without-annual ARR ~$99.9K < $100K floor). Security + rate-limit scout **CLEAN** (all migrations ENABLE RLS through 029, next would be 030; all write + expensive/LLM routes rate-limited; no IDOR residuals). Store/a11y/artifact scout **CLEAN**. GROWTH pre_launch 0/null → no lever signal. **Verified against live tree / DEFERRED / DROPPED:** cron double-send race (activation/habit/winback — REAL narrow F4.1 gap, but the correct fix (claim-before-send + compensating-delete) spans 3 files with a genuine drop-vs-dup trade-off → its own focused run, DEFERRED not forced); diagnosis project-fetch error (intentional `project?.` graceful degradation → DROPPED); lookupDurability prefix-strip test (NOT mutation-provable — the substring fallback always shadows it → DROPPED); web.ts invalid-date parse (dead — current_period_end always null → DROPPED); soft save-count race (documented-intentional → DROPPED); mobile hardcoded LED/splash colors (values already correct → token churn, DROPPED). | **Shipped 4 (all file-disjoint, all both-Sonnet-APPROVED — 8 initial + 2 re-reviews; 2 REQUEST_CHANGES caught real gaps):** **(1) RELIABILITY/Track F4.1** `app/api/saved-designs/route.ts` + new `__tests__/api/saved-designs-existing-persistence.test.ts` — two persisted-data reads ignored their `error` field: the `existing` upsert lookup (errored existence check fell through to INSERT → DUPLICATE saved_designs row) and the `rooms` read (errored read persisted an "Untitled Room"/null-room_type snapshot); both now fail loud (500 via apiError); the `project` metadata read correctly LEFT best-effort; completes the read-error class Run 93 started. Reviewer A caught that the existing-lookup test wasn't mutation-provable (blanket table-keyed error 500'd via the pre-existing insert-error check — right result, wrong reason) → fixed with a call-site-aware mock (SELECT errors, INSERT succeeds) + an insert-never-called assertion; re-verified. **(2) BILLING/Track C+G** `lib/billing/stripe.ts` + tests — webhook `metadata.tier` was blind-cast to a TS type; an invalid/replayed tier 500'd on the stripe_customers CHECK constraint → Stripe retries indefinitely (stuck webhook, entitlement divergence). Added `normalizeBillingTier()` (validates vs migrations 018+021): checkout → safe no-op, subscription → "pro" fallback. Reviewer A caught the checkout no-op was now SILENT (harder to detect than the 500) → added loud logging for the PRESENT-but-invalid case (error on checkout, warn on subscription; absent stays quiet) + doc nit; re-verified. **(3) TEST/Track F2** `__tests__/validation/durability-map.test.ts` — 3 untested REAL branches of `resolveLifestyleFlags` (notes keyword scan / work_from_home / hosting), mutation-provable. **(4) TEST/Track F2** `__tests__/scoring/harmony-composite.test.ts` — the untested `title`-match branch of `computePairwisePenalty` (a core scoring fn called in prod from validation-agent), mutation-provable. | **All 4 both-Sonnet-APPROVED** (the 2 REQUEST_CHANGES — non-mutation-provable test, silent-no-op-without-log — were real and fixed → re-APPROVED; both re-reviewers empirically reverted each guard to confirm the tests now fail). **Single-branch per this run's git constraint → ONE PR to default.** Merged-tree gate GREEN: tsc, **2136 tests** (2126 +10), determinism, eslint 0 (touched), mobile unaffected; saved-designs test stress-run 25× → 0 flakes. **No ROADMAP box ticked** — all 4 are reliability/billing-robustness/coverage hardening; no full Track item completed (F4/F4.1 need the full seeded-E2E harness; F2 already [x]; Track C already [x]). **No migrations, no secrets, no new PENDING_OPS.** **Lessons:** (1) A "returns 500 on a read error" test is only mutation-proof if the mock models the FAILURE the fix targets (SELECT-fails/INSERT-succeeds → duplicate row), not a blanket error the pre-existing insert-check also catches. (2) Converting a loud 500 into a safe 200 no-op trades one divergence for a harder-to-detect one unless the present-but-invalid case is logged loud (distinct from the routine absent/unhandled case). (3) A coverage candidate on a shadowed branch (lookupDurability prefix-strip) isn't mutation-provable → coverage-theater; verify a branch is observable first. (4) A growth-funnel double-send fix with a real drop-vs-dup trade-off across 3 files is its own focused run, not a rushed 5th change. |
| 2026-07-16 | Run 93 — **3 file-disjoint value-bar changes**: two side-effect-integrity/reliability fixes (Track F4/F4.1) + one entitlement-path robustness fix. No DEEP AUDIT due (ran Run 91; next ~Run 95) — ran a 7-Haiku-scout sweep (validation/math correctness, security/RLS, mobile correctness, growth-E7 lifecycle, web-reliability/side-effects, a11y/store-readiness, test/eval coverage) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm install` root + `cd mobile && npm install` (tsc, **2119 tests**, determinism, eslint 0, mobile `tsc --noEmit` clean; working branch reset to origin default tip `457608a`, 0/0). **Scorecard (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C — DATA_BACKEND cutover; design_taste B — authed-axe + F7 screenshots; business_case_strength B — without-annual ARR ~$99.9K < $100K floor). Security scout **CLEAN** (all 29 migrations ENABLE RLS; mockups product_ids/bundle_id bound to room_id — re-confirmed; SECURITY DEFINER search_path pinned; no secret leak). a11y/store scout **CLEAN**. GROWTH pre_launch 0/null → no lever signal. **Verified against live tree / dropped:** bundle-math coffee-table `0.5–1.1` band + spatial-math undersized-room clearance guard (scout called them "bugs" — both are INTENTIONAL tolerance/defensive design, tightening would change production scoring / add false positives → DROPPED); LABELED_DIMENSION_REGEX `inches?` (pipeline emits the `"`-quote form, so "inch"-spelled labeled specs don't fire → fixing a dead path = padding → DROPPED); coverage-scout site-gate + cors "gaps" (already-caught / no-op compile-time cast → FALSE POSITIVES); web.ts tier-cast + stripe pro_annual coverage (mutation caught by existing apartment test / gated-off tier → low-value → SKIPPED); mobile use-free-quota unmount guard (React-18 setState-after-unmount is a safe no-op, no userId stale-race like the Run-92 entitlements fix → below the bar → DROPPED, consistent with the Run-92 batch-padding lesson). E7: no CLEAN disjoint headless-safe change (paid-engagement D2 has an owner-fill placeholder; referral F1 wants a 1hr-delayed send mechanism, not an inline send on the shared PATCH endpoint) → deferred, not forced. | **Shipped 3 (all file-disjoint, all both-Sonnet-APPROVED — 6 reviews, zero re-review cycles):** **(1) RELIABILITY/Track F4.1** `app/api/saved-designs/route.ts` + new `__tests__/api/saved-designs-full-persistence.test.ts` (+3) — a "full"-stage save read candidate_products + product_bundles with `data ?? []`, so a DB read ERROR silently wrote a zero-product snapshot yet returned HTTP 200 (silent data loss on the retention-critical "revisit saved designs" journey); now captures both read errors and returns a generic 500 (Track G3 hygiene) so the client can retry; 3-case regression test drives the real POST handler (products-error/bundles-error→500, both-ok→200), mutation-provable. **(2) RELIABILITY/Track F4** `app/projects/[projectId]/rooms/[roomId]/products/page.tsx` — `handleIngest` had no `!res.ok` branch and no catch, so a failed Add-Product gave the user zero feedback (spinner clears, silent failure); now mirrors the two sibling handlers in the same file (`handleEvaluate`/`handleStatusChange`) — toast.error on `!res.ok` + on a network throw, `finally` still clears the spinner. **(3) ENTITLEMENTS/robustness** `lib/entitlements/server.ts` + `__tests__/entitlements/server.test.ts` (+4) — `hasProEntitlement` read `data.subscriber.entitlements[...]` UNGUARDED after a successful parse, so a 200 with a malformed body (`{}`/`{subscriber:null}`) threw an uncaught TypeError on the entitlement critical path (crash/500) instead of the documented fail-open; guarded the shape + fail OPEN (consistent with the parse-error + 5xx paths); a well-formed empty entitlements object still returns false (unchanged); +4 mutation-provable tests. | **All 3 MERGED to default via one PR (#647 → squash `59afc19`; required checks green).** Merged-result gate re-run GREEN: tsc clean, targeted suites 19/19, integrated **2126 tests** (2119 +7). **All 6 reviewers APPROVE first-pass, zero re-review cycles** (Change A reviewer reverted the fix and reproduced 500→200 mutation; Change C security reviewer confirmed `rcAppUserId` is server-derived — no client-triggerable entitlement-bypass, blast radius bounded to the free-save-limit). **No ROADMAP box ticked** — all 3 are reliability/robustness hardening on critical paths; no full Track item completed (F4/F4.1 remain [ ] pending the full E2E harness). **No migrations, no secrets, no new PENDING_OPS.** **Lessons:** (1) Verify scout "correctness bugs" on the validation layer against the live tree BEFORE building — 2 of this run's top scout finds were intentional tolerance/defensive design, not bugs; shipping them would have changed production scoring. (2) A React-18 setState-after-unmount is a safe no-op — a "mount guard" fix is defensive padding UNLESS there's a real stale-race (like Run 92's userId-change clobber); the device-global free-quota hook has none → correctly dropped. (3) Regex `X?` scopes one char (`inches?`≠"inch"); but before fixing such a path, confirm it actually FIRES in production (the labeled parser's real input uses `"` marks → the "inch" branch is dead → not worth a change). (4) When E7 has no clean disjoint headless-safe change (owner-placeholder copy / delayed-send mechanics), DEFER it — a quiet coherent run beats forcing a half-sequence cron with a fuzzy trigger. |
| 2026-07-16 | Run 92 — **6 file-disjoint value-bar changes** across Track E7 (retention), Track F/design (a11y ×2), Track D (store compliance), Track B (mobile correctness), Track F2 (validator coverage). No DEEP AUDIT due (ran Run 91; next ~Run 95) — ran a 7-Haiku-scout sweep (test/eval coverage, web-reliability/side-effects, security/RLS, mobile, growth-E7, store-readiness, a11y/design-bar) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm install` root + `cd mobile && npm install` (tsc, **2108 tests**, determinism, eslint 0, mobile `tsc --noEmit` clean; working branch `claude/sleepy-goldberg-yxs4ae` at origin default tip 94b9cc5, 0/0). **Scorecard (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C, design_taste B, business_case_strength B). Security scout **CLEAN** (all 29 migrations ENABLE RLS; mockups product_ids/bundle_id bound to room_id — re-confirmed; no secret leak). **Verified against live tree / dropped:** product-scorer category-calibration test (Reviewer B proved the `if(category)` branch was ALREADY covered by `__tests__/integration/scoring-pipeline.test.ts` calling with "rug"/"sofa" → redundant → DROPPED); saved-designs partial-persistence + search/area-analysis Promise.all reliability candidates (real but need a route-orchestration harness → deferred); LABELED_DIMENSION_REGEX `inches?` follow-up (borderline micro, not shipped). | **Shipped 6 (all file-disjoint, all both-Sonnet-APPROVED — 12 initial + 6 re-reviews across 2 cycles):** **(1) GROWTH/Track E7** new `app/api/cron/habit-emails/route.ts` + `buildHabitEmail1/2/3` in `lib/email/templates/lifecycle.ts` + `vercel.json` (12:00 UTC) +9 tests — post-first-analysis habit sequence B1/B2/B3 (docs/email-lifecycle.md Seq 2) for users who ran their first analysis 1/3/7d ago and have NOT upgraded, upselling the $29 Apartment plan; mirrors the activation/winback crons (CRON_SECRET auth, first-analysis window via `room_diagnoses.created_at` + owner-chain embed, de-dup, `hasProEntitlementWeb` paid drop-out, opt-out, per-stage idempotency); ships INERT (dry-run until RESEND_API_KEY+EMAIL_PHYSICAL_ADDRESS). Copy verbatim from the doc. **(2) A11Y/Track F** `app/projects/[projectId]/rooms/[roomId]/products/page.tsx` — aria-label on the 3 filter/sort `SelectTrigger`s (Radix combobox has no accessible name from content). **(3) A11Y/Track F** `components/rooms/sourcing-mode-selector.tsx` — Manual/AI/Hybrid selector → role=group + aria-label + per-button aria-pressed (toggle-button-group; revised from role=radio after BOTH reviewers flagged the unmet radiogroup keyboard contract). **(4) COMPLIANCE/Track D** `app/privacy/page.tsx` — deletion timeframe "within 30 days"→"immediately and permanently" to match support+account pages AND the immediate hard-delete in `app/api/user/delete/route.ts` (store-reviewer cross-check consistency). **(5) MOBILE/Track B** `mobile/src/hooks/use-entitlements.ts` — mountedRef+activeUserRef guard on setCustomerInfo/setIsPro/setIsLoading (fixes an unmount write AND a userId-change stale-race where a slow getCustomerInfo() for the old user clobbers fresh entitlement state; revised from an isLoading-only guard after Reviewer A). **(6) TEST/Track F2** `__tests__/validation/code-compliance.test.ts` +2 — egress-window partial-data null guards (only-height / only-width present); pins all three `winX===null\|\|` guards + the outer `\|\|` (revised: dropped a redundant 3rd case after Reviewer A). | **All 6 both-Sonnet-APPROVED.** maker≠checker earned its keep: sourcing-mode BOTH reviewers rejected the role=radio anti-pattern (fixed → group/aria-pressed, both re-APPROVED); mobile Reviewer A caught the isLoading-only guard leaving refresh()'s setters exposed to a userId stale-race (fixed → comprehensive ref guard, both re-APPROVED); code-compliance Reviewer A caught a redundant 3rd test pinning no guard + a message overclaim (dropped, reworded, both re-APPROVED); product-scorer test DROPPED entirely when Reviewer B proved the branch was already covered (redundant padding). Integrated gate GREEN (tsc, **2119 tests** = 2108 +9 habit +2 code-compliance, determinism, eslint touched, mobile tsc). **All 6 MERGED to default via one PR** (single-branch per this run's git constraint). **No ROADMAP box ticked** — E7 advanced (habit B1-B3) but stays [ ] (upgrade-paywall C1-C3 + referral F1 remain); others are a11y/compliance/correctness/coverage hardening; no full Track item completed. **No migrations, no secrets** (habit cron reuses CRON_SECRET/RESEND_API_KEY/EMAIL_PHYSICAL_ADDRESS; PENDING_OPS set-cron-secret updated to list all three crons). **Lessons:** (1) A coverage test must be checked against the WHOLE suite, not just the target file — the calibration branch looked uncovered in product-scorer.test.ts but the integration test already exercised it; my scout missed it → Reviewer B caught the redundancy. Grep all callers before claiming a branch is dark. (2) `role="radio"`/`radiogroup` is an interaction CONTRACT (roving tabindex + arrow keys), not a label — for an independent-toggle set use role=group + aria-pressed, which matches native button keyboard behavior with zero new JS. (3) An unmount guard on ONE setter is incomplete — the same async op's OTHER setters (and a userId-change stale-race) need the same guard; put it inside the shared refresh(), not just the effect. |
| 2026-07-16 | Run 91 — **DEEP AUDIT (8-lens, due by the 24h rule since Run 88 on 07-14) + 3 file-disjoint value-bar changes** on the validation/math enforcement layer (Track F coverage ×2 + Track F/A correctness ×1). Cold container; working branch at origin default tip `4228ea5` (fetch confirmed HEAD == origin default, 0/0). Baseline gate GREEN after `npm install` root + `cd mobile && npm install` (tsc, **2105 tests**, determinism, eslint 0, mobile `tsc --noEmit` clean). **DEEP AUDIT (8 Haiku scouts, whole-codebase):** correctness/dead-code **CLEAN**, security/RLS **CLEAN** (all 29 migrations ENABLE RLS; mockups product_ids/bundle_id bound to room_id — re-confirms the scorecard `security_rls` finding is STALE/fixed #612), stubs/critical-path-reality **CLEAN** (AI pipeline + Stripe + RevenueCat all wired to real awaited calls), perf (4 fetch-parallelization candidates — 3 DEFERRED: search/route + search/stream have NO handler test, only logic-replicas → unverifiable headless), a11y/design-bar (3 borderline CLS/token micro-items, not shipped), test/eval coverage (real validation-layer gaps → this run's work), deps/artifact-freshness (env-var doc gap — but checkout already fails loud, so doc-only/borderline; pricing+privacy docs verified CONSISTENT). **Scorecard read (DATA, as_of 2026-07-13):** overall C, ship_gate false; three ship_critical below A unchanged & human/CI-gated (functional_reality C, design_taste B, business_case_strength B). GROWTH pre_launch 0/null → no lever signal. **False positives caught by verifying the live tree (NOT shipped):** mobile scout's mockups-IDOR (stale/fixed) + "no rate limiting on paid routes" (all four carry checkRateLimit+checkDailySpend); test scout's parseRoomDimensions "inch" (line-182 regex already includes `inch`; `|| unit==="inch"` is a dead branch — outcome already correct) + bundle-math zero-price mutation (already killed by the existing :432 test; the `mean===0` branch is unreachable dead code). | **Shipped 3 (all file-disjoint, all both-Sonnet-APPROVED — 6 reviews, zero re-review cycles):** **(C1) VALIDATION/Track F+A** `lib/validation/spatial-math.ts` +3 tests — `UNIT_ALT` used `inches?` (matches "inche"/"inches" but NOT bare "inch"), so a positional spec `6inch x 4inch` matched only "6in", failed the separator, and parsed as a 6×6 SQUARE (mis-scoring footprint coverage/clearance); fix `inches?`→`inch(?:es)?` (kept longest-first before `in`); mutation-provable (both reviewers reverted the char-class and reproduced the 6×6 bug). **(C2) TEST/Track F** `__tests__/agents/escalation-ladder.test.ts` — assert `onTier(i,label,false)` fires on the THROW path (catch branch) so a caller's oscillation/velocity counter advances on a thrown tier (not only a rejected one); the cost-contract-critical deterministic escalation layer; existing tests never asserted the catch-site call → a mutation dropping it was uncaught; mutation-verified. **(C3) TEST/Track F** `__tests__/validation/bundle-math.test.ts` — cover the undefined-height auto-pass in the height-based relational scale rules (side_table↔sofa / dining_chairs↔dining_table); prior scale_balance tests used only width rules; asserts a missing-height bundle scores strictly above a concrete height mismatch (1.0 vs 0.5), dropping the guard (NaN→false) collapses them; mutation-verified. | **DROPPED on review (clean tree):** a 4th candidate — perf-mockups `app/api/mockups/route.ts` room+diagnosis Promise.all — got Reviewer A APPROVE (correctness) but Reviewer B **REQUEST_CHANGES on VALUE** (a ~1 DB round-trip saved before a multi-second AI render is imperceptible → reads as change-count padding). Per the rules (Reviewer-B value rejection I couldn't raise) → ABANDONED. **All 3 MERGED to default via one PR** (single-branch per this run's git constraint). Integrated gate GREEN: tsc, **2108 tests** (2105 +3), determinism, eslint. **No ROADMAP box ticked** — all 3 are validation-correctness / coverage hardening; no full Track item completed; F5 (periodic DEEP AUDIT) is a recurring cadence, not a per-run checkbox. **No migrations, no secrets, no new PENDING_OPS.** **Lessons:** (1) A largely-CLEAN deep audit is a valid, successful result — do NOT pad it; 3 solid changes + dropping the borderline 4th is the disciplined outcome. (2) Verify a perf-parallelization candidate has a real HANDLER test, not a logic-replica test, before shipping it headless. (3) "Marginal relative to the dominant cost" is a Reviewer-B value rejection even when the code is correct — perf clears the bar only when the parallelized work is on the critical-latency path. (4) `X?` scopes over the single preceding char: `inches?`="inche"+optional-s (never matches "inch") → use `inch(?:es)?`. | **5 file-disjoint value-bar changes** across the determinism contract, Track E7 (retention), Track F2 (validator coverage ×2), and Track A/F4 (core-flow reliability). No DEEP AUDIT due (ran Run 88; next ~Run 92) — ran a 7-Haiku-scout sweep (security/RLS+rate-limit, AI-pipeline/cost/determinism, web-reliability/side-effects, mobile, test/eval coverage, growth-E7/conversion, a11y/store/design-bar) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm install` root + `cd mobile && npm install` (tsc, **2082 tests**, determinism, eslint, mobile `tsc --noEmit` clean; reset to origin default tip 05dea24). **Scorecard read (DATA, as_of 2026-07-13):** overall C, ship_gate_met false; three ship_critical below A unchanged & human/CI-gated — functional_reality C (in-memory→Supabase cutover), design_taste B (authed-axe + F7 screenshots), business_case_strength B (without-annual ARR ~$99.9K < $100K floor). The scorecard's `security_rls A+→A` mockups-IDOR finding was **STALE — already fixed by Run 86 #612** (verified live: `app/api/mockups/route.ts` binds both `bundle_id` and `product_ids` to `room_id` at :559-580) → no action. **GROWTH pre_launch, 0/null funnel** — no headless growth action. **Rejected/deferred (verified against live tree):** mobile scout's "TS strict error in settings.tsx" (FALSE POSITIVE — baseline `mobile tsc` clean); next/image 13-file sweep (layout-regression risk, borderline); iOS-privacy `NSPrivacyCollectedDataTypes` (needs Apple-enum research; empty array is valid — the binding requirement is the App Store Connect labels, D2); paywall money-back-copy reframe (borderline cosmetic); security scout CLEAN (no-op). | **Shipped 5 (all file-disjoint, all both-Sonnet-APPROVED — 12 reviews incl. 2 re-review cycles):** **(#633) DETERMINISM/Track F** `lib/ai/gemini.ts` + new `lib/ai/retry-delay.ts` +6 tests — the Gemini provider's inline 429 backoff used an UNGATED `Math.floor(Math.random()*baseDelay)+500` (unlike `lib/ai/retry.ts` which gates jitter behind `!DETERMINISTIC`), silently breaking reproducibility on retried calls. Extracted a pure, unit-tested `computeRetryDelay()` gating the jitter under DETERMINISTIC; server/transport backoff byte-identical; RNG-never-called-in-deterministic-mode is mutation-provable. No floor/cost-contract change. **(#634) GROWTH/Track E7** new `app/api/cron/winback-emails/route.ts` + `vercel.json` +7 tests — win-back E2 (day 7) + E3 (day 30) daily cron for cancelled subscribers (E1 already fires on the Stripe-webhook cancellation), mirroring the activation cron: CRON_SECRET bearer auth, dry-run safe, per-(user,stage) idempotency via `user_email_stages`, CAN-SPAM opt-out; queries `stripe_customers status='cancelled'` with `updated_at` as the cancellation-time proxy (same as `lib/growth/metrics.ts`); reuses shipped `buildWinBackEmail2/3` + `winback_2/3` stages (no new copy). Ships INERT. **(#635) TEST/Track F2** `__tests__/validation/outlet-reach.test.ts` +3 — hardwired-fixture skip (chandelier), soft-anchor reachability (`aSoft\|\|bSoft`), vague-placement benefit-of-doubt; mutation-provable. **(#636) TEST/Track F2** `__tests__/validation/code-compliance.test.ts` +9 — IRC R310.2.1 egress-window (all three `&&` terms proven load-bearing incl. the width-fail case added in re-review) + NEC 210.52(C) kitchen-outlet spacing (48" divisor + `>=` boundary). **(#637) RELIABILITY/Track A+F4** `app/projects/[projectId]/rooms/[roomId]/focus/page.tsx` — surface two silent failures on the core money-path: failed vision-mockup gen now toasts (was a spinner dropped with no feedback); failed batch-search now shows `searchError` + Retry instead of a blank/empty results page (SIDE-EFFECT INTEGRITY / BUILDS≠WORKS). | **All 5 MERGED to default** (verified `git log`: tip 23132af carries #633/#634/#635/#636/#637). Integrated gate GREEN on the merged result: tsc, **2105 tests** (2082 +23), determinism, eslint. **All 5 both-Sonnet-APPROVED; maker≠checker earned its keep twice:** BOTH #637 reviewers independently caught a real blank-page regression (the error branches set `searchError` but never advanced `step` to `results` — the only place the banner renders — so a failed search would strand the user on a blank sourcing screen); fixed in v2 (always `setStep("results")` after the if/else, mirroring the SSE-catch path) → both re-APPROVED. #636 Reviewer A REQUEST_CHANGES on a real mutation-coverage gap (egress had height- and area-fail tests but no width-fail → a mutant dropping the `widthOK` term slipped through); added the width-below-20 test → re-APPROVED (live mutation-verified). **No ROADMAP box ticked** — all 5 are determinism/retention/coverage/reliability hardening; no full Track item completed. E7 advanced (win-back E2/E3) but stays [ ] (activation-habit/upgrade sends + visitor/conversion analytics pulls + per-channel social clients still remaining). **No migrations, no secrets, no new PENDING_OPS** (winback cron reuses the activation cron's CRON_SECRET/RESEND_API_KEY/EMAIL_PHYSICAL_ADDRESS env). **Lessons:** (1) The QUALITY_SCORECARD is DATA and can be STALE — its `security_rls` mockups-IDOR finding (as_of 2026-07-13) was already closed by Run 86 #612; verify every named gap against the live tree before building or you re-fix a solved problem. (2) maker≠checker is load-bearing under auto-merge — two independent Sonnet reviewers caught a self-introduced regression that was WORSE than the bug being fixed (relocated blank page); a single self-review would have shipped it. (3) A scout claim that contradicts a green baseline gate (mobile "TS strict error" vs a clean `mobile tsc`) is a false positive — trust the gate over static reasoning. (4) For an `&&`-conjunction validator, symmetric failure-case coverage (one test per term failing while the others pass) is required to be mutation-complete — a boundary-pass test alone doesn't prove a term is in the conjunction. |
| 2026-07-15 | Run 89 — **6 file-disjoint value-bar changes** across Track A (honesty/FTC), Track F (validation correctness + determinism), Track D (mobile store readiness), Track B (mobile UX + design bar). No DEEP AUDIT due (ran Run 88; next ~Run 92) — ran a 7-Haiku-scout sweep (honesty/marketing-claims, web-reliability, tests/coverage, mobile, perf+determinism, store-readiness/a11y, G4-auth-feasibility) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm ci` + `cd mobile && npm ci` (tsc, **2077 tests**, determinism, eslint, mobile `tsc --noEmit` clean; branch already at origin default tip 12009a7). **Scorecard read (DATA):** overall C, ship_gate_met false; three ship_critical below A unchanged & human/CI-gated — functional_reality C (in-memory→Supabase cutover), design_taste B (authed-axe + F7 screenshots), business_case_strength B (without-annual ARR ~$99.9K < $100K floor). **GROWTH pre_launch, 0/null funnel, owner-connect blockers open** — no headless growth action. **Deferred/rejected (verified against live tree):** G4 login-lockout (feasible but requires converting the critical client-side login to a server route — can't e2e-verify headlessly this run → auto-merge regression risk on the #1 auth path; deferred to a run that can add a route test); C5 search-route parallelization (real ~300ms win but NO route-level test covers the handler orchestration + can't runtime-verify search headlessly → uncaught-regression risk); scene-reconciliation tiebreaker (FALSE POSITIVE — the `seen` Map guarantees unique keys so the `===` tie branch is unreachable → behavior-neutral churn); mobile a11y-labels on photo/results buttons (FALSE POSITIVE — those Pressables already have visible `<ThemedText>` children which RN announces as the accessible name → redundant); callback maxDuration (borderline hardening, cut to keep quality). | **Shipped 6 (all file-disjoint, all both-Sonnet-APPROVED — 12 reviews):** **(C1) HONESTY/Track A** `app/(auth)/login/page.tsx` — remove a FABRICATED testimonial ("Sarah M., Brooklyn NY" + invented quote) from the desktop panel (FTC endorsement / App-Store-rejection risk + the project's own no-fake-testimonials rule); replace with verifiable capability highlights mirroring the already-shipped signup/landing honest-claims pattern (icons `aria-hidden`, no invented metrics/personas). **(C2) VALIDATION/Track F** `lib/validation/spatial-math.ts` +5 tests — positional `parseDimensions` now reads inline-unit specs (`6' x 4'`, `72" x 36"`) where the unit sits BETWEEN the number and the `x` separator; the old regex stopped at the first token → mislabeled as a square (72×72 vs 72×48), silently mis-scoring RUGS (`6' x 9'`, parsed on the proportion-scoring path). Per-token optional units + single-trailing-unit fallback; reviewer empirically mutation-verified (3/4 new tests fail on pre-fix code). **(C3) DETERMINISM/Track F** `lib/design-context/infer-preferences.ts` +1 test — `topFrequent()` sorted by count only; ties are common (most tokens appear once) and the top-N slice feeds a SEEDED LLM preference prompt from an UNORDERED Supabase select (no ORDER BY) → non-reproducible; add an alphabetical tiebreaker (same idiom as the repo's own determinism checker). Mutation-verified. **(C6) STORE/Track D** `mobile/app.json` — add the iOS privacy manifest (`ios.privacyManifests`): NSPrivacyTracking:false + 4 required-reason API categories (UserDefaults CA92.1, FileTimestamp C617.1, SystemBootTime 35F9.1, DiskSpace E174.1); closes the ITMS-91053 App-Store rejection vector. Reviewers verified each reason code traces to an actual dependency PrivacyInfo.xcprivacy in node_modules (RN core, async-storage, expo-device, expo-file-system) and that app.config.ts's `...config` spread preserves it. **(C7) MOBILE-UX/Track B** `mobile/src/components/auth/login-screen.tsx` + `signup-screen.tsx` — empty-field auth submit gave a silent no-op (button only disabled while loading) → now surfaces an explicit error via the existing alert box. **(C8) MOBILE-DESIGN/Track B** `mobile/src/app/results.tsx` — Palette + Materials cards rendered unconditionally as empty titled shells; guard them behind `.length>0` matching the file's What-Works/What-Should-Go pattern. | **All 6 both-Sonnet-APPROVED (12 reviews, zero re-review cycles).** Reviewers independently: re-derived the C2 regex on 30 adversarial inputs + a reverted-code mutation test; confirmed C3's tiebreaker matches the repo's own `check-determinism.ts` idiom + is mutation-killing; verified all 4 C6 reason codes against Apple's table AND the actual dependency manifests; confirmed C7 reuses the existing error UI + C8 matches the sibling conditional pattern; confirmed C1 introduces no new unverifiable claim (all 3 highlights pre-exist verbatim on signup/landing/privacy). Non-blocking notes (recorded, not shipped): C2 singular `"inch"` (no `es`) still mis-parses — PRE-EXISTING, not worsened; C6 App Store Connect privacy "nutrition label" still needs owner completion (→ PENDING_OPS). Integrated gate GREEN (tsc, **2082 tests** = 2077 +5, determinism, eslint, mobile tsc + eslint). **No ROADMAP box ticked** — all 6 are honesty/correctness/determinism/store/UX hardening; no full Track item completed; the three ship_critical blockers stay human/CI-gated. **Single-branch per this run's git constraint → ONE PR. No code migrations, no secrets.** **Lessons:** (1) VERIFY every scout claim against the live tree — 2 candidates were false positives (an "unstable tiebreaker" on a Map with guaranteed-unique keys → unreachable branch; "missing a11y labels" on Pressables that already have text children RN announces). (2) A real perf/security win that CANNOT be runtime-verified headlessly (search route with no orchestration test; server-login conversion) is a deferral, not a ship, under auto-merge — the regression would land uncaught. (3) A fabricated testimonial is a shippable-value FINDING, not cosmetics — it's an FTC/App-Store compliance risk; the honest replacement must reuse only claims already sourced elsewhere. |
| 2026-07-14 | Run 88 — **7 file-disjoint value-bar changes** + **DEEP AUDIT** (8-lens, due Run 88; last ran Run 84). Baseline gate GREEN on entry after cold `npm ci` + `cd mobile && npm ci` (tsc, **2069 tests**, determinism, eslint, mobile `tsc --noEmit` clean; branch reset to origin default tip first). **DEEP AUDIT (8 Haiku scouts, whole-codebase):** correctness/dead-code, security/RLS, perf, a11y/design-bar, tests/coverage, mobile+monetization, dep/config-health, artifact/doc-freshness. **SECURITY CLEAN** (independent 52-route-class sweep: RLS on ~29 tenant tables, all id-routes bound via userOwnsRoom/userOwnsProject, SECURITY DEFINER hardened w/ empty search_path, SSRF blocks, timing-safe token compares, webhook signature-then-write, no secret leak; the two low-sev notes — saved-designs implicit-auth clarity, places/photo "no rate limit" — were FALSE (places/photo is already auth+rate+spend-limited)). **Real findings → this run's work:** correctness scout confirmed the two pre-flagged determinism sorts; a11y scout flagged not-found landmark; artifact scout flagged the stale checklist migrations + email mockups claim; perf scout flagged the diagnosis sequential awaits. **False positives caught by verification (NOT shipped):** dep/config "mobile TypeScript 6.0.3 doesn't exist" (mobile `npm ci` + `tsc` both ran clean — TS6 is real), correctness "products/ingest .single() null deref" (code already uses `?.` + userOwnsRoom guarantees the row), correctness "product-extractor unguarded JSON.parse" (caller wraps it in try/catch → graceful success:false), a11y "account/page missing main" (AppShell already provides `<main>` — adding one = nested double-main). **Scorecard read (DATA):** overall C, ship_gate_met false; three ship_critical below A unchanged & human/CI-gated — functional_reality C (in-memory→Supabase cutover), design_taste B (authed-axe + F7 screenshots), business_case_strength B (without-annual ARR ~$99.9K < $100K floor). **GROWTH pre_launch, 0/null funnel, owner-connect blockers open** — no headless growth action. | **Shipped 7 (all file-disjoint):** **(1) VALIDATION/Track F** `lib/validation/spatial-math.ts` — add a labeled-parse path so `parseDimensions` reads the letter-suffixed retail form (`90"W x 38"D x 32"H`) the pipeline actually emits; before, the positional regex stopped at `90"` and mislabeled items as a 90×90 square, silently NO-OP'ing three pairwise furniture-fit rules (nightstand/side_table height, dining depth) in prod. Axis-letter lookahead `(?=$|[\s,xX×])` handles the compact no-space form (`90"Wx38"D`) while never reading a bare word ("Walnut") as an axis; both-axes-required safety net; +8 tests. **(2)+(3) DETERMINISM** `lib/agents/diagnosis-expansion-pipeline.ts` + `lib/design-context/infer-preferences.ts` — the two review-flagged `created_at`-only latest-diagnosis sorts (no tiebreak) feeding seeded LLM prompts; add `id` to the nested selects and reuse `pickLatestDiagnosis()` (created_at DESC, id DESC). **(4) A11Y/Track F** `app/not-found.tsx` — `<div>`→`<main>` landmark (root 404, no AppShell → had none). **(5) PERF/Track A** `app/api/diagnosis/route.ts` — parallelize two independent best-effort reads (sibling summaries + budget context) via Promise.all on the diagnosis hot path (matches the streaming pipeline's existing pattern; results keyed to named vars → deterministic). **(6) DOC/Track D** `docs/pre-submission-checklist.md` — fix stale pending-migration refs (said 017/018; dir now runs through 029) → point to supabase/migrations as source-of-truth + PENDING_OPS for apply notes. **(7) DOC/Track E** `docs/email-lifecycle.md` — correct "AI mockups (coming this quarter)" to present-tense (mockups are shipped + advertised live). | **All 7 both-Sonnet-APPROVED (16 reviews incl. 2 re-review cycles).** Change 1 Reviewer A caught a REAL bug the first tests missed — the initial `(?![A-Za-z])` guard also rejected the `x` separator, dropping axes on the compact `90"Wx38"D` form (the exact bug the parser fixes); fixed to a positive-boundary lookahead + 2 regression tests, re-reviewed → APPROVE. Change 6 Reviewer A REQUEST_CHANGES (calling PENDING_OPS "authoritative" overstated it — migration 028 had no entry); softened wording to name supabase/migrations as source-of-truth, re-reviewed → APPROVE; ALSO added the missing `apply-migration-028` PENDING_OPS entry (signup-nonblocking) this run. Integrated gate GREEN (tsc, **2077 tests** = 2069 +8 spatial, determinism, eslint, mobile tsc). **No ROADMAP box ticked** — all 7 are validation/determinism/a11y/perf/doc hardening; no full Track item completed; the three ship_critical blockers stay human/CI-gated. **PENDING_OPS:** added apply-migration-028. **No secrets, no code migrations.** **Lessons:** (1) Verify EVERY scout claim against the live tree — 4 of the audit's flagged items were false positives (a "missing rate limit" already limited, a "null deref" already `?.`-guarded, an "unguarded throw" already caught, a "missing landmark" already provided by a parent shell); Haiku scouts reason from stale/partial knowledge, so a 30-second grep saves a bad change. (2) A regex guard that excludes "any letter" also excludes the `x` separator — when a delimiter is alphabetic, a blanket `(?![A-Za-z])` reintroduces the bug; use a positive-boundary lookahead listing the real separators. (3) Two disjoint files with the identical determinism-sort pattern are two coherent changes, not padding, when each removes an independent contract violation on a seeded-LLM path (both reviewers agreed). | Track A/conversion (mobile auth value-prop), Track F (validation coverage), determinism-contract (LLM-seed tiebreak). No DEEP AUDIT due (ran Run 84; next ~Run 88) — ran a 6-Haiku-scout sweep (security/RLS, web-reliability, tests/coverage, perf, mobile, a11y/design-conversion) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm ci` + `cd mobile && npm ci` (tsc, **2056 tests**, determinism, eslint, mobile `tsc --noEmit` clean). **Scorecard read (DATA):** overall C, ship_gate_met false; THREE ship_critical below A unchanged — functional_reality C (in-memory→Supabase cutover, human-gated), design_taste B (authed-axe + F7 screenshots, sandbox-unrunnable), business_case_strength B (without-annual ARR ~$99.9K < $100K floor). #610 (mockups IDOR, the security_rls A+→A finding) was ALREADY fixed by Run 86 #612 — no action. **GROWTH pre_launch, 0/null funnel, all owner-connect blockers open** — no headless growth action. **Security scout CLEAN** (fresh sweep: RLS on ~29 tenant tables, all id-routes bound, SECURITY DEFINER hardened, no secret leak). **Two fresh §44 live-prod issues on entry:** #618 (root→/login, mobile drops value-prop panel) → became change #1; #619 (deterministic prod-smoke infra) → larger infra task, deferred. **Rejected/deferred over-flags:** perf #385 pgvector RPC — DEFERRED (the RPC path is INERT until the human-gated Supabase cutover — memory store is the default data layer and `.rpc` is a no-op there; zero current benefit + determinism risk on a scoring path + a human-gated migration); mobile setState-after-unmount hygiene ×6 (results/entitlements/free-quota/login/signup/settings) — HELD as borderline dev-only warnings per established taste (#614 was the clearest one; shipping 5 more = padding); web-reliability found only the one diagnosis-sort determinism bug (taken as change #3). | **Shipped 3 (#620/#621/#622):** **#620 (CONVERSION/Track A)** — mobile-only (`lg:hidden`) condensed value prop above the card on `app/(auth)/login/page.tsx` + `signup/page.tsx`; the desktop aspirational panel is `hidden lg:flex` so a mobile logged-out visitor (root redirects to /login) saw only the bare form (issue #618). An `<h1>` headline (also the pages' first real heading on mobile — CardTitle is a `<div>` — a WCAG/SEO win) + one line, warm-editorial tokens, no invented metrics/testimonials; desktop unchanged. **#621 (TEST/Track F)** — 7 boundary cases in `__tests__/validation/pairwise-proportions.test.ts` for three ZERO-coverage relational rules (dining_table.length ≥ 24×chairs, nightstand.height ±4\" of bed top, side_table.height ±3\" of sofa arm); uses explicit `dimensions` (the `W x D x H` specs regex only yields width). Mutation-verified: neutering each rule (`>=`→`>`, `<=4`→`<4`, `<=3`→`<3`, `max(1,…)`→`max(0,…)`) fails the matching test. **#622 (DETERMINISM)** — `app/api/diagnosis/route.ts` picked a sibling room's latest diagnosis via a `created_at`-only sort with no tiebreaker (non-reproducible on equal timestamps; feeds the seeded LLM prompt — a determinism-contract violation). Extracted a pure, unit-tested `pickLatestDiagnosis()` (created_at DESC, id DESC) into `lib/diagnosis/latest-diagnosis.ts` + selected `id` in the query. Mutation-verified; `check:determinism` green. | **All 3 opened, auto-merge (squash) enabled, CI TRIGGERED; all 3 MERGED to default** (verified `git log`: tip 5eeefd8 carries #620/#621/#622). Integrated gate GREEN before splitting (throwaway octopus merge of all 3, file-disjoint — 6 files): tsc, **2069 tests** (2056 +13: 7 pairwise + 6 latest-diagnosis), determinism, eslint, mobile unaffected. **All 3 both-Sonnet-APPROVED (6 reviews):** reviewers independently re-derived the pairwise arithmetic + re-ran the 4 mutations; confirmed the `lg:hidden`/`hidden lg:flex` complement (no double-render, single-h1 per breakpoint, on-brand tokens, no invented claims); and verified the tiebreak helper is correct + non-mutating, preserves non-tie behavior, and the permutation test genuinely asserts order-independence. **No ROADMAP box ticked** — all 3 are conversion/coverage/determinism hardening; no full Track item completed; functional_reality + design_taste + business_case_strength remain the human/CI-gated ship blockers. **No migrations, no secrets, no new PENDING_OPS.** **Two review-flagged follow-ups recorded for next run (both file-disjoint value-bar candidates):** (a) the three newly-tested pairwise rules are likely INERT in prod — `WhatItNeedsItem` carries only `specs` (no `dimensions`) and `DIMENSION_REGEX` can't parse `90"W x 38"D x 32"H` into 3 dims (letter suffixes break it), so nightstand/side_table see `dims.height===undefined`→null and dining uses a square depth==width approx; fix the regex or add `dimensions` to the type; (b) the same `created_at`-only non-deterministic sort exists unfixed at `lib/agents/diagnosis-expansion-pipeline.ts:118` and `lib/design-context/infer-preferences.ts:178-179,250-251` (latter feeds an LLM call) — reuse the new `pickLatestDiagnosis` helper there. **Lessons:** (1) A "clear perf win" can be INERT under the current architecture — #385's pgvector RPC only helps once the human-gated Supabase cutover lands (memory store is the default data layer, `.rpc` is a no-op there), so shipping it now is zero-benefit complexity + determinism risk; defer perf that only materializes post-cutover. (2) A `W x D x H` specs regex with letter suffixes silently yields ONLY width (the letters break the `x`-separator match) → three furniture-fit rules likely no-op in prod; unit tests that use the `dimensions` object still correctly guard the rule contract, but the integration path is a separate, real bug (found by a reviewer re-deriving the parse). (3) Dev-only setState-after-unmount warnings are borderline — one clear guard on a revenue surface (#614) clears the bar; batching five more of the same pattern is padding. |
| 2026-07-13 | Run 86 — **4 file-disjoint value-bar changes** across Track A+F (security IDOR), Track F+design (a11y landmark), Track C (mobile paywall reliability), Track F (deterministic-layer coverage). No DEEP AUDIT due (ran Run 84; next ~Run 88) — ran a 6-Haiku-scout sweep (web-reliability, tests/coverage, security/RLS, mobile, monetization/billing, a11y/design-bar) + consumed QUALITY_SCORECARD + GROWTH_STATUS as DATA. Baseline gate GREEN on entry after cold `npm ci` + `cd mobile && npm ci` (tsc, **2051 tests**, determinism, eslint, mobile `tsc --noEmit` clean). NOTE: local branch ref was STALE 54 commits behind origin on cold start → `git reset --hard origin/…` first. **Scorecard read (DATA):** overall C, ship_gate_met false; THREE ship_critical below A — functional_reality C + design_taste B (both human/CI-gated, not headlessly buildable), business_case_strength B (without-annual ARR ~$99.9K < $100K floor — clears via human-gated migration 021 OR a real conversion-lift feature); AND security_rls had regressed A+→A on a **named, headlessly-fixable** finding: a missed IDOR guard on the mockups POST route → became this run's #1 change. **GROWTH pre_launch, 0/null funnel, all owner-connect blockers open** — no headless growth action. **Security CLEAN except the one known mockups IDOR** (independent scout swept ~22 id-routes → all bound; RLS on ~29 tenant tables). **Billing CLEAN** (webhook signature-then-write, server-side entitlement fail-closed). **Rejected/deferred over-flags:** web-reliability "45+ routes getUser() not in try-catch" — DEFERRED (middleware pre-refreshes session; 45-file non-disjoint sweep; not clearly a bug); products/evaluate + saved-designs "unchecked .error" — HELD (established defensible fail-open pattern; narrowing risks regression); billing webhook tier not runtime-validated — HELD sub-bar (needs a LEAKED secret; DB CHECK already blocks the bad state); mobile results.tsx save-abort — HELD borderline (dev-only warning); pairwise-reranker "order not asserted" — not taken (#589 already added the tiebreak test). | **Shipped 4 (#612/#613/#614/#615):** **#612 (SECURITY/Track A+F)** — bind the standard-mode mockup POST's client-supplied `bundle_id`/`product_ids` to the ownership-verified `room_id` (`app/api/mockups/route.ts`); before, an owner of room A (passes userOwnsRoom) could pass another user's bundle/products (room B) and have that catalog data fetched, rendered into a paid mockup, and persisted in `selected_products` — cross-tenant read leak (memory-store query is not user-scoped → app-layer bind is the only boundary). Mirrors `bundles/route.ts:74-95`: verify the bundle belongs to room_id + 404 (no enumeration oracle) if any product_id isn't a product of the room, before any render. + `__tests__/api/mockups-product-binding.test.ts` (4 cases: 2 attack→404+no-render-started, 2 owned→passes-bind). **#613 (A11Y/Track F+design)** — add the missing `<main>` landmark to `app/(auth)/login/page.tsx` + `signup/page.tsx` (WCAG 2.1 A / 1.3.1); both render outside the app-shell ((auth) layout is a passthrough; root has no `<main>`) so they were the outliers vs every marketing page. Tag-only `<div className="min-h-screen flex">` → `<main …>`, classes unchanged → no visual change. **#614 (MOBILE/Track C reliability)** — guard `Purchases.getOfferings()` in `mobile/src/components/paywall-sheet.tsx` against setState-after-unmount with a `cancelled` flag set in the effect cleanup (mirrors `useSavedDesigns`) — a late resolve/reject after dismiss no longer updates state on a gone component on the revenue-critical paywall surface. **#615 (TEST/Track F, deterministic layer)** — strengthen the escalation-ladder "all tiers fail" test (`__tests__/agents/escalation-ladder.test.ts`) to assert `chosen`/`tierReached` (tier-0 precedence via the `!best ||` guard) + a first-non-null-candidate case; previously only accepted/attempts were checked so an "overwrite best every iteration" mutation returned the LAST tier undetected. | **All 4 opened, auto-merge (squash) enabled, CI TRIGGERED; all 4 MERGED to default** (verified `git log`: tip 719e510 carries #612-#615). Integrated gate GREEN before splitting (throwaway integration of all 4): tsc, **2056 tests** (2051 +5: 4 mockups + 1 net escalation), determinism, mobile tsc. **All 4 both-Sonnet-APPROVED (8 reviews):** reviewers independently confirmed the IDOR fix closes both paths with no bypass (dup ids deduped by PK+Set, empty→existing 400, uniform 404 non-oracle) and that `product_bundle_items` are only ever written already-room-bound (so verifying the bundle's room suffices); the auth `<main>` is genuinely absent with exactly one per page; the paywall `cancelled` flag is closure-captured + checked before every setState with no over-guard; the escalation assertions are correct traces + real mutation killers. **Non-blocking nit ACTED ON:** escalation Reviewer A flagged the inline comment's mutation description as imprecise ("dropping `!best ||`" literally throws; the 2/tier-1 result comes from an unconditional overwrite) → tweaked to "replacing `if (!best || accepted)` with an unconditional assign", amended + force-pushed #615. **No ROADMAP box ticked** — all 4 are security/a11y/reliability/coverage hardening; no full Track item completed; functional_reality + design_taste + business_case_strength remain the ship blockers (human/CI-gated or needing a bigger feature). **No migrations, no secrets, no new PENDING_OPS.** **Lessons:** (1) The local working-branch ref can be badly stale on cold start (54 commits behind here) — always reset to origin before reading test counts/scouting. (2) A scorecard grade regression can NAME a specific headlessly-buildable fix (security_rls A+→A → the exact mockups IDOR + remediation) — consuming the scorecard as DATA turned it into the run's top change. (3) A cross-tenant IDOR can hide behind a PASSING ownership check — owning the parent (room) is not owning the referenced children (bundle/products); bind every client-supplied id to the tenant boundary. (4) Don't pad with already-guarded defense-in-depth (webhook tier validation) or non-disjoint sweeps (45-route getUser) — a quiet 4-change run beats a padded 6. |
| 2026-07-13 | Run 85 — **4 file-disjoint value-bar changes** across Track C+F (billing-safety test), Track F3/F4 (validation branch coverage), and Track F/design-bar (2 a11y landmark/state fixes). No DEEP AUDIT due (ran Run 84; next ~Run 88) — ran an 8-Haiku-scout sweep (security/RLS, web-reliability/correctness, tests/coverage, mobile, monetization/billing, a11y/design-bar, store/marketing-freshness, determinism/cost-contract). Baseline gate GREEN on entry after cold `npm ci` (tsc, **2044 tests**, determinism, eslint, mobile `tsc --noEmit` clean after `cd mobile && npm ci`). **Security, web-reliability, billing, mobile all CLEAN** (fresh adversarial sweeps: RLS on every tenant table, IDOR guards on all id-routes, webhook signature-then-write ordering, entitlement server-side + fail-closed-in-prod, no secret leak; `maxDuration`/timeouts on all outbound routes; mobile screens have loading/error/empty states + server-authoritative quota). Confirmed #600 (business-case honesty B→A) was ALREADY fixed by the sibling #601 (annual gated-off disclosed, without-annual floor corrected to ~$99.9K) — GTM Auditor owns that grade, no action. **Rejected/verified over-flags:** rate-limiter `cleanup()` + spend-limiter `maybeCleanup()` "untested" — DROPPED (eviction is pure memory-hygiene with NO observable behavior via the public API since `checkRateLimit`/`checkDailySpend` reset expired entries themselves; a mutation-provable test would require adding a test-only store export to prod code = over-engineering); a11y priority-badge rose/amber/slate "design-bar violation" — REJECTED (semantic traffic-light status color, consistent with the file's own emerald/rose keep/remove sections; the proposed terracotta-opacity collapse would destroy the high/med/low signal = UX regression); dashboard/saved `<main>` "missing" — FALSE POSITIVE (both wrapped by `app-shell.tsx` which already provides `<main>`; adding another = nested-landmark regression); Terms "last updated" date "stale" — REJECTED (the date should track content changes, not any code commit); free-tier "1 room" copy undersell — known-deferred (needs a 6-file coordinated feature-matrix rewrite); determinism scout mis-scoped to mobile-only (backend `.chat()` thinkingConfig is mechanically guarded by the passing harness-ratchet test anyway). | **Shipped 4 (#604/#605/#606/#607):** **#604 (BILLING/Track C+F)** — pinned `isAnnualBillingEnabled()` (the pro_annual charge chokepoint, `process.env.ANNUAL_BILLING_ENABLED === "true"`, previously untested): enabled ONLY on literal `"true"`, disabled when unset (must ship OFF), disabled for any non-`"true"` truthy string (`"1"`/`"TRUE"`/`"yes"`/`"on"`) — kills a `Boolean()`-coercion or case-insensitive rewrite that would silently open the gate and charge for a tier the DB CHECK rejects. **#605 (TESTS/Track F3-F4)** — covered two untested branches of `computeAccessConstraints` (delivery-fit validator): the no-elevator stairwell 36" cap (walk-up blocks a wide item behind a wide door) + its negative, and the elevator-cab diagonal depth check (needed depth ≈70% of longest dim) + its negative; depth values pinned tight around the 84×0.7=58.8" threshold to also kill near-boundary 0.7-factor mutations. **#606 (A11Y/Track F+design)** — My Picks (`app/picks/page.tsx`) room-filter buttons conveyed selected state by color only → added `aria-pressed` (matches #586); page had no landmark (renders under root layout, not the app-shell that provides `<main>`) → wrapped content in `<main>` (layout classes moved unchanged). **#607 (A11Y/Track F+design)** — the PUBLIC shared-design view (`app/shared/[token]/SharedDesignView.tsx`, a prospect's first touch from a share link) had a `<header>` but no `<main>` → added the landmark (WCAG 1.3.1); it was the outlier vs every other public/marketing page. | **All 4 opened (#604-607), auto-merge (squash) enabled, CI TRIGGERED; all 4 MERGED to default** (verified `git log`: tip 2f1a6ea carries #604-#607). Integrated gate GREEN in-run before splitting (tsc, **2051 tests** = 2044 +7: 3 billing + 4 access, determinism, eslint, mobile tsc). Each change **mutation-verified** by neutering the target source branch and confirming the new test fails (stairwell `if(false)` → stairwell test fails; 0.7→0.01 → diagonal test fails; `isAnnualBillingEnabled` → `return true` → default-off + non-true tests fail). **All 4 both-Sonnet-APPROVED (8 reviews):** reviewers independently re-derived the access arithmetic (W×D×H parse order, 38+1>36 blocked, 84×0.7=58.8), re-ran the mutations, and confirmed `vi.unstubAllEnvs` in the new describe is block-scoped (no cross-block/cross-file env leak). One non-blocking nit ACTED ON: Reviewer A noted the diagonal test's original 50/80 depth values left a wide un-killed band around the 0.7 factor → tightened to 58/60 (bounds factor to ~(0.69,0.71]) before branching. **No ROADMAP box ticked** — all 4 are coverage/a11y/billing-safety hardening; no full Track item completed; functional_reality (in-memory→Supabase cutover) + design_taste (authed-axe + F7 screenshots) remain the human-gated/CI-bound ship blockers. **No migrations, no secrets, no new PENDING_OPS.** **Lessons:** (1) An in-memory cleanup/eviction function whose correctness is ALSO handled inline by the public read path (reset-on-expired) has NO observable behavior difference — it can't be mutation-tested without adding a test-only prod export; dropping it is the disciplined call, not padding a batch. (2) Before flagging a page as "missing `<main>`", check whether a shared layout (`app-shell.tsx`) already provides one — adding a second is a nested-landmark REGRESSION; only pages OUTSIDE the app-shell (picks, public shared) genuinely lacked it. (3) Semantic/status colors (traffic-light priority, keep/remove emerald/rose) are NOT single-accent design-bar violations — collapsing them to terracotta opacity tiers destroys the signal; distinguish brand-accent misuse from legitimate semantic color. | **DEEP AUDIT (8-lens) + 3 file-disjoint value-bar changes** across Tracks C/D (billing safety), A/F (reliability), E/F (a11y). DEEP AUDIT was DUE (last ran Run 80) — ran it first: 8 Haiku scouts (correctness/dead-code, security/RLS, performance, a11y/design, tests/coverage, mobile+monetization, dep/config, artifact-freshness) across the WHOLE codebase + baseline gate GREEN on entry (tsc, **2025 tests**, determinism, eslint, mobile tsc). **Security CLEAN** (zero critical: RLS on every tenant table, IDOR guards on all id-routes, SSRF/rate-limit/secrets all solid). **Monetization otherwise CLEAN** (server-side gating, no fake-success). Reconciled against QUALITY_SCORECARD (overall C; the two ship-critical below-A dims unchanged: functional_reality C = human-gated in-memory→Supabase cutover, design_taste B = CI/auth-bound authed-axe + F7 screenshots) — neither headlessly buildable, no busywork. **Rejected/verified over-flags:** correctness scout's `.single()`-without-error-field sites are guarded by null checks (not bugs); `getPublicUrl` "unchecked error" is a known false positive (synchronous, never errors); mobile "22 implicit-any" + "all deps unmet" are FALSE ALARMS (mobile `tsc --noEmit` passed clean → deps present, no errors); bundle-scorer "untested edge cases" ALREADY covered (FLOOR clamp/spatial-default/geometric compounding all tested; only impossible `totalWeight===0` remained → churn); mobile saved.tsx button a11y-labels redundant (accessibilityRole=button + visible Text child already announce); most perf "unbounded select" flags risky (narrowing select fields could drop consumer-read fields) + parallelization micro-opts borderline; README "under 10 min vs 30s" not a real contradiction (different scopes, FAQ reconciles); dep-upgrade flags speculative (no concrete CVE). | **Shipped 3 (#595/#596/#597):** **#597 (BILLING/Track C+D)** — the live pricing page advertised "Save 32% with annual — $399/yr" and `/billing/upgrade?tier=pro_annual` rendered a real Stripe checkout, but migration 021 (the `stripe_customers.tier` CHECK-constraint extension for `pro_annual`) is UNAPPLIED to prod (current constraint from 018 = `CHECK (tier IN ('apartment','pro'))`); a completed annual purchase would CHARGE the customer then fail the webhook upsert with a CHECK violation → charged, no entitlement. Added `isAnnualBillingEnabled()` (env `ANNUAL_BILLING_ENABLED`, default OFF) + gated annual end-to-end: checkout route refuses `pro_annual` (charge chokepoint), pricing CTA hidden, upgrade page redirects to /pricing; tests assert 400-when-gated + 200-when-enabled; flag declared in `validation/CAPABILITIES.yml`. **#596 (CONFIG/Track A+F)** — a missing `TAVILY_API_KEY` (required by the search/sourcing pipeline, `lib/ai/tavily.ts` throws without it) was unenforced at boot, so a mis-deploy 500'd 5-30s into the first search instead of failing loud; made it required in prod via `assertProductionEnv`, gated `E2E_AUTH_STACK !== "1"` so the CI journeys cassette-boot (which never reaches Tavily code + sets no key) still boots; test pins both the real-prod requirement AND the exemption. **#595 (A11Y/Track E+F)** — the public waitlist form's conditional submit-error `<p>` had no live region (WCAG 4.1.3 gap); added `role="alert"` so screen readers announce failures, matching the 9+ peer forms already doing this. | **All 3 opened; #595 + #597 MERGED to default (verified `git log`: tip 2b288ce). #596 auto-merge (squash) enabled + CI running (the `journeys` gate my E2E-exemption specifically protects).** Per-change gate GREEN before review (tsc, targeted + full suite, determinism, eslint, capability manifest, mobile tsc). **All 3 both-Sonnet-APPROVED (8 reviews incl. 1 re-review round on #596):** Reviewer B1 correctly REQUEST_CHANGES'd #596's FIRST form (unconditional `REQUIRED_PROD`) because the CI `journeys` job boots via `next start` (NODE_ENV=production) with dummy GEMINI/DEEPSEEK but NO TAVILY key → would boot-fail that required gate; since `.github/` is un-editable by the loop, fixed IN CODE by exempting `E2E_AUTH_STACK` (mirrors the existing DeepSeek conditional); re-review APPROVED and independently found `gemini.ts` `assertCassetteSafe()` already fail-closes any Vercel deploy that sets the flag, so the exemption is unreachable in real prod; a 2nd fresh re-reviewer flagged a truthy-vs-`"1"` mismatch → tightened `!process.env.E2E_AUTH_STACK` → `!== "1"` to match the cassette guards exactly. **No ROADMAP box ticked** — #597 billing-safety, #596 reliability-hardening, #595 a11y; no full Track item completed; functional_reality + design_taste stay the human-gated/CI-bound ship blockers. **PENDING_OPS:** amended `apply-migration-021` (now also requires setting `ANNUAL_BILLING_ENABLED=true` in the same deploy) + added `ensure-tavily-key-prod` (TAVILY now boot-required). No migrations added, no secrets. **Lessons:** (1) A "built feature gated on a human-applied migration" (annual billing) is a LIVE money-loss bug if the UI still advertises + the server still serves it — gate the whole path (UI + checkout chokepoint) behind a flag until the migration lands, don't just hide the link. (2) When a fail-loud env change would break the CI journeys production-mode boot AND `.github/` is un-editable, exempt the E2E boot IN CODE via the same `E2E_AUTH_STACK` flag the cassette guards use (`!== "1"`, not truthy) — real prod is protected by `assertCassetteSafe`. (3) DEEP-AUDIT scouts on a mature codebase over-flag heavily — verify every finding against reality (run mobile tsc before trusting "implicit-any"/"unmet deps"; read the existing test before trusting "untested"; check the null-guard before trusting "unchecked error"). |
| 2026-07-12 | Run 83 — **5 file-disjoint value-bar changes** across determinism / Track A reliability / Track D+F5 artifact-freshness / Track F test-coverage. No DEEP AUDIT due (ran Run 80; next ~Run 84) — ran an 8-Haiku-scout sweep (web-reliability, security/RLS, tests/coverage, determinism/cost-contract, mobile, monetization, store/marketing artifact-freshness, a11y/design/perf). Baseline gate GREEN on entry (tsc, **1994 tests**, determinism, eslint). **Security CLEAN and monetization CLEAN** (fresh adversarial sweeps: 29+ migrations RLS-covered, 40+ id-routes ownership-gated, webhook signature-then-write ordering, no secret leak, no trust-the-client entitlement bug). **Rejected/verified over-flags:** upload `getPublicUrl()` "missing error check" is a FALSE POSITIVE (supabase-js `getPublicUrl` is synchronous and never returns an error); mobile "add Saved tab" DEFERRED (needs a tabIcon PNG asset + can't verify nav renders headlessly — BUILDS≠WORKS risk); next/image adoption DEFERRED (layout-break risk, not visually verifiable headlessly, perf is non-ship-critical); `<main>` landmark on 3 authed pages DEFERRED to the F4 authed axe suite (real but not headlessly axe-verifiable); mobile a11y-label nits sub-bar (aria-labels/text-children already present); "raise thinking to HIGH" backwards vs cheapest-by-default. | **Shipped 5 (#589/#590/#591/#592/#593):** **#589 (DETERMINISM)** — `lib/scoring/pairwise-reranker.ts` sorted by win-count then deep-score but returned `0` on products tied on BOTH, leaving order to input/Map ordering (contract violation); added `a.id.localeCompare(b.id)` (id chosen over url — `CandidateProduct.id` is non-null, `product_url` nullable would collapse the tiebreak), matching #562/#563/`tiebreakProduct()`; mutation-proven tied-case test (fails `['prod-b','prod-a']` without the fix). **#590 (RELIABILITY/Track A)** — signup route did an outbound Turnstile siteverify fetch + admin createUser but had NO `export const maxDuration`, so a slow upstream could hang to the platform default and 504 mid-signup on the #1 critical path; added `maxDuration = 20` (Turnstile already self-aborts at 5s), matching #570's external-I/O hardening (sibling waitlist route already had 30); +guard test. **#591 (TESTS/Track F)** — pinned the new (015317a) Margin telemetry egress gate `lib/observability/margin-meter.ts` — the SOLE external-egress path — covering all three fail-safes (no-key→null, hermetic CI/E2E→null-with-key, memoise/no-throw); mutation-proven (dropping the offline gate leaks in CI+E2E, dropping the key gate leaks with no key). **#592 (TESTS/Track F)** — covered the `accentOnly` budget-suppression branch in `lib/validation/budget-allocation.ts` (an under-spent accent yields status `under` but NO issue; an over-spent accent still flags `over`); mutation-proven (removing `!target.accentOnly` surfaces a false issue). **#593 (DOCS/Track D+F5)** — the new @margin/meter SDK egresses per-call LLM economics (tokens/latency/model/outcome-score) to Margin's ingest API but Margin was undisclosed; added it to `app/privacy/page.tsx` (+Last-updated bump) AND both the Apple App-Privacy + Google Play Data-Safety tables in `docs/app-privacy.md` (the store-form source-of-truth), copy verified accurate against the real recordCall/recordOutcome payloads (no PII/photos/prompt-content). | **All 5 opened, auto-merge (squash) enabled, CI confirmed TRIGGERED; all 5 MERGED to default (verified `git log`: #589-#593 on tip ff4007a).** Base MOVED mid-run (015317a → 4a952ad: a sibling "consume margin-meter from npm, drop vendored copy" commit that touched `lib/observability/margin-meter.ts`) — RE-VERIFIED the whole batch on the NEW base with a fresh `npm ci`: `margin-meter` resolves from npm, #591 passes, tsc clean, **2004 tests** pass (1994 → +10: 1 reranker + 1 signup-guard + 6 margin-meter + 2 budget − ... net +10), determinism green, eslint clean. **All 5 both-Sonnet-APPROVED (11 reviews incl. 1 re-review):** reviewers empirically re-ran every mutation, hand-verified the budget share-math and the reranker stable-sort reasoning, confirmed the margin-meter env isolation (`vi.resetModules`/`vi.stubEnv`) and egress-free construction, and validated the maxDuration mechanism/value tier. **One review round on #593:** Reviewer B REQUEST_CHANGES because the disclosure was web-page-only and left `docs/app-privacy.md` (the Apple/Play form source-of-truth) stale → added Margin to both store tables, re-reviewed → APPROVE. **Caught + fixed a self-inflicted contamination:** a stray working-tree copy of the budget test had been swept into the privacy commit via `git add -A` — stripped it before pushing so the two changes stayed file-disjoint. No migrations/secrets; no new PENDING_OPS. **No ROADMAP box ticked** — all 5 are quality/reliability/artifact hardening; no full Track item completed (G1 stays for the readiness gate to confirm; functional_reality + design_taste remain the human-gated/CI-bound ship blockers). **Lessons:** (1) The default branch can MOVE mid-run from a sibling routine — after building, re-verify the batch on the CURRENT origin tip (fresh `npm ci`) before trusting the pre-built gate, especially when a sibling commit touches a file your new test depends on. (2) `git add -A` on a branch after switching can sweep a stray working-tree file from another change into the wrong commit — prefer `git add <explicit paths>`, and always `git show --stat` the commit before pushing to confirm the fileset. (3) supabase-js `getPublicUrl` is SYNCHRONOUS and never errors — a scout "unchecked error" flag on it is a false positive. (4) A determinism id-tiebreak IS contract-compliant/defense-in-depth even though V8's sort is stable — the repo's determinism.md mandates a final tiebreaker at every sort site regardless; a mutation test distinguishes the fix from mere stable-sort input-preservation by using non-id input order. |
| 2026-07-12 | Run 82 — **7 file-disjoint value-bar changes** across Tracks C/D/B/F/G. No DEEP AUDIT due (ran Run 80; next ~Run 84) — ran an 8-Haiku-scout sweep (tests/eval, security/RLS, web-reliability, mobile, monetization, store/marketing, auth-hardening/a11y, determinism). Baseline gate GREEN on entry (tsc, **1970 tests**, determinism, eslint). **Security CLEAN, web-reliability CLEAN** (fresh adversarial sweeps: 29+ migrations RLS-covered, id-routes ownership-gated, no secret leak; the flagged `.single()`-without-error-field sites are all mitigated by optional chaining). **Rejected/verified-stale over-flags:** the store-listing `pro_annual` "mismatch" (#487) is ALREADY resolved — store-listing intentionally omits it with a dated note; share-links-tier (#502) is a deliberate OWNER monetization decision (viral loop vs paywall), not a unilateral fix; login-lockout G4 needs a server-side login route (larger, deferred); "raise thinking to HIGH" findings are backwards vs cheapest-by-default; the tests scout's `social/index.ts` proposal was already-tested (false positive). | **Shipped 7 (#581/#582/#583/#584/#585/#586/#587):** **#581 (BILLING/Track C)** — checkout's `subscription_data.metadata` omitted `tier`, so Stripe subscription.updated/deleted webhooks (which see only the subscription's own metadata) fell back to `"pro"`, misattributing every `pro_annual` subscriber on renewal/cancel; added `tier` + mutation-proven regression test (fix reverted → test fails). **#582 (MOBILE a11y/Track D)** — RN doesn't auto-associate a visible `<Text>` label with a `TextInput`, so VoiceOver/TalkBack announced only placeholders on login/signup email/password/confirm; added `accessibilityLabel` to each input, the account-toggle Pressables, and the Collapsible header. **#583 (MOBILE/Track B)** — deleted unreferenced `external-link.tsx` Expo starter scaffolding (last importer removed in #576; verified zero importers). **#584 (TESTS/Track F3-F4)** — 19-case unit test for `updateSession` auth-gating boundary (CORS preflight, dev-mode, logged-out redirect-vs-401 page/API, public allowlists, `/guides` prefix + `/guidesX` lookalike-negative, getUser-throw degrade, logged-in redirect); mocks `@supabase/ssr`. **#585 (SECURITY/Track G)** — per-IP rate limit (60/min, new `RATE_LIMITS.sharedDesign`) on the unauthenticated `/api/shared/[token]` endpoint (only public data route with no limit); mutation-proven test (within-limit / 429 / per-IP isolation). **#586 (A11Y/Track F)** — dashboard bedroom/bathroom toggles conveyed selection by color only; added `aria-pressed` + `role=group`/`aria-labelledby` (WCAG 4.1.2), matching the room-selection buttons already in the same component. **#587 (DETERMINISM)** — added `seed: DETERMINISTIC_SEED` to 5 unseeded `.chat()` calls (analyze-apartment Pass A + synthesis, area-analysis/refine interpret + regenerate, area-analysis coordinator search), enforcing the determinism contract; matches the 62 sites already seeded (established pattern #566/#562/#563). | **All 7 opened, auto-merge (squash) enabled, CI confirmed TRIGGERED (queued/in_progress, not the Run-81 zero-check limbo); all 7 MERGED to default (verified `git log`: #581-#587 on tip 93d7359).** Integrated gate GREEN in-run (all 7 on a scratch branch): tsc clean, **1994 tests** pass (1970 → +24: 2 billing + 19 middleware + 3 shared-token), determinism green, eslint clean; mobile `tsc --noEmit` + eslint clean. **All 7 both-Sonnet-APPROVED (14 reviews):** reviewers mutation-verified the billing + middleware + shared-token tests actually catch their regressions, confirmed RN a11y prop correctness + label accuracy, grep-confirmed the mobile deletion unreferenced, checked determinism import/field validity + guarded-ratchet-untouched, and verified the aria ids unique. Noted (non-blocking): default-branch push CI shows a `migrate prod` job failure — it's the post-merge `supabase/setup-cli` step (transient infra; no migrations to apply, human-gated), NOT a gating PR check; all PR jobs (verify/build/journeys/lint/mobile) green. No migrations/secrets; no new PENDING_OPS. **No ROADMAP box ticked** — #581 Track-C billing-integrity, #582/#583 Track-D/B mobile, #584 Track-F coverage, #585 Track-G hardening, #586 Track-F a11y, #587 determinism contract; no full Track item completed (G1 stays unticked for the readiness gate to confirm — maker≠checker). **Lessons:** (1) The Stripe checkout session metadata and the SUBSCRIPTION metadata are DISTINCT — webhooks on subscription.* events see only the latter; anything a subscription webhook needs (tier) must be duplicated into `subscription_data.metadata`, not just session `metadata`. (2) A vitest mock factory using an ARROW fn (`vi.fn(() => obj)`) is not `new`-able — if the code under test does `new X()`, use a normal function; existing tests may not have caught it because they threw before reaching the `new`. (3) `git checkout <file>` to revert a mutation-check ALSO wipes an uncommitted edit on that file — commit or stash the real change first, or re-apply after. (4) Verify a scout's count before trusting it — the determinism scout flagged area-analysis:1915 but line 541 (also unseeded per its raw grep) already passes a local `seed` var; and issue #487's "pro_annual mismatch" was already resolved with a dated note. |
| 2026-07-12 | Run 81 — **4 file-disjoint value-bar changes (1 a recovery of a fell-through Run-80 PR).** No DEEP AUDIT due (ran Run 80; next ~Run 84) — leaned on a 6-Haiku-scout sweep + a HEAD-reconcile of the scorecard. Baseline gate GREEN on entry (tsc, **1955 tests**, determinism, eslint). **RECOVERY — #573 never merged in Run 80.** The Run-80 ledger recorded "#570-573 merged", but #573 (`chg-diagnosis-examples-tests`, the `fetchDiagnosisExamples` test) was still OPEN with auto-merge enabled and ZERO CI checks ever triggered (stale base → the CI workflow never fired for that SHA), so it sat un-merged while the ledger claimed otherwise. Re-cut the test file onto the current tip as **#575** and closed the stale #573. Scout sweep (determinism/correctness, security/RLS, tests/coverage, mobile, web-reliability, growth/marketing): **security CLEAN** (fresh adversarial sweep of 30+ migrations + 52 id-resolving routes — every tenant table RLS-covered, every IDOR route ownership-gated, no secret leak), **tests scout confirmed diagnosis-examples is the sole genuine coverage gap** (all other untested modules are re-exports/static-data/heavy-mock infra). **Rejected several over-flags:** the "broken Pro-annual pricing CTA" (pro_annual IS fully wired in stripe.ts/checkout/webhook — the store-listing omission is a deliberate ASO choice, not a broken checkout); the "waitlist says coming-soon but app is live" (FALSE — the app is PRE-LAUNCH/pre-submission, coming-soon is correct); the area-analysis "hardcoded thinkingLevel:low should be thinkingFor(HIGH)" (backwards vs the cheapest-by-default cost contract — those subtasks are correctly cheap; raising them would INCREASE per-request cost); and a broad "wrap getUser() on 36 routes" (sprawling, and a transient 500 on most is acceptable — picked only the single cleanest OAuth path). **Shipped 4 (#575/#576/#577/#578):** **#575 (TESTS, recovers #573)** — `fetchDiagnosisExamples` two-tier query + dedup + slice-then-filter + graceful DB-error→[] + XML shape (10 cases; dedup & strip-window both proven meaningful via mutation). **#576 (MOBILE/Track B+D)** — stripped a leftover "Docs → docs.expo.dev" `ExternalLink` rendered LIVE in the mobile web-build tab bar (starter-template scaffolding a user sees on every screen) + its now-dead imports/style, and deleted the unused `HintRow` starter component; finishes the cleanup #571 began in the same file. **#577 (RELIABILITY/Track A)** — wrapped the OAuth callback's `exchangeCodeForSession` in try/catch so a transient Supabase/network throw degrades to the existing graceful `/login?error=auth` redirect instead of an uncaught 500 mid-login; `createClient()` kept OUTSIDE the try (fail-loud on `DATA_BACKEND=supabase`-missing-creds must surface) per Reviewer-A's catch; +5-case test (THROW-degrades proven to fail without the fix). **#578 (SEO/Track E)** — the landing page (`app/page.tsx`) exported no metadata → inherited the generic root `<title>` shared by every route; added a keyword-optimized landing title/description/canonical + OpenGraph/Twitter, and `metadataBase` + sitewide OG/Twitter defaults on the root layout (copy grounded in the real hero text, no fabricated metrics, no missing-image reference). | **All 4 opened (#575/#576/#577/#578), auto-merge (squash) enabled; CI (verify+build+mobile+lint+journeys) confirmed TRIGGERED + in_progress (run #1155) — unlike the stale #573.** Integrated gate GREEN in-run (all 4 on a scratch branch): tsc clean, **1970 tests** pass (1955 → +15: 10 diagnosis-examples + 5 auth-callback), determinism green, eslint clean; mobile `tsc --noEmit` + eslint clean (installed mobile deps to verify — sandbox lacked them). **All 4 both-Sonnet-APPROVED (10 reviews incl. 1 re-review):** reviewers trace-verified the test assertions against source (dedup + slice-then-filter both meaningful regression guards), grep-confirmed the mobile deletions unreferenced, checked the Next metadata merge semantics against installed `next@16` source, and confirmed the SEO copy is grounded + the domain real. **One review round on #577:** Reviewer A REQUEST_CHANGES because the try wrapped `createClient()` (whose fail-loud misconfiguration throw would be swallowed) → narrowed the try to just `exchangeCodeForSession`, re-reviewed → APPROVE. **#578 strengthened per Reviewer-A's note** (Next fully replaces, not per-field merges, a page's `openGraph`) by repeating `siteName` on the landing OG block. No migrations/secrets; no new PENDING_OPS. **No ROADMAP box ticked** — #575 Track-F coverage, #576 Track-B/D polish, #577 Track-A reliability, #578 Track-E SEO; no full Track item completed. **Lessons:** (1) A Run-80 PR (#573) was ledgered as merged but never merged — auto-merge on a stale base can leave a PR with ZERO CI checks that never fire; verify actual merge state (`git merge-base --is-ancestor`) before ticking, and re-check that new PRs' CI actually TRIGGERS (this run's did, run #1155). (2) `pro_annual` is fully wired — a scout "broken CTA" flag was a store-listing ASO omission, not a broken path; verify the checkout wiring before treating a pricing-copy mismatch as a bug. (3) The area-analysis "use thinkingFor(HIGH) instead of hardcoded low" finding is BACKWARDS vs the cheapest-by-default contract — a cheap subtask staying cheap is correct; don't "fix" it into a cost increase. (4) A `vi.fn()` whose OWN implementation throws surfaces the error across sibling tests in vitest v4 (a client-METHOD throw does not) — test the caught-throw path via a method that throws, not the mock itself. |  **DEEP AUDIT (8-lens Haiku sweep) + 4 file-disjoint value-bar changes.** DEEP AUDIT was due (last Run 76, ~4 runs). Baseline gate GREEN on entry (tsc, **1948 tests**, determinism, eslint). Ran 8 read-only Haiku scouts across security/RLS, correctness/dead-code, perf, a11y/design, mobile, functional-reality/config, tests/eval-coverage, Track-G hardening. **Security CLEAN, correctness CLEAN** (fresh independent sweeps, no new IDOR/RLS/logic gaps). **G1 rate-limiting found COMPLETE** (independent enumeration: 33 routes `checkRateLimit`/`enforceWriteRateLimit` + 2 bespoke IP limiters on signup/waitlist cover every paid/auth/expensive route) — NOT self-ticked (maker≠checker; leave for the readiness gate to confirm). **Perf findings all inert or already-done:** the Supabase projection-narrowing wins are inert under the default in-memory data backend (payload isn't over a wire), and the "dashboard N+1" is ALREADY `Promise.all`-parallelized (false positive). **a11y findings mostly dead-code/false-positive:** `global-error.tsx` inline hex is the CORRECT Next.js pattern (renders without the app stylesheet), `gallery/page.tsx` hex are literal swatch DATA (not styling), toast/badge emerald/amber are semantic status tokens. **Verified every tests-scout candidate before acting — it over-proposed 3 already-tested modules** (turnstile, sanitize-prompt, email/preferences all have test files); only `email/resend` + `db/diagnosis-examples` were genuinely untested. **Shipped 4 (#570/#571/#572/#573):** **#570 (RELIABILITY)** — explicit `maxDuration` route budget on 4 external-I/O routes that inherited the platform default (places/photo=20, upload=60 [20MB storage write], waitlist=30, cron/activation-emails=300 [per-user send loop]); correctness contract "external calls under a budget shorter than the serverless timeout"; completes the convention already on ~25 provider routes. **#572 (TESTS)** — `ResendProvider.send` (live transactional-email path: activation/receipts/waitlist-confirm), previously UNTESTED — covers 200+id / 200-no-id+warn / body-parse-failure / non-2xx (asserts recipient PII never in the returned error) / fetch-reject / payload shape. **#573 (TESTS)** — `fetchDiagnosisExamples` (few-shot examples for the Pass-2 diagnosis prompt), previously UNTESTED — two-tier query, dedup (proven meaningful via mutation), <3-item drop, 6-item cap, strip-invalid-within-window (rewritten after Reviewer A proved the first version was a no-op), graceful DB-error→[]. **#571 (MOBILE/Track D)** — stripped leftover Expo starter-template scaffolding from the store-bound app: dead `AnimatedIcon` (native+web, drew Expo logo over a blue Expo gradient, never rendered) + dead `WebBadge` + "Expo Starter" brand label→"AptDesignerAI" + 7 orphaned Expo/React assets + a dead CSS module (~236 deletions). | **All 4 opened (#570/#571/#572/#573), auto-merge (squash) enabled; awaiting required CI (verify+build+mobile+lint+journeys).** Integrated gate GREEN in-run (all 4 on a scratch branch): tsc clean, **1964 tests** pass (1948 → +16: 7 resend + 9 diagnosis-examples), determinism green, eslint clean; mobile `tsc --noEmit` + `expo lint` clean (installed mobile deps to verify — the sandbox lacked them). **All 4 both-Sonnet-APPROVED (9 reviews inc. 1 re-review):** reviewers mutation-tested both test files (each meaningful source regression caught), verified `maxDuration` placement/values against each route's real call stack, and grep-confirmed the mobile deletions are fully unreferenced. **One review round on #573:** Reviewer A caught that the original "caps+strips" test placed the invalid item beyond the `slice(0,6)` cap so the strip filter had ZERO coverage (mutation: deleting the filter kept all tests green) → split into a cap test + a strip test with the invalid item INSIDE the window (re-review confirmed the mutation now fails the strip test). No migrations/secrets; no new PENDING_OPS. **No ROADMAP box ticked** — #570 is reliability hardening, #572/#573 Track-F coverage, #571 Track-D polish; no full Track item completed (D3 screenshots still pending; F2/F3 coverage not complete). **Lessons:** (1) The tests scout (like prior runs' scouts) over-proposes ALREADY-TESTED modules — always `ls __tests__` / grep for an existing test file before writing one. (2) Perf projection-narrowing is INERT under the in-memory backend — don't ship it as a wire-payload win until the persistence cutover. (3) `global-error.tsx` inline hex and literal color-swatch hex are CORRECT, not token violations — an a11y scout over-flags both. (4) My own test had a no-op assertion (invalid item sliced away before the filter) — when a cap runs BEFORE a filter, a test for the filter must place its target INSIDE the cap window; a maker-side mutation check would have caught it pre-review. (5) Reviewer subagents can dirty the shared working tree via `git apply` — one left a stray edit on the wrong branch; discard-and-continue, and prefer read-only diff-file review. |
| 2026-07-11 | Run 79 — **5 file-disjoint value-bar changes shipped, 1 abandoned on review (no DEEP AUDIT — last Run 76, ~1 day ago; next ~Run 80).** Baseline gate GREEN on entry (tsc, **1928 tests**, determinism, eslint). RECONCILED the stale scorecard (as_of 2026-07-09) against HEAD FIRST: both named ship-critical gaps are ALREADY fixed — security_rls's GET /api/area-analysis IDOR has the `userOwnsRoom` guard (fixed #530), and artifact_integrity's PENDING_OPS `priority: low` values are all high/normal/urgent now (fixed #532). The 3 remaining ship-critical-below-A dims (functional_reality C = in-memory persistence, human-gated; design_taste B = CI/auth-stack-bound seeded-AxeBuilder + F7 screenshots, unrunnable in-sandbox) are not headlessly buildable — no busywork. Ran a 6-Haiku-scout sweep (determinism/correctness, security/RLS, mobile, web-a11y, tests, AI-pipeline). **Security CLEAN, mobile CLEAN** (77+ hardening runs). Verified the key seed mechanism before acting: `resolveSeed()` forces `DETERMINISTIC_SEED` under the test flag but passes the caller's value in prod, so "missing seed" calls are invisible to `check:determinism` yet real for prod reproducibility. **Shipped 5 (#562/#563/#565/#566/#567):** **#562 (DETERMINISM)** — the identified-products verify-phase sort (`identified-products-pipeline.ts`) ordered crops by `top?.confidence` with no tiebreak and DROPS the tail at `maxVerifyCalls`, so tied crops (0.5 default) won the verify budget by identify-fan-out insertion order, not the crop set; extracted exported `compareVerifyPriority()` + `cropSortKey()` (image+label+box) + order-independence test. **#563 (DETERMINISM)** — `verifyTopSearchCandidates` (`verify-search-candidates.ts`) picked the top candidate per category by `final_item_score` (defaults 0 → ties common) with no tiebreak → which product got browser-verified drifted with `candidatesByCategory` insertion order; extracted exported `rankCandidatesByEvalScore()` (score-desc + product-id tiebreak) + test. **#565 (A11Y)** — the "Paste a product URL" `Input` (room products page) was placeholder-only (no accessible name); added `aria-label` (WCAG 3.3.2/4.1.2), no visual change. **#566 (DETERMINISM)** — `apartment-research/route.ts` had 5 `chat()` calls but only 1 seeded (partial migration); added `seed: DETERMINISTIC_SEED` to the other 4 to match the sibling + `determinism.md` (2 take effect in prod; 2 with `urlContext` tools are inert-but-harmless per the provider's `!hasUrlContext` guard — surfaced in review, disclosed in the PR). **#567 (TESTS)** — added 10 branch/boundary cases for `computeDynamicBaseline` (`lib/scoring/calibration.ts`, drives every product's per-category baseline shift, previously untested directly): `-(median-target)*0.5` both directions, `[-2,2]` clamp, 5-sample threshold→static fallback (count=4 vs 5), no-data fallbacks, case-insensitivity, default target. **ABANDONED #564 (domain-router alphabetical tiebreak) on Reviewer-A REQUEST_CHANGES** — at the sole call site (`shopping-researcher.ts`) the input domain list is deliberately CURATED (category retailers first, then tier defaults) and constant-derived, so JS stable sort already gives run-to-run reproducibility there; an *alphabetical* tiebreak would discard the curated Tavily `include_domains` priority (unseen domains all tie at 0.5) with no offsetting determinism gain, and the only regression-free alternative (index/stable tiebreak) is a behavioral no-op = churn. Closed the PR, deleted the branch. | **All 5 merged to default (#562/#563/#565/#566/#567); #564 closed/abandoned.** Merged default = **1948 tests** pass (1928 → +20: verify-order 6 + verify-search 4 + calibration 10; #565/#566 add 0). The all-6 integration scratch showed 1950, but domain-router's +2 was abandoned with #564. tsc clean, determinism green, eslint clean. Required CI (verify+build+mobile+lint+journeys) GREEN on all 5 → auto-merged (squash). **All 5 both-Sonnet-APPROVED (10 reviews):** reviewers reproduced each pre-fix failure, confirmed the extracted comparators behavior-equivalent, hand-verified the calibration arithmetic + median mechanics, and confirmed the seed field/guard behavior (incl. the `urlContext` seed-drop caveat). Reviewers ran read-only against the diff FILES (per the Run 78 lesson) — no tree-dirtying this run. No migrations/secrets; no new PENDING_OPS. **No ROADMAP box ticked** — all 5 are incremental Track-F hardening (determinism contract, a11y, test coverage); no Track item completed. **Lessons:** (1) A blanket determinism rule ("every score sort needs a tiebreak") is NOT universal — where the sort's INPUT order is itself meaningful and deterministic (curated constant tables), an id/alphabetical tiebreak REGRESSES intent while adding no reproducibility (stable sort already suffices); #564 was the counterexample the review caught. Apply the tiebreak only where input order is non-deterministic (async fan-out) or semantically irrelevant. (2) Verify the scorecard `as_of` before acting — a 2-day-stale scorecard named 2 gaps already closed at HEAD. (3) Passing reviewers the diff as a FILE (read-only) cleanly avoided the Run-78 tree-dirtying failure mode. |
| 2026-07-11 | Run 78 — **2 disjoint determinism-contract fixes (no DEEP AUDIT — last Run 76, next ~Run 80).** Baseline gate GREEN on entry (tsc, 1923 tests, determinism, eslint). RECONCILED the 3 open quality issues against HEAD FIRST (the Run 75/76 lesson) — all three are ALREADY fixed and stale: **#527** (GET /api/area-analysis IDOR) has the `userOwnsRoom` guard (fixed #530, covered by `idor-followup-guards.test.ts`); **#526** (preflight GATE 5 `priority: low`) — PENDING_OPS priorities are all high/normal/urgent now (fixed #532); **#348** (entitlements fail-open) — `lib/entitlements/server.ts` already fails CLOSED in production, OPEN in dev. No work there. Ran a 6-Haiku-scout sweep (security/RLS, correctness, web-a11y, mobile, tests, artifact-freshness). **Security CLEAN, artifact-freshness CLEAN.** REJECTED two whole over-flag classes: (a) web-a11y's 5 "AA failures" were 4 ICON circles (graphical → 3:1 bar, `text-accent-warm` ~4.9:1 passes) + 1 text link the scout itself scored 5.8:1 (passes) — swapping a passing token is cosmetic churn; (b) mobile's 4 "missing accessibilityLabel" were all `Pressable`s with visible `<ThemedText>` children, which RN auto-derives the accessible label from — VoiceOver already announces context. Tests-scout gaps were embedded in the ~2000-line `runAgenticSearch` (heavy-mock/low-value) — deferred. Rejected the correctness scout's gemini.ts jitter finding (sensitive cost-contract file, retry-timing-only/no output effect, and its "retry.ts gates it" premise is false — that file doesn't exist). **Shipped 2 file-disjoint (#558, #559):** **#558 (DETERMINISM)** — `selectExtractionCandidates` (`lib/agents/extraction-gate.ts`) sorted each bucket by `relevanceScore` with no tiebreak; JS stable sort meant equal-relevance ties kept incoming array order → WHICH candidates survived the per-bucket `slice(0,cap)` and reached paid Tavily-Extract + LLM scoring depended on upstream ordering, not the set (→ different final bundles run-to-run). Added `\|\| a.url.localeCompare(b.url)` (matching `tiebreakProduct`/reranker.ts) + a test asserting the selected subset is identical across two input orderings (proven to fail without the fix). **#559 (DETERMINISM)** — `dedupByBrandModel` (`lib/agents/identified-products-pipeline.ts`) sorted deduped products by `confidence` with no tiebreak; ties are common (identification defaults to 0.5) so tied entries kept Map-insertion (= upstream `items`) order, feeding downstream display + the scoring/prompt context (`buildIdentifiedPiecesBlock`) an order that varies with input sequencing. Added the `(brand,model,variant)` key tiebreak (extracted to a shared `dedupKey` helper), exported the fn for a focused 4-case test (tie case proven to fail without the fix). | **Both opened (#558/#559), auto-merge (squash) enabled; awaiting CI (verify+build+mobile).** Integrated gate GREEN in-run (both merged onto a scratch branch): tsc clean, **1928 tests** pass (baseline 1923 → +5: 1 extraction-gate + 4 dedup), determinism green, eslint clean. **Both both-Sonnet-APPROVED:** #558 — Reviewer A (reproduced the pre-fix failure `['e','d','c']`≠`['a','b','c']`, verified `\|\|` short-circuit safe on negative/NaN diffs, no bucket-priority regression) + Reviewer B (value/contract). #559 — Reviewer A (confirmed `dedupKey` refactor behavior-preserving, comparator safe, export clean; traced input order as currently-deterministic-but-latent, fix is contract-required defense-in-depth) + two Reviewer B (both traced the flow into the scoring context + reproduced the pre-fix failure). No migrations/secrets; no new PENDING_OPS. **No ROADMAP box ticked** — both are incremental determinism hardening; no Track item completed. **Deferred follow-up (named):** the `verifyOrder` sort (~line 164, `identified-products-pipeline.ts`) ALSO lacks a `confidence`-tie tiebreak — but it's in the SAME file as #559 (not file-disjoint this run), so next run. **PROCESS LESSON:** reviewer subagents (general-purpose, write-capable) applied/stashed the diff onto the live working tree to test it and left it DIRTY on the wrong branch — one Reviewer-B slot even reviewed the wrong change (read the checked-out branch instead of the pasted diff). Fix used: cleaned the tree, then re-ran the missing extraction-gate Reviewer B as a **read-only, diff-only, do-NOT-modify** reviewer with the correct branch checked out. Next run: give reviewers read-only isolation (or explicit no-write + correct branch) so they can't dirty the tree or read the wrong branch. |
| 2026-07-10 | Run 77 — **recovered a CI-stuck Run-76 PR + 3 disjoint value-bar changes (no DEEP AUDIT — last was Run 76, next ~Run 80).** On entry found **#543 (Run 76 Stripe Customer Portal) still OPEN** — its ledger row claimed it merged but it hadn't: the `journeys` a11y check failed because the new /account "Manage subscription" `warm-outline` button used `text-accent-warm` (#b4501e, 4.44:1 on the card tint, below AA). ROOT CAUSE: #543 was branched from BEFORE #542 (which changed `warm-outline`→`text-accent-warm-strong`); **REBASING #543 onto the #542-inclusive default resolved the contrast to ~5.4:1** — no code change needed. Rebased + re-pushed → journeys GREEN → merged. Then a 7-Haiku-scout sweep (security/RLS, correctness, a11y, mobile, monetization/E7, tests, perf/freshness). VERIFIED-DOWN 2 scout false positives before acting: (a) search/route.ts "insert-order mismatch" — the SAME false positive Run 76 already rejected (batch `.insert().select()` preserves VALUES order; partial failure errors out early); (b) spatial-math parseInches "untested unit branches" — the existing `spatial-math.test.ts` ALREADY covers ft/cm/mm/decimal/separator/re-entrance (scout under-read the test file). Security lens CLEAN. **Shipped 3 file-disjoint:** **#547 (DETERMINISM)** — the coordinator-state + self-correction-state builders in `lib/agents/orchestrator.ts` sorted per-category candidates by `final_item_score` with a plain `sB - sA` (no tiebreaker); JS sort is stable so tied scores kept async-insertion order → `topPickTitle`/`topPickPrice` fed into `planCorrections()`'s LLM prompt drift run-to-run (determinism-contract violation). Extracted the score-desc+URL-tiebreak comparator (already inline ~8× in the file) into exported `compareByFinalScoreDesc()`, applied at both sites, + a regression test proven to fail without the tiebreak. **#548 (MOBILE/STORE-COMPLIANCE)** — Expo settings screen had no in-app path to change/cancel a subscription (Apple 3.1.2 / Play requirement); added a "Manage subscription" row opening the native store management page via RevenueCat `CustomerInfo.managementURL`, with an honest alert (no fake success/dead-end) when RC is unconfigured or no store sub exists. Mirrors #543's web portal. **#549 (A11Y)** — landing hero "after"-preview caption used `text-accent-warm/60` (~2.1:1 on the light gradient; axe misses text over a CSS gradient) → `text-accent-warm-strong` (~5.2:1). | **All 4 merged** (#543 recovered + #547/#548/#549). Integrated gate GREEN in-run (3 new merged onto a scratch branch): tsc clean, **1919 tests** pass (+3 tiebreak), determinism green, eslint clean; #543 re-verified via CI journeys GREEN (the independent gate confirming the contrast fix). **6 Sonnet reviews (2/change on the 3 new), all APPROVE:** #547 both-APPROVE first-pass (both reviewers reverted the tiebreak to confirm the test catches it); #549 both-APPROVE (reviewers independently hand-computed 4.91–5.38:1 across the gradient stops); **#548 got 1 conditional-APPROVE** — Reviewer B caught the code comment citing Apple "5.1.1(i)" (that's the Privacy-Policy guideline; subscription-management is 3.1.2) → fixed the citation (comment-only) → merged. #543's prior both-APPROVE still held (rebase-only, diff unchanged). **1 CI flake handled:** #548's journeys job failed in ~12s with `/tmp/app.log: No such file or directory` (web app never started — a mobile-only diff cannot affect web journeys) → `rerun_failed_jobs` → GREEN → merged. No migrations/secrets; no NEW PENDING_OPS (the `enable-stripe-customer-portal` owner step from Run 76 already covers #543; #548 relies on the already-listed RevenueCat setup). **No ROADMAP box ticked** — all 4 are incremental hardening / enhancements to already-complete Track C/D; none completes a Track item (D3 store assets still need a human device). **Lessons:** (1) A Run-N ledger can claim a merge that CI never landed — on entry, verify each recent PR's artifact actually exists on default (`ls` the promised file), don't trust the ledger row; #543's portal route was absent despite the row. (2) A PR branched before a sibling design-token fix can fail an a11y gate that a REBASE alone fixes — check the base before hand-patching. (3) Re-verify scout claims against BOTH the code AND the existing tests — a "coverage gap" the test file already covers is churn, and a "correctness bug" a prior run already dismissed is a recurring false positive. (4) A fast-failing journeys job (~12s, missing app.log) is an infra flake, not a test failure — distinguish setup-failure from assertion-failure before assuming your diff broke something, especially when the diff can't touch the failing surface. |
| 2026-07-10 | Run 76 — **DEEP AUDIT (8-lens, due since Run 72) + 5 disjoint value-bar changes.** Ran a 7-Haiku-scout whole-codebase sweep (security/RLS, correctness/dead-code, perf, a11y/design-bar, mobile, monetization/growth, functional-reality/config). Reconciled the stale-ish QUALITY_SCORECARD (as_of 2026-07-09, overall **C**): its two medium ship-critical gaps were ALREADY fixed at HEAD — `security_rls` GET /api/area-analysis guard (fixed #530) and `artifact_integrity` GATE 5 `priority: low` (fixed #532, GATE 5 re-run GREEN). `functional_reality` C (in-memory data layer) stays human-gated (PENDING_OPS `cutover-to-persistent-data`); `design_taste` B stays CI/auth-stack-bound. VERIFIED-DOWN two scout false positives before acting: (a) `proxy.ts` "middleware not enforced" — Next 16.2.4 uses the `proxy.ts` convention, correctly wired; (b) search/route.ts "insert-order mismatch" — a single multi-row INSERT…RETURNING preserves VALUES order. **Shipped 5 file-disjoint:** **#540 (CORRECTNESS)** — Harmony Pass A merged parallel score chunks via `.find()` by category → duplicate-category items (two throw pillows / wall-art / nightstands) both got the FIRST match, dropping one's real score; the composite-key Sets meant to fix it were built then `void`-ed. Extracted the positional merge already used by the Final Assessment path into a shared tested helper `lib/agents/chunk-merge.ts` + used on both sites + removed dead code. **#541 (SECURITY/IDOR)** — `POST /api/bundles` bound the bundle's `product_ids` to the room (candidate_products lookup by room_id + `.in()` before bundle creation) → closes a cross-tenant read + paid-compute leak via a later bundles/evaluate (RLS inert under the memory store). **#542 (A11Y)** — shared Badge `warm` (~4.2:1 on the /15 tint) + Button `warm-outline`/`warm-ghost` hover (~4.4:1 on /10) below WCAG AA → `text-accent-warm-strong` (extends #538's token to the shared primitives, 20+ pages). **#543 (MONETIZATION/COMPLIANCE)** — self-serve Stripe Customer Portal on /account (`POST /api/billing/portal` → billingPortal.sessions.create; customer id from the user's OWN stripe_customers row under RLS, never client-supplied; 404 free / 502 on failure, no fake success). **#544 (SIDE-EFFECT INTEGRITY)** — checkout-success page claimed "Payment confirmed / Welcome to Pro" from the `?tier=` query param with ZERO verification → gate the confirmed copy on the REAL entitlement (getWebBillingStatus); honest auto-refreshing "Finalizing…" state otherwise; conversion event fires only on real confirmation. | Opened **#540-544**, auto-merge (squash) enabled on all 5. Full INTEGRATED gate GREEN in-run (all 5 merged onto a scratch branch): tsc clean, **1920 tests** pass (baseline 1908 → +12: 5 chunk-merge + 3 bundles + 4 billing-portal), determinism green, eslint clean. **ALL 5 both-Sonnet-APPROVED — 10/10 first-pass APPROVE, zero REQUEST_CHANGES** (reviewers independently reverted #540/#541 to confirm the tests fail without the fix, hand-computed the #542 contrast ratios, and verified #543 has no IDOR / no secret leak / correct 502). No migrations/secrets committed; added ONE PENDING_OPS owner step (`enable-stripe-customer-portal` — Stripe requires the Customer Portal be activated in the Dashboard before #543's route works). **No ROADMAP box ticked** — all 5 are incremental hardening (Track A/F correctness, Track G access-control, Track F design-bar, Track C/D self-serve, functional-reality); none completes a Track item, and the scorecard's ship-critical gaps are already-closed (#530/#532) or human-gated (persistence / design_taste). **Lessons:** (1) VERIFY scout claims against the real stack before acting — `proxy.ts` is CORRECT for Next 16 (the middleware rename), not a dead-middleware bug; INSERT…RETURNING preserves order. (2) An abandoned half-fix (`void group1Keys`) is a smell that the real merge is still buggy — the sibling correct path (Final Assessment positional merge) is the reference; extract+share it rather than re-patch. (3) A fake-success page (confirmed copy from a query param) is a SIDE-EFFECT INTEGRITY violation even for billing — gate the success on the real downstream effect (entitlement), fall back to an honest auto-refreshing pending state, never a query-param claim. |
| 2026-07-10 | Run 75 — consumed the independent QUALITY_SCORECARD (as_of 2026-07-09, overall **C**, ship_gate false) as the run's audit signal (last full 8-lens DEEP AUDIT was Run 72; the fresh adversarial scorecard served as the prioritized findings this run). **Reconciled its top ship-critical gaps against HEAD and found them already-closed or human-gated:** `security_rls` A→A+ gap (missed `GET /api/area-analysis` guard) **already fixed by #530**; `artifact_integrity` B→A (preflight GATE 5 `priority: low` enum) **already fixed by #532** (GATE 5 re-run GREEN this run); `functional_reality` C (in-memory data layer) is human-gated PREPARE-complete (#531 `DATA_BACKEND` selector — real Postgres round-trip needs the owner cutover, not headlessly buildable). Ran an 8-Haiku-scout sweep (Track-G security, correctness/hot-paths, test-coverage, mobile, a11y/artifact-freshness). Selected **4 file-disjoint value-bar changes** the scouts surfaced + I verified. **#535 (SECURITY/IDOR):** `GET+POST /api/rooms` resolved/wrote by a client-supplied `project_id` with NO ownership guard (the sole runtime tenancy boundary, RLS inert under the in-memory store) → authed cross-tenant room enumeration (GET) + project pollution (POST). Added `userOwnsProject` guard before read+insert (404) + 4 regression tests. **#536 (CORRECTNESS/data-loss):** `analyze-apartment` persisted per-room diagnoses via a `room_type`-keyed map → two same-type rooms (2 bedrooms) collapsed, both saved the LAST room's analysis (silent corruption). Fixed to index-aligned `roomResults[i].analysis` + a real route-mock regression test (proven to fail on the pre-fix lookup). **#537 (DETERMINISM):** `prefilterBundleCombos` sorted combos with no tiebreaker (both sort sites) → equal-score combos ordered by unstable async completion order → chosen bundle drifts run-to-run. Added `comboTiebreak` (sorted product-id key) on both sorts + tests covering primary + fallback paths. **#538 (A11Y):** 3 accent text pills on `bg-accent-warm/10\|15` tint used `text-accent-warm` (#b4501e ≈4.25:1, below AA) → swapped to the purpose-built `text-accent-warm-strong` (#a3441a, 5.1:1), matching dashboard's pattern. | Opened **#535-538**, all 4 both-Sonnet-APPROVED. **10 Sonnet reviews:** #535/#538 both-APPROVE first pass; **#536 got 1 REQUEST_CHANGES** (value reviewer: no regression test + my "E2E covers it" claim was false) → added a full route-mock test (mocks only external I/O; drives the real POST; asserts distinct per-room persistence) proven to fail on the buggy lookup → re-review APPROVE (independently reverted+reproduced). **#537 got 1 REQUEST_CHANGES** (correctness reviewer: `minKept` fallback sort site untested) → added a fallback-path case asserting `fallback_topN` fired + stable order → re-review APPROVE (independently falsified). Auto-merge (squash) enabled on all 4; full integrated gate GREEN in-run (tsc clean, **1908 tests** pass [baseline 1900 → +8: 4 IDOR + 3 bundle-determinism + 1 apartment-persistence], determinism green, eslint clean); awaiting required CI (verify+build+mobile). No migrations/secrets. **No ROADMAP box ticked** — the 4 are incremental hardening (Track G security, Track A/F correctness+determinism, Track F a11y), none completes a Track item; the scorecard's ship-critical gaps are already-closed (#530/#532) or human-gated (persistence). **Lessons:** (1) Reconcile a stale-ish scorecard against HEAD FIRST — 2 of its 3 top ship-critical gaps were already fixed by the prior run (#530/#532), so the highest-value remaining work came from a fresh scout sweep, not the scorecard's named gaps. (2) `Promise.all(xs.map(fn))` preserves input order — persist index-aligned (`results[i]`), never via a lossy secondary key (`room_type`) that collapses duplicates; a "same-type" fixture is the cheap regression guard. (3) Every `Array.sort` on a computed score needs a stable id-keyed tiebreaker on EVERY sort site — a reviewer caught the fallback site the first test missed. (4) Don't over-justify a skipped test ("E2E covers it") — verify the claim; a heavy multi-LLM route IS mockable via the established route-mock + provider-mock pattern, and the test must be proven to fail without the fix. |
| 2026-07-09 | Run 74 — consumed the fresh QUALITY_SCORECARD (overall **B→C**: `functional_reality` A→C — the PRODUCTION DATA LAYER is a non-persistent in-memory mock; `security_rls` A+→A — one missed IDOR; `artifact_integrity` A→B — preflight GATE 5 red). Shipped **4 file-disjoint value-bar changes** + housekeeping, targeting the top scorecard gaps. **#531 (functional_reality — the #1 ship blocker):** `DATA_BACKEND` selector in `lib/supabase/server.ts` — the PREPARE step the scorecard prescribed ("real data client + runtime RLS wiring… not a risky cutover"). Default (`memory`) byte-equivalent to today; `DATA_BACKEND=supabase` returns a real user-scoped `@supabase/ssr` client for BOTH auth+data (persistent Postgres + RLS at runtime); missing creds under the flag FAIL LOUD in both `createClient()` and `getCurrentUserId()` (no silent memory fallback). Ships INERT; owner cutover recorded as PENDING_OPS `cutover-to-persistent-data`. + wiring test (7 states) + CAPABILITIES.yml decl. **#530 (security_rls):** closed the last 2 IDOR gaps the scorecard/Run-73 named — `userOwnsRoom` guard on `GET /api/area-analysis` (was leaking another user's private diagnosis JSON by room_id); product↔room binding (`product.room_id !== room_id` → 404 before paid scoring) on `POST /api/products/evaluate` + regression tests. **#528 (side-effect integrity):** `handleEvaluate`/`handleStatusChange` in the products page called `loadProducts()` regardless of `res.ok` (fake success) → guard + `toast.error` + reload only on real success. **#529 (mobile):** `results.tsx` read AI-analysis array fields unguarded (`.map`/`.length`/spread) behind only a top-level object check → `?? []` guards a null-crash white screen (no error boundary in `mobile/`). Housekeeping: reconciled 2 `priority: low` OWNER_ACTIONS → `normal` (preflight GATE 5 was red → **artifact_integrity B→A**). | **ALL 4 MERGED** (#528→515c5f1, #529→deeb1d0, #530→f9d9d32, #531→5e08246; verify+build+mobile+lint green; tip 5e08246). Baseline **1889 tests** → **1899** (+10: 7 data-backend + area-analysis GET guard + product-binding + product-not-owner). **9 Sonnet reviews:** C2/C3/C4 both-APPROVE first pass; **C1 got 2 REQUEST_CHANGES** (a dangling `cutover-to-persistent-data` cross-ref before the PENDING_OPS entry existed; asymmetric fail-loud in `getCurrentUserId`) → reworded comments self-contained + added the fail-loud symmetry + test → **2 fresh re-reviews both APPROVE**. No new ROADMAP ticks (data-layer is PREPARE-only — Track A stays [ ] until the owner cutover; IDOR/silent-failure/mobile map to already-ticked A/B). No migrations/secrets committed. **Lessons:** (1) The scorecard's "PREPARE, don't cutover" is best served by a flag-gated, inert-by-default real path (default byte-equivalent) — reviewable persistence code without a blind flip that would break the memory-store-dependent test suite + CI seeding. (2) A code comment must NOT cite a shared-ledger entry (PENDING_OPS) that lands in a LATER housekeeping PR — both C1 reviewers flagged the dangling slug; make the comment self-contained and add the ledger entry same-run. (3) Fail-loud must be SYMMETRIC across every entry point (`createClient` AND `getCurrentUserId`) or a flag's safety guarantee has a hole. |
| 2026-07-09 | Run 73 — SECURITY: broken-access-control (IDOR) hardening pass, 3 disjoint PRs + 1 determinism fix. Scout sweep (6 Haiku lenses) surfaced a **systemic missing-ownership-check cluster**: ~11 API routes call `getUser()`/`getCurrentUserId()` then resolve a room/project/bundle by a **client-supplied id** with NO ownership check. Root cause: the data layer is the in-memory store (`createClient()`→`createMemoryClient()` for data, real Supabase only for auth) whose query builder does NOT auto-scope by user — so tenancy is enforced ONLY by an explicit `userOwnsRoom`/`userOwnsProject` guard, which these routes lacked (`picks`, `rooms/[roomId]/diagnosis` were already correctly scoped). Verified each by direct read (e.g. `diagnosis/route.ts` passed a deliberately-unused `_userId` — smoking gun). **Shipped:** **#519 (SEC-A read-leaks)** — `userOwnsRoom` guard on `GET /api/products`, `GET+POST /api/area-analysis/refine-chat`, `POST /api/saved-designs` (each leaked another user's candidate products / refine chat / snapshotted private diagnosis+products+bundles). **#520 (SEC-B compute/write)** — guard BEFORE the paid LLM call on `POST` diagnosis, diagnosis/stream, area-analysis/refine, bundles/evaluate (via bundle→room), products/evaluate, products/evaluate-set; refine also now derives the project from the OWNED room, not a client `project_id`. **#521 (SEC-C project-scoped)** — new `userOwnsProject` helper (filters projects by BOTH id+user_id) + unit tests, applied to `analyze-apartment` GET+POST and `apartment-research` POST (the latter WRITES building_research back to a client project_id → cross-tenant overwrite). **#522 (determinism)** — `refine-summarizer.ts` `.chat()` was missing `seed: DETERMINISTIC_SEED` (non-deterministic in prod, lone outlier vs ~28 sibling agents). | Opened #519-522; **8 Sonnet reviews, all APPROVE** (1 REQUEST_CHANGES on SEC-A tests → added refine-chat POST coverage + owner-happy-path assertions → re-review APPROVE, mutation-tested). **ALL 4 MERGED** (#519→597c3b4, #520→c9055c2, #521→36ddc73, #522→81ddd67; verify+build+mobile+lint green; tip 81ddd67). Baseline **1875 tests** on entry → **1889** (+14: 8 read-guard + 2 compute-guard + 4 ownership-helper). No migrations/secrets/ROADMAP-ticks (no specific IDOR checkbox; recorded as Track A/G security hardening). Guards return 404 (not 403) matching the codebase convention — no enumeration oracle. **Residuals flagged for a future run (NOT fixed this run):** (a) `products/evaluate`+`evaluate-set` verify room ownership but not that the client `product_id` belongs to that room (`userOwnsCandidateProduct` exists, unused there); (b) SEC-B regression test covers 2 of 6 routes (rest verified by review, not test). **Lessons:** (1) The in-memory-store data layer means EVERY route is responsible for its own tenancy — grep `getUser` + `.eq("<id>")` without a `userOwns*` guard to find IDOR gaps; a `_underscored` unused userId param is a reliable smoking gun for a dropped ownership check. (2) Guard BEFORE the resource read for read-leaks, and BEFORE the LLM call for compute-abuse; for bundle→room the one cheap bundle lookup is unavoidable but evaluation/products are gated after it. (3) Don't trust a client-supplied secondary id (project_id) even when the primary (room) is owned — derive it from the owned resource. |
| 2026-07-09 | Run 72 — DEEP AUDIT (8-lens) + 5 disjoint value-bar changes: 1 G2 server-side-validation (last write-endpoint gap) + 1 G1 rate-limit sweep (9 write endpoints) + 1 A1 public-share-page reliability guard + 2 F2 pure-logic test suites (MMR reranker + query-exploration) | **DEEP AUDIT due (Run 69 ran the last, 2026-07-08) → ran the 8-lens Haiku sweep this run.** Consumed QUALITY_SCORECARD (as_of 2026-07-05, overall **B**, **design_taste B = the SOLE ship-critical dim below A**; its 2 capping items — authed axe + F7 screenshots — CI/auth-stack-bound, unverifiable in-sandbox per Runs 67-71, NOT attempted blind) + GROWTH_STATUS (pre-launch 0/null → no lever signal). **DEEP AUDIT findings:** SECURITY/RLS **CLEAN through migration 029** (26 public tables RLS-enabled, both entitlement paths fail-CLOSED-on-misconfig/OPEN-on-outage, SSRF guarded, no secrets — #348's entitlement point already RESOLVED). **PERFORMANCE lens largely INERT** — confirmed the data layer is the in-memory store (`createClient()` returns `createMemoryClient()`; real Supabase only for auth), so #385/embedding "full-table scan" is an in-process array scan → pgvector-RPC fix would be DEAD CODE (MemoryClient has no `.rpc`) → NOT built. A11Y/DESIGN: `--score-*` tokens exist-but-dead while verdicts.ts hardcodes WCAG-AA-tuned weights → swap risks contrast regression → churn, dropped; design system stays A-territory, capped only by CI-bound axe/screenshots. MOBILE/BILLING: core journey + gating production-ready. TEST/EVAL: MMR + query-exploration under-covered (shipped #513/#514). TRACK-G: G2 had ONE gap (rooms/images POST, #511); G1 ~9 unrated write endpoints (#515); G4 login-lockout is Supabase-managed. ARTIFACT: PENDING_OPS `as_of` stale + phantom "Anthropic" in spend-caps (fixed this PR). **Shipped 5 file-disjoint (one integration tree, gated once, split into branches):** **#511 G2** — rooms/images POST only checked truthiness → pure unit-tested `isAcceptableStoredImageUrl` helper + field length bounds. **#515 G1** — shared `enforceWriteRateLimit` (60 writes/30 deletes/min/user) on projects/rooms/products/bundles/saved-designs/email-prefs writes. **#512 A1** — public shared-design page 500'd on null/malformed snapshot → `isValidSnapshot()` → clean 404. **#513/#514 F2** — MMR + query-exploration branch tests. | Opened #511-515, all both-Sonnet-APPROVED. **2 real review catches on #511** (2 cycles): the https-only `new URL()` check would 400 the app's REAL upload path (memory-store returns relative `/uploads/...`) → fixed to accept internal paths; re-review then found `!startsWith("//")` was BYPASSABLE via backslash/tab/newline (`/\evil.com`) normalizing off-origin → fixed to `new URL(url,base).origin===base`; a 3rd fresh reviewer confirmed closed + regression tests. **ALL 5 MERGED** (#511→f02be9b, #512→18d6923, #513→7cc0323, #514→f3bb8f4, #515→45d53a6; verify+build+mobile+lint green; tip f02be9b). Baseline **1846 tests** on entry → **1875** (+29). No migrations/secrets. **Ticked ROADMAP G2** (last write-validation gap closed + deep-audit confirmed comprehensiveness; parent Track G stays [ ] — G1 floor-plan-DELETE gap + G4 partial). #512→A1 (already [x]); #513/#514→F2 (already [x]); #515→G1 (stays [ ]). Readiness NOT attempted (design_taste auditor-owned at B). **Lessons:** (1) VERIFY the data layer before a "DB perf" fix — this repo runs on the in-memory store, so pgvector/N+1/payload findings are inert until the real-DB migration + the RPC would crash the MemoryClient. (2) Same-origin URL validation must delegate to the platform `URL` parser (`new URL(url,base).origin===base`), NEVER hand-rolled `startsWith("//")` — backslash/tab/newline bypasses normalize off-origin exactly as the browser resolves `<img src>`. (3) A dead-but-present token system (`--score-*`) tempts a "wire it up" change that can silently regress deliberately-tuned WCAG-AA contrast — confirm contrast-neutrality before calling it a fix. |
| 2026-07-08 | Run 71 — 7 disjoint value-bar changes: 1 SSRF security fix + 2× G2 server-side-validation on PATCH writes (rooms/products) + 1 A1 dashboard silent-failure guard + 1 mobile blank-screen guard + 1 F2 test + 1 ship-critical business-case honesty fix (closes #486) | DEEP AUDIT NOT due (Run 69 ran the 6-lens sweep 2026-07-08 → next ~Run 72). Consumed QUALITY_SCORECARD (as_of 2026-07-05, overall **B**, **design_taste B = the SOLE ship-critical dim below A**; its 2 capping items — authed axe on seeded diagnosis/mockups/compare + F7 committed screenshots — remain CI/auth-stack-bound, unverifiable in-sandbox per Runs 67-70, NOT attempted blind) + GROWTH_STATUS (pre-launch 0/null → no lever signal). Ran a 7-scout Haiku sweep (A1 web correctness, F2 coverage, security/RLS, a11y/design, mobile, artifact-freshness, Track-G/F hardening). **Scout results:** security/RLS **CLEAN through migration 029** (26 public tables RLS-enabled, entitlements fail-CLOSED in prod, no secrets) EXCEPT one real NEW SSRF (shipped #503); store-readiness CLEAN; a11y scout's toast/badge "info" blue variants have NO call sites (dead → churn, dropped); topbar emerald step-indicator is a defensible semantic → deferred; artifact scout's email-lifecycle Pro-Annual gap is low-value + entangled with the open #487 owner decision → deferred; perf inert under the in-memory store. **Shipped 7 file-disjoint (built in one integration tree, gated once at 1846 tests, split into per-change branches):** **#503 SECURITY** — `computer-use/product-verify` passed a user `product_url` into Browserbase (server-side fetch) with NO validation → added `validateExternalUrl()` mirroring the sibling ingest route (blocks private-IP/metadata/localhost SSRF). **#509 G2** — `rooms/[roomId]` PATCH copied ALLOWED_KEYS straight to the DB with zero validation → added string-length + **enum-membership** (room_type/status/budget_mode/sourcing_mode, lists verified 1:1 against migrations 001/002) + **integer** budget_dollars + **non-empty** name/room_type (NOT NULL cols) so malformed input returns a clean 400 not a DB 500. **#505 G2** — `products/[productId]` PATCH same gap → added validation mirroring the sibling POST (strings ≤2000, arrays ≤100/≤500, dimensions/metadata objects ≤50KB, finite price/user_rating). **#504 A1** — dashboard `handleAnalyze` silently reverted to "setup" on a failed/thrown apartment analysis (spinner→blank upload, no error) + the room-notes PATCH was console-only → both surfaced via the app's `toast.error` primitive (analysis reverts w/ message; notes inform-and-proceed). **#507 MOBILE** — `results.tsx` `analyzeRoom()` did an unchecked `as {analysis}` cast; a malformed 200 returned undefined → the screen reached stage 'done' with a BLANK dead-end (no results/error/retry) → guard throws so `run()`'s catch shows the retry UI. **#506 F2** — `groundConfidence()` (core scoring, 7 signal penalties, 0 prior coverage) → +6 tests over each penalty isolated + stacking + floor/ceiling clamp + 1-decimal rounding. **#508 DOCS (ship-critical honesty, closes #486)** — BUSINESS_CASE summary claimed `floor_met_year1: true`/"exceeds the floor in year 1" while the $122.9K base ARR is built entirely from STEADY-STATE pools (~171 monthly + ~167 annual, fed by ~12+4 net-new/mo) → set `floor_met_year1: false`, rewrote `time_to_floor` to the honest ramp (year-1 exit run-rate ≈$58-60K, floor ~year 3), relabelled $122.9K steady-state throughout; arr_year1.base stays 122900 (matches body+preflight), no inputs inflated. | Opened #503-509, all both-Sonnet-APPROVED (16 first-pass reviews + **2 re-reviews on #509**: Reviewer A REQUEST_CHANGES flagged 3 real gaps — enum columns only length-checked (invalid value → DB-CHECK 500 not 400), budget_dollars not integer-checked (2999.99 → coercion 500), present-but-null on NOT NULL name/room_type skipped validation (→ NOT NULL 500) → all 3 fixed, enum lists verified against migrations 001/002 + cross-checked against getRoomSections() so nothing legitimate is rejected → both cycle-2 reviewers APPROVE). **ALL 7 MERGED** (#503→d519696, #504→f135422, #505→98c79f2, #506→aeefd0d, #507→90429be, #508→219ef4c, #509→a3b93db; required checks verify+build+mobile+lint green; final default tip 98c79f2). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, **1840 tests**) → +6 F2 → **1846**. No new migrations/secrets → PENDING_OPS unchanged. Closed issue **#486** with the merged-fix reference (left the grade to the independent auditor — maker ≠ checker). **No ROADMAP box ticked** — #503/#505/#509 advance Track G (G2 validation partial; not all write endpoints re-audited so G2 stays [ ]); #504 advances A1 (already [x]); #506 advances F2 (already [x]); #507 is Track B functional-reality (already [x]); #508 fixes business-case honesty but the DoD business-case box stays [ ] (still needs the maximization levers). Readiness NOT attempted (design_taste still auditor-owned at B; CI-verifiable-only closure). **Lessons:** (1) a scout "design violation" in a shared UI primitive must be confirmed to have real call sites — the toast/badge `info` blue variants were dead (doc-comment example only), so fixing them would've been churn. (2) When adding server-side validation that duplicates DB CHECK/NOT-NULL constraints, verify the enum lists against the ACTUAL current migration (room_type's live constraint is 002's replacement, not 001's stale 5-value list) and cross-check what the app sends, so a "stricter 400" doesn't become a legitimate-value regression. (3) A present-but-null value slips past `v !== null`-guarded checks and 500s at a NOT NULL column — validate NOT NULL columns explicitly. |
| 2026-07-08 | Run 70 — 4 disjoint value-bar changes: 1 A1 silent-failure guard on the CORE journey + 1 F2 pure-logic test suite + 2 a11y/design-bar contrast fixes (web dashboard + mobile primitive) | DEEP AUDIT NOT due (Run 69 ran the 6-lens sweep earlier today → next ~Run 72). Consumed QUALITY_SCORECARD (as_of 2026-07-05, overall **B**, **design_taste B = the SOLE ship-critical dim below A**; its 2 capping items — authed axe on seeded diagnosis/mockups/compare + F7 committed screenshots — remain CI/auth-stack-bound, unverifiable in-sandbox per Run 67/68/69, so NOT attempted blind) + GROWTH_STATUS (pre-launch 0/null → no lever signal). Ran a 7-scout Haiku sweep (A1 web correctness, F2 coverage, security/RLS, mobile, design/a11y, artifact-freshness, perf+store). **Scout results:** security/RLS **CLEAN through migration 028/029** (RLS solid, entitlements fail-CLOSED in prod, SSRF guarded, no secrets) → no security busywork; store-readiness CLEAN; perf findings all inert under the in-memory data layer (topKSimilar N+1 needs a human pgvector migration + a MemoryClient that supports .rpc → deferred, same as #385); SharedDesignView priority badges are dark-text-on-light (AA-passing) → taste not a11y, deferred; the artifact scout's share-links-Pro-gating inconsistency is a monetization DECISION (gate vs free-viral-loop) touching the business case → deferred, not forced. **VERIFIED-DOWN a scout false positive (lesson applied):** the mobile scout flagged the animated-icon blue splash gradient as "the first visual users see" — but `AnimatedIcon` (expo-logo + blue gradient) is DEAD template code (exported, never rendered); the real splash is the on-brand `AnimatedSplashOverlay` → dropped as churn. **Shipped 4 file-disjoint:** #497 A1 (highest value) — the core per-room focus page (photo→diagnose→source→mockup) swallowed failures on 4 handlers: handleSaveDesign (fake success), handleGenerateMockup (unguarded fetch throw stranded the spinner), handleManualSubmit (console-only), and handleSaveAndContinue (the "mark room completed" PATCH was ENTIRELY unchecked → a failed completion still navigated the user away believing the room was done = state/server desync) → surfaced each via the app's existing toast.error primitive + block navigation unless the completion PATCH succeeds. #498 F2 — lib/constants/identifiable-brands.ts (product-identifier prompt + confidence-floor gate) had only 2 thin asserts → +18 tests over the untested branches (getIdentifiableBrands env-override parse/merge/dedup-by-lowercase/malformed-filter + never-throw fallbacks; lookupBrand case/trim; formatBrandsForPrompt hint-slicing + limit boundaries) via a real temp file, cleaned up in afterEach. #499 A11Y — dashboard Done/Outstanding badges rendered white on bg-emerald-600/90 (~3.4:1) + bg-amber-500/90 (~1.9:1), both FAILING WCAG AA (10px text; /90 also composited over room photos so contrast was background-dependent) → solid bg-emerald-700 (~5.5:1) + bg-amber-700 (~5.0:1), semantics kept. #500 DESIGN(mobile) — ThemedText linkPrimary hardcoded color '#3c87f7' (off-brand blue that ALSO failed WCAG AA ≈3.3:1 on the light bg) overriding the theme → routed through the theme brand accent (#b4501e/#d4733e, AA-safe), hardcoded hex removed. | Opened #497-500, all both-Sonnet-APPROVED (8/8 reviewers APPROVE); **ALL 4 MERGED** (required checks verify+build+mobile+lint green; default tip 82a86da). Reviewers independently re-derived the contrast ratios (C3 + C4), verified #497 against the REAL PATCH endpoint semantics (patchRes.ok is a trustworthy signal — 200 on success, apiError on failure) + confirmed the toast primitive is globally mounted, and confirmed the F2 assertions are load-bearing (malformed-filter/dedup/slice would fail if regressed) with correct reference-equality checks. C2-A raised 2 non-blocking maintainability nits (fixed temp-filename; hardcoded IKEA as first brand) — left as-is (correct today, both reviewers APPROVE). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, **1822 tests**) → +18 F2 → **1840**. No new migrations/secrets → PENDING_OPS unchanged. **No ROADMAP box ticked** — #497 advances Track A/A1 (already [x]), #498 advances F2 (already [x]), #499/#500 feed the auditor-owned design_taste but do NOT touch its capping axis (authed axe + F7 screenshots), so the grade is untouched (never self-graded). Readiness NOT attempted (design_taste still auditor-owned at B; its closure is CI-verifiable-only build work). **Lessons:** (1) VERIFY scout claims about "first visual"/"used" before building — the mobile splash "violation" was dead template code (`AnimatedIcon` exported but never rendered). (2) An unchecked mutation PATCH before a navigate() is the highest-severity silent-failure class (progress-loss, not just a missing toast) — always gate navigation on the write actually succeeding. (3) A translucent badge fill (`/90`) over user-content images makes a static contrast ratio meaningless — solid fills are both the a11y fix and a determinism win. (4) design_taste remains the lone ship blocker and is genuinely CI/auth-stack-bound in this sandbox (supabase-local unrunnable: registry 503 + rlimit-denied container init) — its closure is the "extend the render cassette to the text stages → seed authed diagnosis/mockups/compare → authed axe + toHaveScreenshot baselines" push, a CI-only-verifiable focused run, not doable blind here. |
| 2026-07-08 | Run 69 — DEEP AUDIT (6-lens, due since Run 68) + 3 disjoint value-bar changes: 1 mobile null-crash fix + 1 security-critical test + 1 living-artifacts doc | DEEP AUDIT ran BEFORE selecting (6 Haiku lenses over the whole codebase). Consumed QUALITY_SCORECARD (as_of 2026-07-05, overall **B**, **design_taste B = the SOLE ship-critical dim below A**; its 2 capping items unchanged: authed axe on seeded diagnosis/mockups/compare + F7 committed screenshots) + GROWTH_STATUS (pre-launch 0/null → no lever signal). **Audit results:** security/RLS **CLEAN through migration 029** (26 tables RLS-enabled, no secrets, entitlements fail-CLOSED in prod) → no security busywork; correctness substantially clean (3 low-value mobile silent-swallow logs deferred; other `.catch` sites intentional/`.ok`-guarded); artifact/pricing consistent except the press-kit gap below; a11y baseline clean; perf N+1 in embedding-index persists (non-ship-critical B, deferred). **ENVIRONMENT CONSTRAINT (decisive):** the design_taste closure needs the authed E2E stack — `npx supabase start` FAILED both attempts (ghcr/ecr image pulls 503 through the proxy; container init `error setting rlimit type 7: operation not permitted`), and playwright `next dev` webServer is known to time out at 120s in-sandbox → authed axe on diagnosis/mockups/compare + F7 authed screenshots are **CI-verifiable-only, not runnable here** (same as Run 67/68). Did NOT ship unverifiable E2E code (BUILDS≠WORKS). **Shipped 3 file-disjoint (mobile / test / doc — zero file overlap):** #492 fix(mobile) — `useColorScheme()` returns `'light'|'dark'|'unspecified'|null` at runtime but all 12 screens used `Colors[colorScheme === 'unspecified' ? 'light' : colorScheme]`; the guard only handled 'unspecified' and let null/undefined fall to `Colors[null]` → undefined → crash on first `colors.text` (RN's bundled .d.ts hand-declares the return non-nullable so tsc never caught it) → fixed all 12 sites to `colorScheme === 'dark' ? 'dark' : 'light'` (crash-safe, no regression). #493 test(security) — `assertServiceRoleKey` (admin.ts §28/§32 fail-loud key guard) had ZERO coverage → 7 tests over real branches (anon/authenticated JWT + sb_publishable_ → throw; service_role/sb_secret_/opaque/malformed → pass, no false reject; malformed exercises the decode catch). #491 docs(press-kit) — App facts table + boilerplate omitted the Pro Annual $399/yr tier that stripe.ts/pricing/BUSINESS_CASE carry → added the row + boilerplate (32% verified: 588→399). | Opened #491/#492/#493, all both-Sonnet-APPROVED (6/6 reviewers APPROVE; C1 reviewers traced the RN runtime source to confirm the null return + reproduced the green gate; C2 reviewers re-ran 12/12 green in isolated worktrees + traced every case; C3 reviewers verified $399/32% against the real config); **ALL 3 MERGED** (#491→56b71d5, #493→e809beb, #492→2f76732; required checks verify+build+mobile+lint green). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile after `cd mobile && npm install`, **1815 tests**) → +7 F2 (assertServiceRoleKey). No new migrations/secrets → PENDING_OPS unchanged. **No ROADMAP box ticked** — C1 advances Track B/D quality (already [x]), C2 advances F2 (already [x]), C3 is E-track living-artifacts (already [x]); never mass-tick, never self-grade. Readiness NOT attempted (design_taste still auditor-owned at B; its closure is CI-verifiable-only build work). **Lessons:** VERIFY every scout "untested"/"bug" claim before building (3 of the tests-scout's "NO TESTS" targets already had tests this run); `cd mobile && npm install` before trusting `mobile tsc` on a cold container; supabase-local is unrunnable in-sandbox on BOTH axes (registry 503 + rlimit-denied container init) — don't burn budget retrying, treat design_taste closure as CI-verified build work; RN `useColorScheme` can return null at runtime despite its bundled TS type. |
| 2026-07-06 | Run 68 — DEEP AUDIT (8-lens, due since Run 64) + 7 disjoint value-bar changes: 2× A1 silent-failure guards + 1 SSRF security fix + 2× a11y/design-bar + 1 living-artifacts + 1 determinism+F2 | DEEP AUDIT ran BEFORE scouting (8 Haiku lenses over the whole codebase; next due ~Run 72). Consumed QUALITY_SCORECARD (as_of 2026-07-05, overall **B**, **design_taste B = the SOLE ship-critical dim below A**; other ship-critical dims A/A+) + GROWTH_STATUS (pre-launch 0/null → no lever signal). **Audit key results:** security/RLS **CLEAN through migration 029** (26 tables RLS-enabled, no ≥030, no secrets, entitlements fail-CLOSED in prod — #348's fail-open verified guarded) → no security busywork; the P0 signup-email issue is **RESOLVED by design** (route auto-confirms, sends nothing, no false "sent" message); perf findings all inert under the in-memory data layer; artifact/pricing consistent. **Shipped 7 file-disjoint (built in one integration tree, gated once, split into per-change branches):** #477 A1 — create-room-dialog empty `catch {}` ("Phase 8") swallowed a failed room-create (silent no-op) → inline role=alert error + dialog stays open. #478 A1 — floor-plan-upload-zone handleRemove cleared the UI before checking the DELETE (state/server desync) → res.ok-guarded + inline error, keeps the summary card. #479 SECURITY — evaluate-set passed user URLs into server-side extractFromUrl() with no validation (SSRF to private IPs/metadata) while the sibling ingest route guards → added validateExternalUrl() per URL (per-item error shape). #480 DESIGN — off-token blue "In Progress" dashboard badge → neutral on-token bg-primary/text-primary-foreground. #481 A11Y — two icon-only image-overlay close buttons on focus/page had no accessible name / focus-visible → aria-label + aria-hidden X + focus-ring. #482 LIVING-ARTIFACTS — privacy/terms "Last updated" stale at April 16 while content materially changed 2026-07-04 → July 4, 2026. #483 DETERMINISM+F2 — topKSimilar sorted by similarity float with NO deterministic tiebreak (contract violation on the identify path) + 0 coverage → added image_url/brand/model/variant tiebreak + 7-case unit suite. | Opened #477-483, all both-Sonnet-APPROVED; **ALL 7 MERGED** (required checks verify+build+mobile+lint+validate-capabilities+validate-gtm green; default tip eef7b54). 12 first-pass reviews + **2 re-reviews on #480**: Reviewer A REQUEST_CHANGES on the first attempt (`bg-accent-warm text-white` fails WCAG AA ≈3.3:1 in DARK mode at 10px + accent-warm already marks the "Selected" pill on the same dashboard = semantic overload) → fixed to the neutral bg-primary (~15-17:1 both themes, distinct triad) → both cycle-2 reviewers APPROVE. Reviewer A mutation-tested #483's tiebreak (reverted it → the both-input-orders test went red → load-bearing). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, **1808 tests**) → +7 F2 → **1815**. No new migrations/secrets → PENDING_OPS unchanged. **No ROADMAP box ticked** — advances A1/F2/G2-security (all already [x]) + living-artifacts; the 2 a11y/design fixes feed the auditor-owned design_taste but do NOT touch its capping axis (never self-graded). **design_taste's 2 capping items (authed axe on seeded diagnosis/mockups/compare; F7 committed screenshots) confirmed genuinely CI/cassette-bound** — playwright's `next dev` webServer TIMED OUT at 120s in-sandbox (local screenshot gen unreliable), and seeded-authed-axe needs the cassette extended to the text stages (the Run-65 "full journey cassette" focused run). Deferred honestly. Readiness NOT attempted (design_taste still auditor-owned at B). **Lessons:** a warm-accent white-on-fill isn't automatically on-bar (check white-on-fill contrast in BOTH themes at the real size + check the token isn't already doing another job on the same surface); the integration-tree→split-into-branches pattern gates a large disjoint batch ONCE; playwright webServer times out locally in this sandbox (E2E screenshot/authed work verifies only in CI). |
| 2026-07-05 | Run 67 — 9 disjoint value-bar changes attacking design_taste (the SOLE ship-critical dim below A): the authed a11y gate + its root-cause contrast fix, + 3 A1 silent-failure guards + 3 F2 tests + 1 design-slop fix | DEEP AUDIT not due (Run 64 → next ~Run 68). Consumed QUALITY_SCORECARD (as_of 2026-07-05, overall **B**, functional_reality raised C→A, **design_taste B is now the lone ship-critical blocker**; auditor named 2 capping items: no authed axe coverage + no committed visual artifacts) + GROWTH_STATUS (pre-launch → no lever signal). Ran an 8-scout Haiku sweep (A1 correctness, F2 coverage, security/RLS, authed-route a11y-risk); **security audit CLEAN through migration 029** (no ≥030; all write routes rate-limited+validated; no secrets/SECURITY DEFINER holes) → no security busywork manufactured. **design_taste (the convergence push):** #459 (a11y) — `getScoreColor()` returned emerald/amber/rose-**600** text (~3.0–3.6:1 on the #faf9f7 bg — fails WCAG 2 AA), the root cause a scout found would fail an authed axe scan across compare/diagnosis/bundles/products/focus/picks; bumped to -700/-800 (≥5.2:1), traffic-light semantics + dark-mode -400 unchanged. #460 (test/a11y) — added an authed AxeBuilder scan over the 4 fixture-reachable signed-in routes (/dashboard, /account, /saved, /billing/upgrade) inside the journeys suite (reducedMotion, no critical/serious), closing the "zero authed axe" capping gap; runs in CI's journeys job. #461 (design) — replaced an off-token bg-purple "Swap first" chip (auditor-named slop) with on-system slate on ManualScorecardView. **A1 silent-failure guards:** #462 dashboard removeImage (unconditional UI removal on a failed DELETE → state/server desync) now res.ok-guarded+toast; #463 saved/[id] handleDelete + handleToggleShare silently swallowed failed DELETE/PATCH → guarded+toast; #464 compare page load fell through to the empty state on a failed fetch → loadError card + Retry (shared Button) + cancelled race-guard + clears on later success. **F2 tests (0 prior coverage each):** #465 tavily.ts (request-body assembly, 400→TavilyError, url normalization, 429 retry via fake timers); #466 semantic-extract.ts (early-return guards, disable/error/unparseable→null, parse-normalization/clamping/canonicalization across 5 helpers, 20 cases); #467 user-cache.ts (cache hit/miss/fingerprint-key/memoized-null/in-flight dedup). | Opened #459-467, auto-merge SQUASH; **ALL 9 MERGED** (required checks verify+build+mobile+lint+validate-capabilities+validate-gtm green). 18 first-pass Sonnet reviews (2×9) + 2 re-reviews on #464 (BOTH reviewers REQUEST_CHANGES independently: RevA found a real stale-`loadError` bug — never cleared on a successful load when roomId changes, so a later good room keeps the error card; RevB flagged the hand-rolled `<button>` vs the shared `<Button variant="outline">` in the sibling focus/error.tsx — both fixed, both re-reviews APPROVE). Reviewers mutation-tested the F2 suites (breaking url-normalization/status-propagation/in-flight-check/memoization turned the assertions red → load-bearing, not tautological). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, 1775 tests) → **+33 F2 tests → 1808**. A reviewer subagent's mutation test left a stray HSL-clamp removal in the working tree → caught + reverted before it could reach a branch. No new migrations/secrets → PENDING_OPS unchanged. **No ROADMAP box ticked** — advances F2 (already [x]) + feeds the auditor-owned design_taste (authed axe = 1 of its 2 named capping items; NEVER self-graded); F4/F7 remain [ ] (the committed-screenshot half of F7 needs the auth stack, which Docker/Supabase-local is unavailable for in this sandbox → deferred, recorded honestly). Readiness NOT attempted (design_taste still auditor-owned at B; the axe gate must land + be re-graded first). **Lessons:** (1) fix the a11y ROOT CAUSE before landing the gate — one shared token fn (getScoreColor) drove contrast failures across 6 pages; (2) when splitting many disjoint changes off one working tree, git stash `-u` puts NEW files in `stash^3` (not `stash@{0}`) — restore them from there; (3) vi.mock of a `new`-constructed SDK must use a `class`/`function`, not `vi.fn().mockImplementation(()=>({}))` (an arrow discards the returned object under `new`); (4) reviewer subagents can mutate the shared working tree during mutation-testing — re-check `git status` clean before building branches. |
| 2026-07-05 | Run 66 — 6 disjoint value-bar changes: 4× A1 silent-failure guards (mockups/bundles/setup/dashboard) + 2× F2 tests on critical AI-routing paths | DEEP AUDIT not due (Run 64 ran the 8-lens sweep → next ~Run 68). Consumed QUALITY_SCORECARD (as_of 2026-07-03, overall C; ship-critical below A: functional_reality C — increment 2 landed Run 65, awaiting re-grade — + design_taste B) + GROWTH_STATUS (pre-launch 0/null → no lever signal). Ran a 4-lens Haiku scout sweep (A1 web correctness, F2 coverage, security/RLS, artifact-freshness) folded with Run 65's carry list: **security audit CLEAN through migration 029** (no ≥030; all write routes rate-limited+validated; no secrets) + **artifact audit essentially clean** (1 below-bar stale doc-line, already judged below-bar Run 64) → no security/artifact busywork manufactured. Shipped 6 file-disjoint. **#452 A1 (highest value — the canonical "account → broken dashboard" guard):** dashboard onboarding ensureProject/ensureRoom read `.id` WITHOUT res.ok → non-ok create set projectId=undefined, poisoning every downstream room/upload/analyze; now the helpers throw on !res.ok+missing id, saveProjectMeta throws on failed PUT + both step-callers catch→toast→DON'T advance (no silent data loss mid-onboarding), handleUpload wrapped + per-file failures counted. **#456 A1** — mockups page load swallowed a failed fetch + handleGenerate (core money moment) silently no-op'd on failed products/empty shortlist/failed POST → guarded load (error-card+retry) + generate-error banner + **both .json() parses guarded** (.json().catch+curated fallback). **#453 A1** — bundles page silent load + 2 dead-button actions → error card + action banner. **#451 A1** — setup page handleSave navigated away UNCONDITIONALLY on a failed PATCH (silent data loss of budget/sourcing/keep-replace/priorities/context) → gated on res.ok + error banner + load-failure card. **#454 F2** — provider-factory.ts getProvider routing + DeepSeek→Gemini account-error fallback latch (0 prior tests), 8 cases. **#455 F2** — deepseek.ts request conversion (convertMessages/convertTools/buildResponseFormat) via public chat()+mocked fetch, 13 cases. | Opened #451-456, auto-merge SQUASH; **ALL 6 MERGED** (CI verify+build+mobile+lint+validate-capabilities+validate-gtm green). 12 first-pass Sonnet reviews (2×6) + 1 re-review: #456 got ONE Reviewer-A REQUEST_CHANGES (unguarded .json() could leak a raw SyntaxError, contradicting the curated-message intent + #445 precedent) → fixed (.json().catch+curated fallback) → re-review APPROVE. Reviewers mutation-tested the F2 tests (removed the latch line / broke 5 deepseek behaviors → assertions failed) confirming load-bearing not tautological. Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, 1754 tests); +21 F2 tests → 1767. **CI flake:** #451's journeys job hit the recurring supabase/setup-cli@v1 `version:latest` GitHub-API rate-limit (cost 1 rerun_failed_jobs; journeys NOT a required check, the 6 required were green). No new migrations/secrets → PENDING_OPS unchanged. **No ROADMAP box ticked** — advances A1 (already [x]) + F2 (already [x]); feeds auditor-owned functional_reality/design_taste (never self-graded); open F3/F4/F7/G1/G4/E7/D3 not completed. Readiness NOT attempted (design_taste still B; functional_reality pending re-grade). **Lessons:** the A1 silent-failure vein is deep (scout found dashboard/setup/compare/saved beyond the carry list) — picked the 4 highest-impact distinct journeys, deferred compare/saved as lower-value (not padding). Guard EVERY client `.json()` on a path that surfaces `err.message` to the user (#445 precedent). On any journeys CI failure, check setup-cli-rate-limit (infra→rerun) vs a real test failure FIRST. |
| 2026-07-05 | Run 65 — functional_reality increment 2 SHIPPED (THE ship-critical convergence blocker) + 3 disjoint (A1 correctness ×2 + F2 test) | DEEP AUDIT not due (Run 64 ran it → next ~Run 68). Consumed QUALITY_SCORECARD (overall C; ship-critical below A: functional_reality C + design_taste B) + GROWTH_STATUS (pre-launch). **#442 functional_reality (ship-critical) — the FOCUSED convergence item deferred across Runs 60-64:** wired the recorded-provider cassette (lib/ai/cassette-provider.ts, increment 1) into the served app — `geminiProvider` delegates to the cassette when `E2E_AUTH_STACK=1`, gated SOLELY on the flag with a fail-closed `assertCassetteSafe()` that throws on any Vercel deploy (mirrors assertRateLimitBypassSafe; **NOT** NODE_ENV — the journeys job serves a prod build via `next start`). New authed journey drives the AI design→render money path (create project+room via the app API → POST /api/mockups recommendation branch → cassette renders) and asserts a REAL decodable PNG (8-byte magic + non-zero IHDR). +`__tests__/ai/cassette-guard.test.ts` (guard fail-closed + activation). +CAPABILITIES.yml E2E_AUTH_STACK. **#443 A1** — focus page unguarded mount-load (Promise.all + existing-analysis) → infinite "analyzing" spinner on first-paint network failure; guarded → surfaces the retryable error (Reviewer-A cycle-2: scoped the products fetch to its own try so a preload failure can't fall through to an unintended fresh area-analysis POST). **#444 F2** — assertProductionEnv (prod-boot env contract, 0 tests) 7 cases. **#445 A1** — products page loadProducts swallowed failed fetch → misleading empty state; guarded + error/retry card gated on `loadError && products.length===0`. | Opened #442-445; **ALL 4 MERGED** (CI verify+build+mobile+lint+journeys green). 8 Sonnet review passes + 1 security review; #443 took a 2-cycle re-review (both other changes both-APPROVE first pass). **#442 needed 4 CI iterations — peeled 4 stacked causes (DEEP_DIAGNOSIS): (1) supabase/setup-cli@v1 `version:latest` GitHub-API rate-limit (infra → rerun); (2/3) mis-diagnosed grant + page.request auth (reverted); (4) THE ROOT CAUSE — lib/supabase/server.ts createClient() proxies ALL data ops to the IN-MEMORY store (createMemoryClient), real Supabase only for .auth, so admin/Postgres seeding is invisible to the route → seed via the app's OWN API instead; memory storage writes the PNG to public/uploads and returns a relative URL `next start` doesn't serve at runtime → read bytes from disk.** journeys money-path now GREEN. Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, 1735 tests); +12 tests. No net-new migrations/secrets (migration 030 was added then REVERTED once the memory-store layer was found) → PENDING_OPS unchanged. **No ROADMAP DoD box ticked** (functional_reality feeds the auditor-owned scorecard + the 2-gate readiness path; not self-graded). Readiness NOT attempted (design_taste still B; scorecard to re-grade functional_reality). **Lessons:** the app's DATA layer is the in-memory store, NOT Postgres — Supabase is auth-only (+ admin tables via getAdminClient); ANY E2E needing app data MUST seed through the app API. Test-flag CI gates key on `process.env.VERCEL`, never NODE_ENV. New `process.env.X` read requires a CAPABILITIES.yml entry (env-drift guard). |
| 2026-07-04 | Run 64 — DEEP AUDIT (8-lens) + 7 disjoint value-bar changes (3× G2 security + 2× a11y design_taste + 1× A1 correctness + 1× F2 security-test) | **DEEP AUDIT was DUE (~Run 64, last ~Run 60) — ran the full 8-lens Haiku sweep BEFORE scouting** (correctness/dead-code, security/RLS, perf, a11y/design, functional-reality mapping, test/eval, mobile/billing/store, artifact-freshness); prioritized findings recorded dated in docs/loop-memory.md. Consumed QUALITY_SCORECARD (overall C; ship-critical below A: functional_reality C + design_taste B) + GROWTH_STATUS (pre-launch 0/null). Shipped 7 file-disjoint: **#434 G2 security** — rate-limit room-images POST/DELETE (60/min per-user, inline config; last un-limited authed write in the sweep). **#435 G2 security** — rate-limit web saved-designs POST (10/min, matching the mobile equivalent's existing limit; closes an asymmetric-defensibility gap). **#436 G2 security** — size-cap projects PUT/PATCH (256KB per JSONB accumulator building_research/apartment_analysis + 2000-char string fields) before the DB write; reviewers verified legit writes go through direct server-side updates so none are rejected. **#437 design_taste (ship-critical)** — dashboard onboarding pills + sqft input had no focus-visible ring (WCAG 2.4.7 on the first-interaction path); added the single-accent terracotta ring matching ui/{button,card,input}. **#438 design_taste (ship-critical)** — identified-product correction modal: aria-labels on placeholder-only Brand/Model/Variant inputs + type=button on suggestion buttons. **#439 F2** — sanitize-prompt injection-detection + quote-escaping tests (security-critical structural half; PII was already tested) 20 cases incl. the global-regex lastIndex-reset regression guard + JSON-escape/2000-truncation. **#440 A1 correctness** — picks page `.catch(()=>{})` silent-swallow + no r.ok/Array.isArray (rendered empty state on API error, risked a non-array `.forEach` crash) → error card + retry + non-array guard (sibling of #431). | Opened #434-440, auto-merge SQUASH. **All 7 MERGED (#434-440)** (CI green: verify+build+mobile+lint+journeys). 14 Sonnet review passes (2×7), all 7 both-APPROVE first pass, 0 re-review cycles. **An 8th candidate (duplicate orchestrator-helpers test) was BUILT then KILLED by BOTH reviewers on value** — `orchestrator-filters.test.ts` (#381) already covered those 3 helpers; a Haiku scout hallucinated "0 coverage" → abandoned clean (verify + value-review backstop both fired). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, 1713 tests); +20 tests landed (sanitize-injection). No new migrations/secrets → PENDING_OPS unchanged. **functional_reality increment 2 DEFERRED** as a focused run — CI-only-verifiable/all-or-nothing; SHARPENED plan recorded (recommendation_mockup path = 1 cassette stage; dual getProvider+geminiProvider interception w/ hard prod-refuse; push→watch-journeys→merge-only-if-green). **No ROADMAP box FULLY completes** (G2 partial; F2/design_taste-a11y ongoing gates; A1 backlog partial) → none ticked. Readiness NOT attempted (2 ship-critical dims still below A). **Lessons:** Haiku scouts hallucinate "untested" — grep `__tests__/` before building a test; `react-hooks/set-state-in-effect` is ON for app/ and fires on synchronous setState-in-effect (move resets to the retry handler); use INLINE rate-limit configs to keep two same-run security changes file-disjoint. |
| 2026-07-04 | Run 63 — functional_reality FLAGSHIP (render-pipeline cassette test) + 5 disjoint (2× F2 test + G2 security + A1 correctness + Track-E honesty) | DEEP AUDIT not due (~Run 64 — run it next run). Consumed QUALITY_SCORECARD (overall C; 2 ship-critical dims below A: functional_reality C + design_taste B) + GROWTH_STATUS (pre-launch 0/null — no lever signal). Ran 4 Haiku scouts (security-G / F2-coverage / mobile+correctness / marketing-store). **KEY: built the functional_reality cassette increment 1** (Run 61/62's mapped FOCUSED item). **#427 functional_reality (ship-critical, C) FLAGSHIP** — `lib/ai/cassette-provider.ts` (reusable recorded-provider implementing AIProvider.chat, dispatch by request shape: image-modality→real base64 PNG, json-mime→stage JSON, FAIL-LOUD on unmatched stage) + `__tests__/integration/render-pipeline-cassette.test.ts` drives the REAL render pipeline (buildMockupContext→generateMockupPrompt→generateMockupImage) and asserts a REAL decodable PNG. Corrected Run 62's plan: the render stages import `geminiProvider` DIRECTLY (not via getProvider) → vi.mock is the test hook; increment 2 (E2E runtime, reuses cassette-provider.ts → NOT disjoint → a later run) must intercept that direct import gated on E2E_AUTH_STACK. **#428 F2** — checkRateLimit sliding-window tests (security-critical; only bypass tested before) 7 cases. **#429 F2** — pLimit concurrency tests (0 coverage) 5 cases (reviewers mutation-tested). **#430 G2 security** — bundles POST input validation (unbounded name + product_ids fan-out + a latent non-array→.map() 500; closes a gap #402 missed). **#431 A1 correctness** — saved-designs page: silent fetch-error (`.catch(()=>{})`, no r.ok/array check → API error rendered empty state) + silent delete-failure now surface (error card + reloadKey retry + toast). **#432 Track E honesty** — purged invented pre-launch adoption metrics across the WHOLE funnel (hero 500+/4.9★/fake avatars + footer CTA + signup 500+/4.9★/8.2 stat grid) → honest capability/privacy claims; grep-verified none remain. | Opened #427-432, auto-merge SQUASH. **#427-431 MERGED** (CI green: verify+build+mobile+lint+journeys); **#432 auto-merge-armed** (all required checks green; journeys finishing). 14 Sonnet review passes (2×6 + 2×1 re-review on #432). 5 changes both-APPROVE first pass; #432 took the full 2-cycle re-review (cycle 1: A reword 'photos stay private'→overstates, B same metrics live in signup; cycle 2: both converged on ONE remaining footer-CTA line + confirmed rest clean) → fixed comprehensively + grep-self-verified → merged at the 2-cycle cap. Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, 1699 tests); +14 tests (7 rate-limiter + 5 p-limit + 2 render-pipeline). No new migrations/secrets → PENDING_OPS unchanged. **No ROADMAP box FULLY completes** (F2/A1 gates already ticked; functional_reality increment 1 of 2; G2 partial; Track-E honesty polish) → none ticked. Readiness NOT attempted (2 ship-critical dims still below A). **Lessons:** render stages bypass getProvider (import geminiProvider directly); cassette must fail-loud on unrecorded stage (anti-false-green); honesty fixes have wide blast radius — grep the WHOLE funnel up front (3 surfaces, not 1). |
| 2026-07-04 | Run 62 — 6 disjoint changes incl. a DIRECT hit on ship-critical design_taste (1 design + 1 security G1/G2 + 1 correctness + 3 F2 tests) | DEEP AUDIT not due (~Run 64). Consumed QUALITY_SCORECARD (overall C; 2 ship-critical dims below A: functional_reality C + design_taste B) + GROWTH_STATUS (pre-launch 0/null — no lever signal). Ran 5 focused Haiku scouts (design-target / security-remaining / F2+openai-bug / cassette-feasibility / F-G-D sweep). **KEY: stopped deferring design_taste — the contrast VALUES are computable offline (WCAG formula), only the axe GATE needs render.** Shipped 6 file-disjoint, locally-verifiable: **#420 design_taste (ship-critical, #204)** — dashboard welcome-step AA contrast: darken `--muted-foreground` light `#7c7268→#6f665c` + lighten dark `#8a8077→#948a7c` (strictly ↑ contrast, no muted-surface regression) + a new `--accent-warm-strong` token (light `#a3441a`=5.13, dark `#d4733e`) for the pill TEXT only (brand accent untouched); all 3 failing elements (4.25/4.47/4.49) now ≥5.1 both themes. **#421 security G1/G2** — the `identified-products/confirm` PATCH fires a paid Gemini embedImage with NO rate limit + type-only validation → added `RATE_LIMITS.productConfirm` 429 + 200-char caps on brand/model/correction + metered the paid embed via `checkDailySpend` at the actual call (skip write-back over-cap, never 500). **#422 correctness** — `openai-schema.ts` multi-element `allOf` merge left a stale `required` (invalid strict schema → 400s structured calls); recompute additionalProperties+required from the final merged properties + a regression test (closes the Run 61 latent bug). **#423 F2** — `getDesignContextPrompt` (prompt-context every design agent sees, 0 tests) 17 cases incl. the floor-plan matched-vs-fallback-vs-no_match branch. **#424 F2** — `api-error.ts` (G3 sanitization boundary, ~25×, 0 tests) 7 cases: client never gets raw schema/table/column, server logs full detail. **#425 F2** — `determinism.ts` resolveSeed/resolveTemperature (reproducibility entry point, 0 unit tests) 9 cases via resetModules+stubEnv. | Opened #420-425; #420-422 MERGED mid-run (auto-merge on CI green: verify+build+mobile+lint+journeys), #423-425 both-APPROVED + auto-merge queued. 12 first-round Sonnet reviews (2×6) → 11 both-APPROVE first pass; 1 re-review cycle on #423 (Reviewer A caught the no_match test didn't isolate its guard + 2 tautological "no-section" tests → fixed with a truthy variant + bedrooms anchors + a multi-window case → re-APPROVED). 1 in-cycle lint fix (#423 unused type import). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, 1672 tests); +33 tests (17 user-profile + 7 api-error + 9 determinism, plus 1 openai-schema). No new migrations/secrets → PENDING_OPS unchanged. **functional_reality (ship-critical, C) DE-RISKED** — feasibility scout mapped the cassette (8.3/10, entry points + per-stage schemas + memory-store/Tavily-mock) → recorded as a 2-increment FOCUSED next run (local integration test, then E2E_AUTH_STACK-gated runtime `<img>` assertion). **No ROADMAP box FULLY completes** (G1/G2 partial; F2/F5 ongoing; design_taste contrast fixed but F7 axe-gate+screenshots remain) → none ticked. Readiness NOT attempted (2 ship-critical dims still below A). **2nd latent openai-schema gap found** (Reviewer A): multi-object-branch allOf silently drops earlier-branch properties (no live call site) — recorded for a future deep-merge fix. |
| 2026-07-04 | Run 61 — 6 disjoint changes (2× security G1/G2 + 2× F2 test + BUSINESS_CASE COGS recompute + mobile iOS a11y) | DEEP AUDIT not due (~Run 64). Consumed QUALITY_SCORECARD (overall C; the 2 ship-critical dims below A are functional_reality C + design_taste B) + GROWTH_STATUS (pre-launch 0/null — no lever signal). Ran a 6-lens Haiku scout sweep (G1 rate-limit / G2 validation / F2 pure-module tests / mobile / artifact-freshness / security-RLS+G4). **Assessed BOTH ship-critical blockers as NOT-safely-landable blind THIS run and deferred with precise notes (see loop-memory):** (1) functional_reality needs a recorded-provider (cassette) tier for a real photo→mockup runtime `<img>` assertion — viable gated on `E2E_AUTH_STACK` (already in the CI journeys job, no `.github` edit) but large/cost-contract-sensitive + only CI-verifiable → a FOCUSED-run item; (2) design_taste dashboard welcome-step AA contrast — computed the RESTING-state ratios (badge accent-warm 4.25, hero subtitle muted 4.47, dark step-hint 4.49 — real but MARGINAL AA fails; Run 60's 1.49–3.22 were framer-motion mid-animation artifacts) but the fix needs authed render + the axe gate I can't run locally (no supabase-local/docker) — same conclusion as Run 60. Shipped 6 file-disjoint, locally-verifiable, value-bar-clearing instead: **G2 (#402)** server-side input validation on the rooms/products/projects POST write routes (cap strings/arrays/JSON; type-correct vs `CandidateProduct` — materials/colors `string[]`, dimensions/metadata objects, price finite number). **G1 (#403)** rate-limit the two un-throttled expensive routes — `/api/upload` (20MB→storage; wired the dead `RATE_LIMITS.upload`) + `/api/mobile/entitlements` (paid RevenueCat REST; new `mobileEntitlements` 30/min bucket). **F2 (#401)** `lib/scoring/verdicts.ts` (score→colour band boundaries ≥8/≥6/else + verdict-map completeness, 0 tests). **F2 (#404)** `lib/ai/openai-schema.ts` (Zod→OpenAI strict converter: $ref/anyOf-nullable/allOf/oneOf/keyword-strip + strict invariants, 0 tests). **BUSINESS_CASE (#405)** COGS recompute to the real tier (HIGH-thinking core analyses route to `TEXT_TIERS.mid`=gemini-3.1-flash-lite; $0.25/$1.50 per 1M → ~$0.002/analysis, was $0.0006; ARR unchanged — COGS moves margin not revenue). **mobile a11y (#406)** iOS VoiceOver announce on the results loading screen (`accessibilityLiveRegion` is Android-only — the Run 60 cross-platform follow-up). | Opened #401-406, ALL with auto-merge (SQUASH) → merge on CI green (verify+build+mobile+lint+journeys). 12 first-round Sonnet reviews (2×6), ALL both-APPROVE, 0 re-review cycles; 2 in-cycle self-fixes on already-approved diffs (G1 comment corrected per Reviewer B's unverifiable-claim nit; BUSINESS_CASE "GA May 2026" softened per Reviewer A's unsourced-date nit). Baseline gate green on entry (tsc/eslint/determinism clean root+mobile, 1647 tests); +25 tests (10 verdicts + 15 openai-schema). No new migrations/secrets → PENDING_OPS unchanged. **No ROADMAP box FULLY completes** (G1/G2 partial; F2/F5 ongoing; BUSINESS_CASE a living-artifact refresh; mobile a11y polish) → none ticked. Readiness NOT attempted (2 ship-critical dims still below A). **Latent bug found + recorded** (openai-schema multi-element `allOf` merge leaves a stale `required` → an invalid strict schema; both reviewers confirmed; the test does NOT cement it) for a future fix. **Lesson: the mobile scout HALLUCINATED 3/6 findings** (claimed missing `[]` deps arrays that were already present) — always verify scout line-level claims against the real file before building. |
| 2026-07-03 | Run 60 — DEEP AUDIT (8-lens) + 4 merged (3× F2 test + mobile a11y) + 2 abandoned | DEEP AUDIT was DUE (last full Run 57) → ran the full 8-lens read-only Haiku sweep BEFORE scouting. **KEY architecture re-confirm:** the DEFAULT runtime uses the in-memory store (`memory-store.ts`, `.rpc()` is a no-op); CI's journeys job uses real Supabase-local — this made several scout findings inert. **Triaged out (not shipped, recorded as settled):** perf Promise.all/Map + pgvector-RPC (#385) findings are inert under the memory store; "no per-call gemini timeout" is covered by the provider 180s floor < 300s route budget; `getCurrentUserId` mock fallback is NOT a leak (single-data-space store + middleware + prod-fail-loud); badge/toast/diagnosis SEMANTIC status colors are legitimate (not the design_taste gap); a broad speculative GRANT-gap migration was DEFERRED (evidence undercut it — 001+011 tables already work in CI, only stripe_customers(018) failed; unverifiable without a local `supabase db reset`). Shipped 4 file-disjoint: **F2 (#394)** `parsePagination` (every paginated route, 0 tests) — inclusive `rangeEnd`, maxLimit clamp/floor, non-positive/NaN/Infinity fallback. **F2 (#395)** `isGeminiCompatibleImageUrl` (vision-format pre-filter, 0 tests) — pathname-scoped so a query can't fake/hide an ext, case-insensitivity, non-URL catch fallback. **F2 (#396)** `buildDesignProfile` (prompt personalisation, 0 tests) — characterization test pinning the no-data gate, state-alone-no-op, nested-over-flat lifestyle merge, and the bedrooms=0/bathrooms-alone hasData quirk. **mobile a11y (#397, Track B/design_taste)** — results-screen loading card got `accessibilityLiveRegion="polite"`+`accessibilityRole="progressbar"` (SR silence over the 15-30s upload→analyze swap). | Merged #394-397 — all auto-merged on CI green (verify+build+mobile+lint+journeys); verified default tip `25036f7`. 12 first-round Sonnet reviews (2×6), no re-review cycles. **2 abandoned (the gates working):** **#393 authed-axe** (ship-critical design_taste, #204) both-APPROVED but its journeys job FAILED on **real serious WCAG 2 AA color-contrast violations** on the signed-in dashboard welcome step (intro badge accent text 3.0; hero subtitle muted 2.91; the 3 step-preview cards' 10px/12px accent+muted labels 1.49–3.22) — the axe gate did its job. Landing the gate needs the contrast FIXED first (a design change unsafe to nail blind: no local auth-stack render, dark-mode parity, + axe measured mid-`StaggerItem` animation opacity), so closed the PR + recorded the exact targets on #204 (reason: gate_test/design_blocked_on_render). **#398 brand-metadata titles** both reviewers REQUEST_CHANGES (correctly): fixing `<title>` while leaving footer/body/watermark/email stale creates NEW inconsistency → needs a wholesale sweep behind a `BRAND_NAME` constant (reason: review_value). tsc/eslint/determinism clean (root+mobile), 1596→1647 tests (+51 across the 3 F2 files). No new migrations/secrets → PENDING_OPS unchanged. No ROADMAP box FULLY completes → none ticked. Readiness NOT attempted. |
| 2026-07-03 | Run 59 — 6 disjoint changes (ship-critical functional_reality E2E + ship-critical design_taste + 2× F2 test + correctness/spend + money-path hardening) | No DEEP AUDIT (ran Run 52/56/57 → next ~Run 60). Consumed QUALITY_SCORECARD (overall C, 2 ship-critical dims below A: functional_reality C + design_taste B) + GROWTH_STATUS (pre-launch 0/null — no funnel signal); ran a 6-lens Haiku scout sweep. **KEY: rejected two scout clusters as FALSE POSITIVES** — (a) the security scout's "UPDATE policies missing WITH CHECK" HIGH finding: PostgreSQL uses the USING expression as the implicit WITH CHECK when WITH CHECK is omitted for UPDATE, so `user_id` reassignment is already blocked (matches Run 58's DO-NOT-RE-FLAG list); (b) the functional scout's 5-route "DELETE returns success without row-count check" cluster: these are ownership-scoped IDEMPOTENT deletes that check the in-band `error` and `apiError` on real failure — a 0-row delete of an absent/unowned row is correct idempotent behavior, not fake success. Shipped 6, targeting the two ship-critical gaps + Track F: **functional_reality/F4 (#386)** — the authed paywall journey only asserted "/billing/upgrade renders a heading"; strengthened to assert the REAL money-path OUTCOMES (Continue-to-checkout button; free user sees the UpgradeCtaCard on /saved; a seedProEntitlement-seeded Pro user gets hasPaid=true AND the CTA disappears — the paywall→unlock end to end), all in the existing CI journeys job (Supabase-local, no live Stripe). **design_taste/F7 (#387)** — the room-hub stepper (core signed-in surface) rendered its 6 steps in 6 competing accent colors (blue/emerald/amber/purple/rose/cyan) + hardcoded emerald connector/status → unified to the single terracotta accent-warm token + neutral states (the "generic AI-UI" VISION forbids). **correctness/spend (#388)** — products/evaluate 500'd on the post-persist status flip; since product_evaluations has no unique constraint, that made the client retry a SUCCEEDED eval → duplicate row + re-burned LLM credits → aligned to the settled log-and-continue pattern (diagnosis/bundles-evaluate, §32). **F2 test (#389)** — 37 boundary cases pinning the SSRF url-validator (validateExternalUrl, 0 prior tests): 172.16-31 block vs 172.15/172.32 public, 192.168 vs 192.167/9, IPv6 ULA/link-local vs public, credentials, non-HTTP schemes. **F2 test (#390)** — 15 cases for extractJsonObject (LLM-reply parser every agent uses, 0 prior tests): each strategy + the brace/escape string state machine. **money-path hardening (#391)** — Stripe checkout had the SDK 80s default timeout + no route maxDuration → bounded with timeout:15s + maxNetworkRetries:0 + maxDuration=20 (email/Turnstile already bound theirs). | Merged #386-391 — all auto-merged on CI green (verify+build+mobile+lint+journeys), verified default tip `48d77a0`. 12 first-round Sonnet reviews (2×6) + 2 re-reviews (#391) + 2 re-reviews (#386 migration). **2 in-cycle reworks caught by review/CI (the gates working):** (1) #391 Reviewer A caught that the Stripe SDK's default `maxNetworkRetries` is 2 (not 0), so a timed-out request would retry ~3×15s ≈ 45-50s > maxDuration — defeating the timeout; fixed with explicit `maxNetworkRetries:0`, re-reviewed → both APPROVE. (2) **#386's first CI run FAILED (functional_reality gate working as designed)** — the new paywall tests hit `permission denied for table stripe_customers` for BOTH the seed AND the app's own getWebBillingStatus: migration 018 never GRANTed table privileges and `supabase db reset` doesn't reliably apply Supabase default-privilege auto-grants → root-caused + added migration 029 (explicit service_role DML + authenticated SELECT grants; RLS boundary unchanged, GRANT and RLS are independent gates); CI re-run GREEN, both migration re-reviewers confirmed correctness. tsc/eslint/determinism clean (root+mobile), +52 tests (37 url-validator + 15 extract-json). **New migration 029 → apply-migration-029 recorded in PENDING_OPS (HUMAN-APPLIED).** No ROADMAP box FULLY completes (F4 core photo→mockup still lacks a real-mockup runtime assertion; F7 screenshots still absent) → none ticked; functional_reality/design_taste deltas land but the independent scorecard owns those grades. Readiness NOT attempted. |
| 2026-07-03 | Run 58 — 4 disjoint changes (ship-critical security + correctness/eval-regression + F2 test + mobile reliability) | No DEEP AUDIT (next due ~Run 60). Consumed the (stale, as_of 2026-07-01) QUALITY_SCORECARD + GROWTH_STATUS (pre-launch, 0/null — no funnel signal) + open issues; ran a 6-lens Haiku scout sweep. **KEY: caught a scout's WRONG fix** — for the #306 diagnosis eval regression it proposed `z.coerce.boolean()`, but that is `Boolean(v)` so `Boolean("false")===true` would silently flip a genuine "does not match" into "matches"; used an explicit `z.preprocess` string map instead. Shipped 4: **security/correctness (#379, issue #348)** — `hasProEntitlement` (RC/mobile) + `hasProEntitlementWeb` (Stripe/web) returned `true` (granting Pro to EVERYONE) when the secret key was UNSET, in any env → now fail CLOSED in production on that MISCONFIGURATION (loud log) while keeping the deliberate fail-OPEN on a genuine RC/DB runtime OUTAGE; `isEntitlementConfigured()` separates the two; the `billing/status` display route routed through the same logic so the shown plan can't contradict enforcement (+4 tests). **correctness/eval-regression (#380, issue #306)** — the diagnosis model sometimes returns `room_type_confirmation.matches_declared` as the string "true"/"false" → Zod `z.boolean()` hard-failed the whole diagnosis; added a `looseBoolean` helper (explicit trim/lower "true"/"false"→bool, NOT the coerce footgun; garbage still throws) (+7 tests). **F2 (#381)** — exported + tested `isLikelyProductUrl`/`isProactivelyBlockedDomain`/`extractBackfillKeywords`, the pure helpers that drop non-PDP/non-retailer URLs before an extraction LLM call + build the fallback query (spend-gating, 0 prior tests) (+22 tests). **mobile reliability (#382, Track B)** — the results screen fired upload→analyze in a useEffect with no cleanup, so a back→forward remount started a SECOND concurrent PAID `/api/mobile/analyze` while the first was still open; now an AbortController in a ref is aborted on unmount / superseded on retry, post-await setState guarded by `signal.aborted`. | Merged #379, #380, #381, #382 — all auto-merged on CI green (verify+build+mobile+journeys), branches deleted; verified default tip `ca6fc8e`. 8 Sonnet reviews (2×4) + 2 re-reviews on #379; ALL APPROVE. **1 in-cycle rework (#379):** both reviewers independently flagged that `app/api/billing/status/route.ts` bypassed the fixed gate (would show "Pro" in prod while enforcement caps the user) → routed it through the same env-aware logic + fixed the stale docstrings, re-reviewed → both APPROVE. Reviewer verifications: #380 mutation-tested by swapping in `z.coerce.boolean()` (4/7 tests fail); #382 correctness reviewer confirmed AbortController/AbortSignal runtime support in the real react-native@0.85.3/whatwg-fetch tree. tsc/eslint/determinism clean (root+mobile), 1513→1520+ tests (+7 diag, +22 orchestrator, +4 entitlement... net root +33; #379 already counted). No new migrations/secrets → PENDING_OPS unchanged. No ROADMAP box FULLY completes (C4/F2/F3/B already ticked or partial) → none ticked. functional_reality stays C (OWNER-BLOCKED); readiness NOT attempted. Partial-fix status recorded on issues #306 (boolean half) + #348 (entitlement half). |
| 2026-07-03 | Run 57 (concurrent with Run 56 #365) — DEEP AUDIT + 7 disjoint changes (correctness + F2 test + 2× a11y web/mobile + mobile reliability + preflight security + maxDuration hardening) | Ran CONCURRENTLY with a parallel instance's Run 56 (#365, which shipped #362-364 + its own deep audit); my sweep was independent and file-DISJOINT. DEEP AUDIT — 8-lens read-only Haiku sweep (correctness/dead-code, security-RLS, performance, a11y-design, test-eval, dependency-config, artifact-freshness, mobile-monetization); security swept CLEAN (no-op). **KEY: the QUALITY_SCORECARD (as_of 2026-07-01) is STALE on 3 of its named top_gaps — verified each against code and REJECTED:** product-verify maxDuration ALREADY =300 (route.ts:24 + wall-clock cap product-verifier.ts:271); refine.eval ALREADY a real live eval (summarizeRefineChanges behind RUN_EVALS); preflight RLS-coverage + NEXT_PUBLIC secret gates ALREADY present (GATE 6). Shipped 7: **correctness (#366)** — both search routes swallowed the PRIMARY candidate_products insert failure (supabase-js in-band error) → returned HTTP 200 products_found:0 / a 'done' SSE event, a fake 'found nothing' after the full agentic search already burned cost; now mark the agent-run failed + surface an error (500 / SSE error+closeStream, mirroring the orchestrator-error handler); secondary writes (evals, status flips) stay log-only by design. **F2 test (#367)** — lib/scoring/drift-monitor.ts (scoring-drift surveillance, 0 dedicated tests) → 18 mutation-resistant cases pinning the 3 thresholds + exact boundaries (stddev<1.2 clustering, median>7.5 strict, range<3 strict), MIN_SAMPLE_SIZE gate both sides, confidence_score exemption, NaN hygiene, even/odd median interpolation, 5000-cap eviction, resetScoreBuffer. **a11y web (#368)** — login/signup error banners were role-less div (SR silent on auth failure) → role='alert'. **a11y mobile (#369)** — auth-screen error Views → accessibilityRole=alert + liveRegion=assertive; 5 role-less action Pressables → role=button; submit buttons → busy/disabled state; single-text buttons kept label-free (RN aggregates child Text). **mobile reliability (#370)** — Settings sign-out was `void signOut()` (swallowed the in-band + thrown error → session stays live on a shared device) → await+check+Alert, mirroring the delete-account pattern. **security gate (#371)** — preflight GATE 6 had no EXPO_PUBLIC_* secret-leak grep (mobile vars bake into the unrevocable app binary) → added the mobile mirror of the NEXT_PUBLIC gate; closes the mobile half of the security_rls A+ item. **hardening (#372)** — identified-products confirm route awaits a remote Gemini embedImage before its DB write but had no maxDuration (a platform-kill aborts the confirmation write) → maxDuration=300, matching 17 sibling routes. | Merged #366, #367, #368, #370, #371, #372 (auto-merged on CI green: verify+build+mobile+journeys); #369 both-reviewers-APPROVE + auto-merge queued (mobile CI green pending). 14 Sonnet reviews (2×7); all 7 APPROVE both reviewers. 1 reviewer parity nit applied (#366 dropped a redundant console.error — apiError already logs). Reviewer B verified #367 by running the suite + a threshold-mutation; Reviewer A verified #372's DB-write-after-embed ordering. tsc/eslint/determinism clean (root+mobile), 1495→1513 tests. No new migrations/secrets → PENDING_OPS unchanged. No ROADMAP box FULLY completes → none ticked. functional_reality stays C (OWNER-BLOCKED); did NOT attempt the readiness issue. DEFERRED (recorded in loop-memory): free-tier '1 room' copy (gate now RESOLVED — 3-save cap, diagnoses uncapped; an undersell not an oversell; needs a full free-vs-Pro feature-gating audit before rewriting); mobile signup 'confirmation link sent' vs web's no-email flow (Supabase-config-gated — verify before acting); results.tsx unmount-cancellation; projects-list select('*') unused-column payload. |
| 2026-07-03 | Run 56 — DEEP AUDIT + 3 disjoint changes (security SSRF + a11y + F2 test); 1 abandoned | DEEP AUDIT DUE (last Run 52, 4 runs ago) → ran the full 8-lens read-only Haiku sweep FIRST (correctness/dead-code, security-RLS, performance, a11y-design, test-eval, dependency-config, artifact-freshness, functional-wiring-mobile). QUALITY_SCORECARD overall C — sole ship-critical blocker functional_reality C stays OWNER-BLOCKED. **KEY AUDIT DISCOVERY: the app runs on an IN-MEMORY data layer (memory-store), not Postgres** — `lib/supabase/server.ts createClient()` swaps only `.auth` to real Supabase; `.from`/`.storage`/`.rpc` all hit createMemoryClient(). This invalidated a perf candidate and bit the first security draft (see below). Scout FALSE POSITIVES verified & rejected (Haiku over-report): product-verify maxDuration ALREADY=300 (#283, scorecard stale); search route insert "silent loss" ALREADY logs every in-band write (#309 pattern); diagnosis/rooms GET "no try/catch" — Next catches→500 (standard); gemini undefined-key — assertProductionEnv fails loud in prod. Shipped: **Security (Track G)** — `POST /api/projects/[projectId]/floor-plan` passed image_url to runFloorPlanExtraction→fetchAsBlob, which raw-fetch()es absolute URLs → SSRF (169.254.169.254 IMDS/localhost/private IPs). Guard allows relative `/uploads/...` (no `..`, local-disk read) OR https-on-own-Supabase-host, rejects else 400; preserves the current relative-path flow, forward-compatible with the real-Storage cutover (#364). **A11y (Track F/A2)** — ManualSourcingForm remove-URL icon-only Button had NO accessible name (WCAG 4.1.2) → aria-label (humanized category label reused from the visible heading) + aria-hidden on decorative Trash2/Plus (#362). **F2 test** — cosineSimilarity (retrieval-gating vector math; topKSimilar drops priors below minSimilarity) had no dedicated test → 12 mutation-resistant cases (identical=1/scale-invariance/orthogonal=0/opposite=-1/known non-trivial cosine/symmetry/[-1,1] bound; zero-norm→0-not-NaN split 3 ways to catch a `\|\|`→`&&` mutation; dim-mismatch exact-message throw) (#363). | Merged #362 + #363 (auto-merged on CI green: verify+build+mobile+journeys). #364 (SSRF) both-reviewers-APPROVE + auto-merge queued. 10 Sonnet reviews (2×C3 + 2×C5 + 2×C2 first cycle + 2×C2 second cycle). **2 review rejections (the value bar + review working):** (a) C3 Reviewer A caught the aria-label using the raw snake_case key → fixed to reuse the humanized heading label; (b) C2 Reviewer A caught the FIRST SSRF draft (https-host-only, copied from mobile/analyze) as a would-be REGRESSION — real image_urls are relative `/uploads/...` today, which it 400'd → reworked to allow both shapes, re-reviewed by 2 fresh reviewers → both APPROVE. **ABANDONED perf/embedding-pgvector-rpc (never pushed):** proposed a pgvector match_product_embeddings RPC (migration 029) for topKSimilar's full-table scan (a scorecard-named perf gap); both reviewers REQUEST_CHANGES after finding the RPC is INERT under the in-memory data layer (memory-store.rpc returns {data:null}) — the perf gap doesn't bind until an unscheduled memory-store→Postgres cutover, so shipping inert code + a must-apply migration for zero current benefit is premature. tsc/eslint/determinism clean, mobile tsc clean, 1483→1495 tests. No new migrations/secrets → PENDING_OPS unchanged. No ROADMAP box FULLY completes → none ticked. functional_reality stays C (OWNER-BLOCKED). |
| 2026-07-02 | Run 55 — 6 disjoint changes (correctness + G2 security + Track-B a11y + 3× F2 agent tests) | DEEP AUDIT not due (ran Run 52 → next ~Run 56). QUALITY_SCORECARD overall C — sole ship-critical blocker functional_reality C stays OWNER-BLOCKED (core E2E needs Supabase-local + Stripe test-mode in CI, `.github/` human-applied). Ran a 5-lens Haiku scout sweep (security-G / correctness / test-coverage / mobile / artifact-freshness); security G1 CLEAN (all 26 paid routes rate-limit+spend-guarded), G4 sufficient (login/reset delegated to Supabase), correctness scout's top writes (search/analyze-apartment/refine) already gracefully logged (#309 pattern) — not gaps; artifact audit mostly CLEAN. Shipped: **correctness (A)** — `POST /api/diagnosis` room-status→"diagnosed" update ignored supabase-js's in-band error → room stuck at prior status w/ no server signal; capture+log, mirroring bundles/evaluate+search (#337). **G2 security** — the 3 remaining unguarded `await request.json()` (area-analysis refine-chat 3-5min pipeline; computer-use product-verify Browserbase session; saved-designs/[id] PATCH share-token write) returned a misleading 500 on malformed JSON → try/catch→400, matching ~30 other routes; a full app/api re-audit confirms zero unguarded const-parses remain (#338). **A11y (mobile, Track B)** — the design-tips Collapsible header (used in explore.tsx) was a role-less Pressable (SR dead-end, no toggle state) → accessibilityRole=button + accessibilityState={{expanded}}; role alone carries the name (RN aggregates child Text), no redundant label (#339). **F2** — first unit suites for the pure token-gating surface of two heavy agents: `shopping-researcher.ts` sanitizeSearchQuery (behavior-neutral export) + deduplicateCandidates → 15 cases (#340); the junk prefilter EXTRACTED to a pure exported `prefilterJunkCandidates` (patterns hoisted to module scope — safe, non-global regexes) → 9 cases incl. the `\bnovel\b` boundary guard so "novelty console table" survives (#342); `design-coordinator.ts` auto-finalize decision (gates a ~50-100K-token harmony turn) EXTRACTED to a pure exported `evaluateAutoFinalize` → 15 cases pinning 8.5/0.2/stabilized boundaries + the ≥1-round gate + reason precedence (#341). | Merged #337-341 (all auto-merged on CI green: verify+build+mobile+journeys); #342 both-reviewers-APPROVE + auto-merge queued on CI. 12 Sonnet reviews (2×6) + 1 re-review; **1 review rejection**: Reviewer B caught #338's "last two" completeness claim as FALSE (a 3rd unguarded route existed) → added the 3rd guard + corrected the claim + re-audited → APPROVE (the value bar working). Combined suite +39 tests, tsc/eslint/determinism clean, mobile tsc clean. No new migrations/secrets → PENDING_OPS unchanged. No ROADMAP box FULLY completes (G2 broader than request.json guards; F2 incremental) → none ticked. functional_reality stays C (OWNER-BLOCKED). INCIDENT: a review subagent ran `git stash/checkout` in the SHARED working tree and wiped my uncommitted design-coordinator edits mid-run → recovered by re-applying + committing-before-review; hardened by instructing later reviewers to use read-only git only. |
| 2026-07-02 | Run 54 — 5 disjoint changes (F2 test + F3 eval + correctness + 2 a11y) | DEEP AUDIT not due (ran Run 52 → next ~Run 56). QUALITY_SCORECARD overall C — sole ship-critical blocker functional_reality C stays OWNER-BLOCKED (core E2E needs Supabase-local + Stripe test-mode in CI, `.github/` human-applied). Ran a 7-lens Haiku scout sweep; SECURITY CLEAN no-op (RLS complete on all migrations, no committed secrets, entitlements server-verified). Shipped: **F2** — first unit suite for `lib/agents/post-search-coordinator.ts` (624 LOC, 0 prior tests): exported the pure `formatStateForAgent` + 24 mutation-resistant cases pinning the audit-trend bands (STALLED `\|delta\|<0.3`, REGRESSING, improving, improving-strongly `>=1.0`, last-two-entries delta w/ full trend arrow), saturation (`count>=3 AND scoreMax>=7.0`), diminishing-returns (`re-search>=2`), missing/dropped surfacing, tried-queries `slice(-8)` — boundaries tested both sides; one case documents `6.0→6.3`→STALLED via float precision `0.299…<0.3` (#333). **F3** — mockup image-generation was the ONLY core pipeline stage without a live eval → `evals/__tests__/mockup.eval.test.ts` calls real `generateMockupImage` + round-trips `verifyMockupImage` (confidence>=0.5 anti-flake floor), gated `RUN_EVALS=1`, gold fixture INLINED so it doesn't leak into the shared `loadGoldCases()` loops (diagnosis eval + runner.test iterate the dir) (#334). **Correctness** — the self-learning embedding write-back is a one-shot side effect that durably persists `embedding_written_back=false` on a transient insert failure with NO backfill retry → added bounded deterministic `insertEmbeddingWithRetry` (3 attempts, fixed delay, no jitter, injectable delay=0 for tests) at both confirm+correct call sites + 7 tests (#332). **A11y (web)** — diagnosis streaming progress conveyed per-step status ONLY via a decorative icon (no text equiv) with no aria-live → aria-live=polite + icons aria-hidden + sr-only status text per step (#330). **A11y (mobile)** — 11 role-less action Pressables across photo/results/saved core-journey screens announced as static text → accessibilityRole=button + accessibilityState on Save; single-text buttons so no redundant labels (#331). | Merged #330-334; all 5 auto-merged on CI green (verify+build+mobile+journeys). Combined suite 1444 pass/11 skip (+31), tsc/eslint/determinism clean, mobile tsc clean. 10 Sonnet reviews (2×5); all 5 APPROVE both reviewers — non-blocking notes only (embedding retry doesn't wrap a *thrown* connection error, pre-existing; `withRetry` reuse a simplify-later; slice(-8) test could be stronger). No new migrations/secrets → PENDING_OPS unchanged. No ROADMAP box FULLY completes (F2/F3/F4/correctness all incremental within partial tracks) → none ticked; F3 now has a live eval for every core stage but the independent scorecard still lists tests_evals gaps so F3 stays unchecked pending independent confirmation. functional_reality stays C (OWNER-BLOCKED). |
| 2026-07-02 | Run 53 — 5 disjoint changes (F2 test + 2 a11y + mobile reliability + artifact honesty) | DEEP AUDIT not due (ran Run 52 same day → next ~Run 56). QUALITY_SCORECARD overall C — sole ship-critical blocker functional_reality C is OWNER-BLOCKED (core E2E needs Supabase-local + Stripe test-mode in CI, `.github/` human-applied). Ran a 7-lens Haiku scout sweep; security CLEAN no-op. Shipped: first unit suite for `lib/agents/mockup-agent.ts` (0 prior tests) — 18 mutation-resistant cases on pure `buildMockupContext` (fallback precedence, `[]`-truthy empty-array→undefined collapse, action_list→what_it_needs placement-map overwrite) + 3 on generateMockupImage result paths, Reviewer A mutation-verified (#324, F2); RefineChat partial-success warning was piped through the RED destructive banner (reads as failure) → split a distinct amber `warning` state reusing existing amber tokens + aria-expanded/aria-hidden/role=alert on the toggle & banners (#325, A2/F4); mobile results.tsx flipped to "Retry Save" on a failed save with NO user-facing message (silent error for a paying user) → added an Alert mirroring the post-purchase pattern (#326, Track C); support "Pricing & billing" blurb advertised a "trial period" the web product doesn't offer (FAQ: "no trial clock"; stripe.ts sets no trial_period_days) → "the free tier" (#327, D/F5); room-type picker options were unlabeled Pressables with NO accessibilityRole (SR dead-end on a core-flow step) → accessibilityRole=button + label (#328, Track B). ABANDONED 2 on Reviewer B value/regression grounds (the bar working): analyze-apartment Promise.all (<0.05% of a 180-300s LLM-dominated route); paywall accessibilityLabel (RN already aggregates child Text on a role=radio Pressable → likely redundant + drops the subline). | Merged #324-328; CI green (verify+build+mobile+lint+journeys) on all 5, none flaked. Combined suite 1413 pass/9 skip (+21), tsc/eslint/determinism clean, mobile tsc clean. 14 Sonnet reviews (2×7); all 5 merged APPROVE both, 2 candidates Reviewer-B-rejected on value → abandoned (clean tree, remote branches left un-PR'd). No new migrations/secrets → PENDING_OPS unchanged. No ROADMAP box fully completes (all 5 incremental within partial Tracks A/B/C/D/F) → none ticked. functional_reality stays C (OWNER-BLOCKED). |
| 2026-07-02 | Run 52 — DEEP AUDIT + 6 disjoint changes (2 correctness + perf + a11y + test + quality-gate) | DEEP AUDIT due (last Run 48) → ran the 8-lens read-only Haiku sweep FIRST; SECURITY CLEAN (no-op, RLS complete on all 27 migrations). Shipped from audit findings: guard the unchecked `search_sessions` insert in search + search/stream — a null session made every `session?.id` undefined so the end-of-run `.eq("id",undefined)` update orphaned the session (#309, class of #274/#296/#297); refine-chat honesty — the assistant `refine_messages` insert was unchecked → 200 with `assistant_message: undefined` (a reply that vanishes on reload + a client null-deref crash); since the re-analysis already committed a new room_diagnoses row, return applied analysis + `assistant_message: null` + warning (NOT a 500 that hides it and forces an expensive duplicate-message retry) + client filters null (#313); parallelize the independent accepted/rejected reads in `loadUserFeedbackContext` (every search) → Promise.all (#310, class of #265/#277/#290); a11y — aria-label + aria-pressed on the theme-toggle group and aria-label on the floor-plan remove button (icon-only, `title`-only before → unlabeled to SR) (#311); browserbase-driver.ts (263 LOC, sole Computer-Use↔Chrome integration, 0 tests) → 23 mock-only tests, Reviewer A mutation-verified all 7 mutations flip red (#312); raise decorative vitest coverage floors 25/19/30/25→40/30/42/40 (~10pt under real ≈50/39/54/51), comment honest that they gate `npm run test:coverage` only, not the bare-`vitest run` CI verify job (#314). | Merged #309-314; CI green (verify+build+mobile+journeys) — #311 needed one re-trigger past a transient `supabase/setup-cli` "rate limit exceeded" flake (not the change). Combined integration suite 1390 pass/9 skip (+23), tsc/eslint/determinism/coverage all green. 16 Sonnet reviews across 6 changes; 2 changes took a cycle-2 revision (refine-chat: reviewer showed throwing was worse than the fake-success; coverage: reviewers caught the comment over-claiming CI enforcement). A5 ticked + annotation refreshed (stale "zero live evals/placeholder" → 5 real live evals + live-eval.yml). functional_reality stays C — OWNER-BLOCKED (core E2E needs Supabase-local + Stripe test-mode in CI, `.github/` human-applied). PENDING_OPS += wire-coverage-into-CI. |
| 2026-07-01 | Run 51 — 4 disjoint changes (LLM-core test + perf + mobile a11y + docs) | DEEP AUDIT not due (last Run 48, next ~Run 52). Consumed the fresh QUALITY_SCORECARD (as_of today, overall C — functional_reality C is human-gated on E2E_AUTH_STACK; correctness gap it names already closed #283) + 6 Haiku scouts. Shipped: `verify-search-candidates` (post-search Computer-Use grounding, 0 LLM) first unit suite — 14 tests, mutation-verified by a reviewer (URL cache-key normalisation, top-1-by-score, cache-hit short-circuit, fill-only-empty merge, cache-write only on `completed`, skip/fail/reject paths) (#289); evaluate-set parallelize independent project + sibling-rooms reads → Promise.all, one round-trip off the hot path (#290, same class as #265/#277); mobile back-button a11y — `accessibilityRole`/`accessibilityLabel` on photo/room-type/results core-journey screens (#291); README documents DeepSeek as a required prod provider (env.ts requires DEEPSEEK_API_KEY unless AI_PROVIDER=gemini) (#292). DROPPED two scout candidates after verification: (a) security "CRITICAL — missing WITH CHECK on 13 tenant tables" = FALSE POSITIVE (Postgres uses USING as WITH CHECK when omitted on FOR ALL); (b) "brand-name inconsistency AptDesigner→AptDesignerAI" = FALSE POSITIVE (brand-kit defines "AptDesigner" as the preferred DISPLAY name). | Merged #289-292; CI green (verify+build+mobile+journeys), combined suite 1367 pass/9 skip (+14). 8 Sonnet reviews, ALL APPROVE first pass, zero re-reviews. No new migrations/secrets; PENDING_OPS unchanged; no ROADMAP box fully completes (all 4 incremental within partial Tracks A/F) → none ticked. |
| 2026-07-01 | Run 50 — 4 disjoint changes (correctness ship-critical + security preflight guard + mobile monetization + eval completeness) | Fresh QUALITY_SCORECARD (overall C, 3 ship-crit dims < A) as the vetted feed + 3 Haiku scouts. Shipped: computer-use product-verify `maxDuration=300` + agent-loop wall-clock cap (`maxWallClockMs=270_000`, injectable clock, driver disposed on trip) so the agentic Browserbase verify loop stops cleanly instead of platform-killed mid-turn — closes the LAST (18th) pipeline-route correctness gap the scorecard named (#283); preflight GATE 6 — mechanical per-public-table RLS-coverage (direct + dynamic `do $$ … array[…] … enable RLS` forms) + `NEXT_PUBLIC_*(SECRET/SERVICE_ROLE/PRIVATE)` client-secret-leak assertions, both no-ops on the clean 26-table tree, security_rls→A+ (#286); mobile post-purchase entitlement refresh honesty — `refresh()` retries 3× + returns `Promise<boolean>` so a paying user isn't stranded `isPro=false`, honest sync alert (#284); `refine.eval.test.ts` → real gated live eval (5th core stage now live, calls `summarizeRefineChanges`) + runner smoke tests relocated verbatim to `runner.test.ts` (#285). | Merged #283-286; CI green (verify+build+mobile+journeys), combined suite 1353 pass/9 skip. 8 Sonnet reviews + 1 re-review (RLS-parser file-scoped false-negative found by Reviewer A → fixed to array-in-do-block scope + verified via reproduction). No new migrations/secrets; PENDING_OPS unchanged; no ROADMAP box fully completes (all 4 incremental within partial Tracks A/C/F). |
| 2026-07-01 | Run 49 — 8 disjoint changes (2 tests + correctness + security + compliance + mobile + perf+test + a11y) | Scouted 8 Haiku lenses; shipped: PipelineTracer test (#273); products/evaluate unchecked status-write guard (#274); places/photo daily-spend guard — G-class wallet-drain (#275); mobile restore-purchases honesty — no fake success / misleading error (#276); diagnosis-expansion parallelize sibling+budget fetches + first unit suite (#277); identified-products-pipeline first unit suite (#278); keyboard-accessible scorecard toggle — WCAG 2.1.1 (#279); privacy policy discloses ALL 11 sub-processors incl. Stripe/RevenueCat/Tavily/Maps/Browserbase/Resend/Turnstile/Vercel + app-privacy doc (#280). | Merged #273-280; CI green (verify+build+mobile+journeys). 16 Sonnet reviews + 3 re-reviews; privacy took 3 completeness rounds. |
| 2026-07-01 | DEEP AUDIT (Run 48) | Ran the 8-lens read-only Haiku audit across the whole codebase. Security/RLS CLEAN through migration 027 (no-op = correct). Findings distilled → this run's 7 file-disjoint changes below; deferred items (embedding-index pgvector RPC, twin-write status-flips, mobile entitlement-refresh surfacing) recorded in loop-memory. | (audit) |
| 2026-07-01 | Store readiness (Run 48) | Made the Expo signup screen's Terms of Service / Privacy Policy tappable (in-app browser, canonical .com URLs) — Apple 5.1.1(v)/Play require in-app legal access before account creation; signup was the last un-linked surface. | PR #263 |
| 2026-07-01 | Store readiness (Run 48) | Reconciled the user-facing contact email across 7 UI surfaces (checkout, terms, support, privacy, faq, marketing footer) from legacy hello@aptdesigner.app → canonical hello@aptdesignerai.com (owner decision #261; matches the transactional sender). | PR #264 |
| 2026-07-01 | Performance (Run 48) | Parallelized the two independent reads (project fetch + inferUserPreferences) in the diagnosis route via Promise.all — removes a serialized DB round-trip (~200-400ms) on the core hot path; behavior identical, determinism preserved (destructured by position). | PR #265 |
| 2026-07-01 | A11y (Run 48) | Toast dismiss button given an accessible name (WCAG 4.1.2) + focus ring + aria-hidden icon, and Radix announcement urgency typed (error=assertive, else polite) — previously every toast announced assertively and the close button had no name. | PR #266 |
| 2026-07-01 | A11y (Run 48) | Compare-table sort controls made keyboard-accessible: moved the sort trigger from a non-semantic <tr onClick> onto a real <button> with a focus-visible ring + state-aware aria-label (WCAG 2.1.1/4.1.2) on an authed design-dense route. | PR #267 |
| 2026-07-01 | Tests/evals (Run 48) | Covered the floor-plan prompt formatter (format-floor-plan.ts — feeds every non-vision agent prompt) with 13 deterministic branch tests; both reviewers mutation-verified. lib/agents coverage (~21% gap). | PR #268 |
| 2026-07-01 | Tests/evals (Run 48) | Covered the cross-session user-feedback context loader (loadUserFeedbackContext — feeds the paid search pipeline) with 9 fail-closed branch tests; the rooms-error assertion mutation-verified to pin the roomsError guard independently. | PR #269 |
| 2026-06-30 | Business case (Run 47) | Re-grounded base-case organic 50%→40% (top of cited benchmark), added a net-margin sensitivity table, credited the BUILT referral (#226) + web upsell (#238) levers + Apple SBP 15% upside; ARR unchanged ($122.9K, no gaming). Both reviewers verified the arithmetic + anti-gaming posture; strengthens ship-critical business_case_strength. | PR #258 |
| 2026-06-30 | Security/G1 (Run 47) | Rate-limit + daily-spend guards + maxDuration on the two fully-unguarded paid LLM endpoints (floor-plan extraction, product correction) — a single authed user could previously drive unbounded model spend. | PR #251 |
| 2026-06-30 | Reliability (Run 47) | maxDuration=300 on 4 product/bundle LLM routes that lacked it (Vercel platform-kill risk on paid paths) + checkDailySpend on the 2 (products/ingest, bundles/evaluate) missing it. Completes the #206 maxDuration coverage. | PR #250 |
| 2026-06-30 | Correctness (Run 47) | Guarded the mockup_jobs insert against fake success — a failed insert left the job undefined while the expensive image pipeline ran, downstream status updates no-op'd, and the route returned id:undefined as a 200. Now 500s before the work; cache-path insert kept non-fatal (valid image still returned). | PR #252 |
| 2026-06-30 | Mobile (Run 47) | 30s timeout on the save-design fetch (was a raw fetch with no AbortController → frozen "Saving…" Save button dead-end; Retry never returned). | PR #253 |
| 2026-06-30 | Mobile (Run 47) | 15s timeout on the account-delete fetch (raw fetch → stuck disabled "Delete account" button with no feedback on the Apple-required deletion flow). | PR #254 |
| 2026-06-30 | Tests/F2 (Run 47) | mockup-verifier agent test — 11 provider-mocked branch tests incl. the self-correction retry loop (verify→regenerate→accept / max-attempts). lib/agents (LLM core) ~21% covered. | PR #256 |
| 2026-06-30 | Tests/F2 (Run 47) | product-verifier agent test — 8 tests: 0.75 match-threshold gate, min(prior,score) caution, structured→text fallback, grounding-metadata backfill, both fail-open paths. | PR #257 |
| 2026-06-30 | Tests/F2 (Run 47) | room-architecture-extractor agent test — 11 tests (empty/parse-fail/shape-fail/throw fail-open, 4-photo cap, floor-plan image, formatter branches). Reviewer measured 94% stmt / 83% branch on the module. | PR #255 |
| 2026-06-30 | Correctness (Run 46) | The **streaming search** route inserted `product_evaluations` rows then flipped candidate products to `status="evaluated"` with both writes unchecked — the twin-write status-drift pattern fixed in evaluate-set (#233), flagged in the Run-45 rotation guide. A failed insert still marked products "evaluated" (orphaned status, hidden from re-evaluation) while the stream reported `done`. Now checks the insert error, only flips status when the rows persisted, logs both via `logServerError`. | PR #240 |
| 2026-06-30 | Correctness (Run 46) | **analyze-apartment** ran a per-room `Promise.all` of "insert room_diagnoses" + "update rooms status=diagnosed" (plus a project update) with every write unchecked, so a failed insert still flipped the room to "diagnosed" and silently dropped that room's analysis. Sequenced the writes: persist diagnosis first, only flip status once it landed (`continue` on insert failure), log every write error. | PR #241 |
| 2026-06-30 | Mobile / Monetization (Run 46) | The paywall swallowed RevenueCat `getOfferings()` failures and kept `FALLBACK_OPTIONS` (null packages), so "Start Free Trial" hit `if (!RC_KEY || !pkg)` and **silently dismissed** — a dead CTA / lost conversion on a configured build. Split the guard (dev-mode dismiss vs configured-no-pkg → "Pricing unavailable" alert, sheet stays open) and log the failure. | PR #242 |
| 2026-06-30 | Mobile reliability (Run 46) | The saved-designs **share** fetch had no timeout; a hung endpoint never rejects, so the existing catch never fired and the Share button froze with no recovery. Added a 15s `AbortController` timeout that routes a hang through the existing fallback share sheet. | PR #243 |
| 2026-06-30 | Tests / lib/agents (Run 46) | Covered the **furniture-cropper** vision agent (9 provider-mocked tests): crop parse + source tagging + token accumulation, determinism contract, box clamping on overflow, tiny-box noise filter, empty-label→"other", per-photo error resilience, empty-input short-circuit, room-hint rendering. (lib/agents was ~21% covered.) | PR #244 |
| 2026-06-30 | Tests / lib/agents (Run 46) | Covered the **product-identifier** tiered confidence-floor filter (7 tests): above-floor return + priors echo + token sum + model, determinism contract, absolute USER_PROMPT_FLOOR drop, in-list pass-through (MIN_CONFIDENCE_IN_LIST intentionally not enforced), out-of-list guardrail (MIN_CONFIDENCE_OUT_OF_LIST−0.35), schema-fail + LLM-error fail-open. | PR #247 |
| 2026-06-30 | Tests / lib/agents (Run 46) | Covered the **floor-plan-extractor** (7 tests): well-formed normalization, invalid-enum coercion to safe defaults + bad-number→undefined, missing-wall/label defaults, determinism contract (seed + JSON mime + diagnosis thinking + Files-API resolution), unexpected-shape failure, fail-open on image-resolution + LLM errors. | PR #245 |
| 2026-06-30 | Tests / lib/agents (Run 46) | Covered the **mockup-prompt-validator** pre-image audit (6 tests): well-formed audit success + token sum + model, determinism contract, prompt construction (specs/placement/direction), empty-state fallbacks, schema-fail → success=false, fail-open on LLM error. | PR #246 |
| 2026-06-30 | Monetization / business-case lever (Run 45) | Built the in-product **web upgrade/expansion surface** — the `business_case_strength` gap (expansion lever listed-not-built; web parity with the mobile paywall). Reusable `UpgradeCtaCard` (warm-editorial: usage meter w/ `role=progressbar`, Pro benefits, real Stripe checkout link `/billing/upgrade`); new `GET /api/billing/status` returns plan + free-tier save usage (server-side entitlement via `supabase.auth.getUser()`, never client-trusted; fails open to `hasPaid` when billing creds absent so an unverifiable user is never nagged); `/saved` surfaces the card only for free-tier users, copy adapts at the limit. No new secret/migration (reuses existing Stripe entitlement path). | PR #238 |
| 2026-06-30 | Correctness (Run 45) | The diagnosis SSE route flipped `rooms.status="diagnosed"` with an unchecked update — a failed write silently left the row out of sync with status-filtered reads (diagnosis itself already persisted, so non-fatal). Now destructures the error and surfaces it via `logServerError`; stream behaviour + non-fatal contract unchanged. | PR #232 |
| 2026-06-30 | Correctness / paid path (Run 45) | `evaluate-set` inserted a `product_evaluations` row then flipped the product to `status="evaluated"` with both writes unchecked — a failed insert still marked the product evaluated, orphaning the status + hiding it from re-evaluation. Now checks the insert error, only updates status when the row persisted, and logs both failures via `logServerError`. | PR #233 |
| 2026-06-30 | Mobile reliability / Track B (Run 45) | The photo-capture screen's Android pending-result recovery called `getPendingResultAsync().then(...)` with no `.catch`, so a rejection (permission revoked, FS error) became an unhandled promise rejection. Added a `.catch` that logs and falls back to normal re-pick (best-effort recovery; happy path unchanged). | PR #234 |
| 2026-06-30 | Tests / eval coverage (Run 45) | Covered the previously-untested **requirement-validator** (the final gate between product search + the user) with 7 tests: success + token accounting, the determinism contract (seed + cheap thinking), `formatProduct` rendering via the prompt (price/materials/colors/dims + NONE for an uncovered category), schema-fail → `success:false`, fail-open on a thrown LLM error, and Tavily not reached by default. Provider-mocked, headless. | PR #235 |
| 2026-06-30 | Tests / eval coverage (Run 45) | Covered the previously-untested **category-planner** (decides the FINAL search category list) with 5 tests: success + token accounting, the determinism contract (seed + cheap thinking + structured-output request), the in-call fallback from structured+tools to a tools-only chat when the model rejects `responseSchema` (asserts the second call drops the schema but keeps the seed), schema-fail, and fail-open when both calls error. | PR #236 |
| 2026-06-30 | Tests / eval coverage (Run 45) | Covered the previously-untested **correction-planner** (drives the re-search/drop/accept loop) with 6 tests: success + token accounting, the determinism contract, the `enableTools` branch (grounding section present by default / omitted when disabled), an `accept` action plan, schema-fail, and fail-open on a thrown LLM error. Mocks `getProvider`. | PR #237 |
| 2026-06-30 | Growth / business-case lever (Run 44) | Built the waitlist **referral loop** — the concrete invite/reward mechanic the business case lists as a revenue lever (organic-share input only defensible once a real referral path exists). Migration 026 adds `referral_code` (unique partial index) + `referred_by` to `waitlist_emails`; pure tested helpers (`lib/waitlist/referral.ts`) generate an unguessable 8-char code + sanitise inbound `?ref=`; `/api/waitlist` issues each subscriber's code (collision-retry + post-loop 500 guard), attributes a verified referrer, returns the code; the waitlist form surfaces a "Jump the line" share card (copy + aria-live). Closes the scorecard `business_case_strength` "referral listed-not-built" gap. Migration human-applied (PENDING_OPS 026). | PR #226 |
| 2026-06-30 | Compliance / store-readiness (Run 44) | **CAN-SPAM email opt-out** — lifecycle emails already linked to /account for "Manage email preferences" but had no backing store/UI/send-gating. Migration 027 adds the `user_email_preferences` tenant table (RLS auth.uid()=user_id); `isMarketingOptedOut()` fail-closed; cookie-authed `/api/user/email-preferences` (user-scoped client → RLS); accessible `role=switch` toggle on /account; the **marketing** sends (win-back webhook + activation cron) now gated on the preference (transactional mail untouched; paid-welcome annotated CAN-SPAM §6 exempt). Migration human-applied (PENDING_OPS 027). | PR #227 |
| 2026-06-30 | Correctness / determinism (Run 44) | `extractFromImage` called `geminiProvider.chat` without `seed: DETERMINISTIC_SEED` while its siblings pass it — a determinism-contract gap the harness-ratchet doesn't catch. Added the seed + a regression assertion. | PR #222 |
| 2026-06-30 | Correctness (Run 44) | `apartment-research` ignored supabase-js's in-band `{error}` on the `projects.update()` that persists `building_research`, faking a 200 while the research was lost on reload. Now checks the error → 500 with a retry message (fatal-vs-log precedent, PR #195). | PR #223 |
| 2026-06-30 | Mobile reliability (Run 44) | The core mobile journey's `uploadImage` + `analyzeRoom` fetches had no `AbortController`, so a hung request left the Results screen on an indefinite spinner (force-quit-only escape). Added `fetchWithTimeout` (90s upload / 330s analyze — just past the server's 300s maxDuration), mirroring the web's external-call timeout discipline. | PR #224 |
| 2026-06-30 | Tests / eval coverage (Run 44) | Covered the previously-untested 722-line deterministic `area-analysis-validator` (post-LLM constraint patcher) with 11 branch tests across all five check blocks (architectural/invalid-remove strip, exclusion removal, keep-item-in-remove, missing-request auto-inject + flag, furniture-pairing auto-inject + flag), the flagged-vs-removed `wasModified` distinction, the deep-clone no-mutation contract, and missing-array safety. | PR #225 |
| 2026-06-29 | Performance (Run 43) | Parallelized the two independent photo-grounding LLM calls in the area-analysis pipeline (`verifyWhatShouldGoAgainstPhotos` for `what_should_go` + `verifyWhatWorksAgainstPhotos` for `what_works`) with `Promise.all` — they write distinct fields with no cross-dependency but ran serially, so this stage paid ~2× its latency on the core paid path. Results applied in a fixed order → output + determinism unchanged. Closes the QUALITY_SCORECARD `performance` top_gap. | PR #213 |
| 2026-06-29 | Reliability (Run 43) | Declared `export const maxDuration = 300` on `POST /api/area-analysis/refine-chat`, which re-runs the full 3–5 min `runAnalysis` pipeline but had no duration declared (the one heavy route PR #206 missed) — Vercel's short platform default could kill it mid-run on a paid path. Closes the scorecard `correctness` gap on that route. | PR #214 |
| 2026-06-29 | Tests / F2 (Run 43) | Added `whatitneeds-enricher.test.ts` (12) for the previously-uncovered LLM-core module: the pure `hasSpecificity` dimension+material truth table + common dimension formats, and `enrichWhatItNeeds` branches (no model call when all specific, skip-list categories, patch-by-index, fail-open on reject + non-array). Provider-mocked, headless. | PR #215 |
| 2026-06-29 | Tests / F2 (Run 43) | Added `product-extractor.test.ts` (7): `extractFromImage` parse/validate success (+ markdown-fenced JSON), room-photo attachment order, 3-photo cap, and 2 fail-soft branches (model rejects / unparseable); `extractFromUrlBatch([])` short-circuit asserted against the real collaborator (`tavilyExtract`). Targets the scorecard `tests_evals` critical gap (lib/agents ~21%). | PR #216 (auto-merge queued; gate green, 2 approvals) |
| 2026-06-29 | Correctness / reliability (Run 42) | Declared `export const maxDuration = 300` (Vercel Pro ceiling) on the 9 long-running LLM pipeline routes (area-analysis +refine, mockups, diagnosis +stream, search, analyze-apartment, apartment-research, mobile/analyze). None declared a duration, so on Vercel they inherit the short default and get platform-killed mid-pipeline — a "builds green, request killed" failure on the core paid path. Closes the QUALITY_SCORECARD high-severity correctness gap. | PR #206 |
| 2026-06-29 | Security / G1 (Run 42) | Added per-user `checkRateLimit` + `checkDailySpend` to `POST /api/products/evaluate` (productEvaluate 30/min) and `evaluate-set` (bundleEvaluate 10/min) — the last two paid-LLM endpoints with no guard, so an authed user could drain the LLM budget (the scorecard "G1" gap). Mirrors the diagnosis pattern; server-side only. | PR #207 |
| 2026-06-29 | Store readiness / compliance (Run 42) | Corrected the privacy page's AI-processor list: it falsely named "Anthropic Claude, OpenAI" (neither is a dependency/subprocessor). Now lists the real processors — Google Gemini (photos+text) + DeepSeek (text-only) — matching the already-correct docs/app-privacy.md. Removes an App-Store/data-safety review risk. | PR #208 |
| 2026-06-29 | Design bar (Run 42) | Added `app/not-found.tsx`, a design-system 404 (Card/Button/accent-warm token/Lucide/dark-parity), replacing Next.js's unstyled default template that broke the warm-editorial design bar. Server component; graceful logged-out routing. | PR #210 |
| 2026-06-29 | Reliability (Run 42) | Fixed a double-close in `app/api/search/stream/route.ts`: the `!result.success` branch closed the SSE controller, then the `finally` closed it again → "Controller is already closed" thrown in finally + un-finalized agent run (same class as #191). Added the `streamClosed` latch + idempotent `closeStream()`; also declared `maxDuration = 300` here (disjoint from #206). | PR #209 |
| 2026-06-29 | Tests / F2 coverage (Run 42) | Added `__tests__/agents/refine-summarizer.test.ts` (provider-mocked, 4 real branches: token summing, slim()-filtered prompt + low-thinking request, empty-text fallback, non-retryable-error fallback) for the ~21%-covered lib/agents LLM core. | PR #211 |
| 2026-06-29 | Correctness (core pipeline) | Surfaced previously-silent DB write failures (supabase-js returns errors in-band): unchecked `room_diagnoses` inserts in area-analysis POST + refine now fail honestly (500 + agent run marked failed) instead of returning 200 with data that vanishes on reload; search completion-path `Promise.all` write batches + bundles status-flip now log in-band errors (matching the file's existing `insertError` precedent). Same fake-success class as #185. | PR #195 |
| 2026-06-29 | Reliability (SSE) | Guarded the diagnosis stream against a client-disconnect crash: `sendEvent` enqueued without a closed-controller guard, so a mid-diagnosis disconnect threw "Controller is already closed", the outer catch threw a second uncaught error via `sendEvent("error")`, and `controller.close()` a third — leaving the agent run un-finalized. Added a `streamClosed` latch + try/catch and an idempotent `closeStream()` (matches the search/stream precedent). | PR #191 |
| 2026-06-29 | Tests (F2 coverage) | Added 10 deterministic tests for the previously-untested `lib/agents/search-cache.ts` (query+screen LRU caches): hit/miss, full key sensitivity, batch-URL order-insensitivity, 48h TTL from creation-not-access, 500-entry LRU eviction with recency bump, and the DETERMINISTIC bypass (mocked flag + fake timers + per-test module reset). | PR #196 |
| 2026-06-29 | Tests (F2 coverage) | Added 9 tests for the previously-untested `lib/validation/harmony-math.ts` orchestrator: aggregation (neutral 0.5, per-item [0,1] 2dp, overall=mean), the lighting-adequacy rules it owns directly, cross-room direction gating, and the prompt evidence block — without re-testing the 12 already-covered sub-validators. | PR #194 |
| 2026-06-29 | A11y (web) | Added accessible `title` attributes to the two dashboard Google Maps iframes (WCAG 2.1 SC 4.1.2 — iframes had no accessible name): the neighbourhood map names the location, the building map is "Building location map". | PR #192 |
| 2026-06-29 | A11y (mobile) | Labelled the photo-picker preview Pressable (the mobile core-flow entry point) with `accessibilityRole="button"` + a context-aware `accessibilityLabel` so VoiceOver/TalkBack announce it as an actionable button rather than plain text. | PR #193 |
| 2026-06-29 | Store readiness (Track D) | Mobile in-app account deletion (Apple 5.1.1(v) — required, previously missing on mobile): new Bearer-authed `DELETE /api/mobile/account` (user id from validated JWT, `userDelete` rate limit, server-only admin cascade) + `mobile/src/app/settings.tsx` Account screen (legal links via in-app browser, sign out, delete-with-destructive-confirm, success only on real 2xx) wired into the home header. | PR #186 |
| 2026-06-29 | Store readiness (Track D) | Mobile paywall: made Terms/Privacy tappable (in-app `openBrowserAsync`, stays in purchase flow), added a11y role/label/state to close/start-trial/restore, made trial billing copy explicit (charged when trial ends). | PR #187 |
| 2026-06-29 | Correctness (deep audit) | `POST /api/bundles` silently returned 201 with zero items when the items insert failed → later evaluate scored an empty bundle. Now checks the insert error, deletes the orphaned bundle (logs cleanup failure), returns apiError. | PR #185 |
| 2026-06-29 | Tests (Track F2) | New `__tests__/validation/lookups.test.ts` (20) — color/material lookups, hex/rgb parse incl. malformed-hex rejection, wood/metal identification. | PR #183 |
| 2026-06-29 | Tests (Track F2) | New `__tests__/validation/proportion-math.test.ts` (13) — rug-extension penalties, height target/tolerance + category normalization, score floors, rounding. | PR #184 |
| 2026-06-28 | Marketing / launch (Track E8) | Built the pre-launch SITE GATE: `lib/security/site-gate.ts` + wired into `lib/supabase/middleware.ts`. When `SITE_GATE_PASSWORD` is set, non-exempt browser routes redirect to `/waitlist` (API → 503) while marketing/legal/waitlist routes stay open; owner unlocks via `?gate=<pw>` → httpOnly cookie with a SHA-256-derived token (raw pw never stored/committed). Ships inert when the env var is unset. 17 tests. Unblocks pre-launch outreach. E8 ticked. | PR #173 |
| 2026-06-28 | Reliability (ROADMAP hard rule) | Added `signal: AbortSignal.timeout(5000)` to the 2 Google Places fetches in `app/api/places/photo/route.ts` (place-details + photo-media) that had none — the last external fetches missing a timeout. Existing catch returns 502 on timeout. | PR #174 |
| 2026-06-28 | Performance (search hot path) | Parallelized the independent `project` + latest-`diagnosis` fetches (after `room`) in the `/api/search` POST handler via `Promise.all`, saving a DB round trip on a core entry point. Results destructured by position → determinism preserved. | PR #175 |
| 2026-06-28 | Correctness / reliability | Guarded `request.json()` on the 3 remaining core-pipeline routes (`analyze-apartment`, `diagnosis`, `diagnosis/stream`) so a malformed body returns 400 not an unhandled 500. Completes the PR #160 class of fix. | PR #176 |
| 2026-06-28 | Monetization (Track C) | Aligned the mobile client free-save limit (`mobile/src/hooks/use-free-quota.ts`) from 1 → 3 to match the server gate (`FREE_SAVE_LIMIT=3`) and web; mobile free users were paywalled after 1 save (worse free experience). Server stays the real enforcer. | PR #177 |
| 2026-06-28 | Store compliance (Track D2) | Disclosed Vercel Web Analytics (used via `@vercel/analytics`) as a third-party processor in `docs/app-privacy.md` (App Privacy + Play Data Safety tables) and corrected the stale "no third-party SDK" notes — cookieless, no PII, no ad identifiers. Closes an incomplete-disclosure store-review risk. | PR #178 |
| 2026-06-28 | Tests (Track F2) | Added `__tests__/validation/spatial-math.test.ts` (32 assertions) covering `parseDimensions` (unit conversion, separators, dimensionality, regex re-entrancy, null path) + `computeSpatialConstraints` (defaults, clearance pass-through, placement-conflict + natural-pair exemptions) — real branch behavior on a previously-untested math module. | PR #179 |
| 2026-06-28 | Reliability (ROADMAP hard rule) | Added `signal: AbortSignal.timeout(...)` to 3 external fetches that had NONE: Tavily search/extract (15s, sourcing hot path), Resend email send (10s, webhook path), Cloudflare Turnstile siteverify (5s, every public POST). A stalled connection can no longer hold a serverless function open to its full budget; existing retry/fail-open/catch paths absorb the AbortError. Both reviewers APPROVE. | PR #167 |
| 2026-06-28 | Security (G3 — error hygiene) | Closed the remaining client-facing raw-error leaks: SSE `error` events in diagnosis/stream + search/stream (raw err.message → logServerError + generic, event shape unchanged), products/evaluate-set per-item errors, and the Stripe webhook signature error. Also aligned the webhook email site-URL fallback `.ai`→canonical `.com` (matches checkout). Full app/api sweep confirms no client-facing `.message` leaks remain → **G3 ticked**. Both reviewers APPROVE. | PR #168 |
| 2026-06-28 | Security (G5 — CAPTCHA) | Added the Turnstile widget to the signup form (server route already verified the token). Closes the signup half of G5 and a latent breakage (signups would fail once TURNSTILE_SECRET_KEY was set). Mirrors waitlist-form: inert until NEXT_PUBLIC_TURNSTILE_SITE_KEY is set. Both public forms now covered → **G5 ticked** (keys owner-applied). Both reviewers APPROVE. | PR #169 |
| 2026-06-28 | Tests (F2 — critical paths) | +14 route tests for billing/checkout (auth/rate-limit/tier-validation/session-id passthrough/generic-500) and user/delete (auth/rate-limit/503/cascade-keyed-on-session-id/generic-500). Rate limiter mocked for deterministic 429s. Both reviewers APPROVE. | PR #170 |
| 2026-06-28 | Tests (F2/E7 — internal APIs) | +19 route tests for internal growth-metrics (closed-by-default 503, IP rate-limit, Bearer gate, generic 500) and social-queue (shared auth, enqueue validation, media filtering, dedupe pass-through, the `1e500`→Infinity flush-limit guard). Real HMAC token-compare kept live. Both reviewers APPROVE. | PR #171 |
| 2026-06-28 | Security (G3 — error hygiene) | New `lib/utils/api-error.ts` (`apiError`/`logServerError`): logs the full error server-side, returns a generic client message. Applied across ~20 API routes (products, rooms, projects, bundles, saved-designs, mockups, upload, picks, floor-plan, ingest, evaluate, identified-products) so raw Supabase/Postgres error strings (schema/table/column names) no longer leak to clients. PGRST116→404 status + the projects-update field-context log preserved. SSE/webhook deferred. 3 reviewers APPROVE (1 re-review confirmed complete coverage). | PR #164 |
| 2026-06-28 | Tests (F4/F2 — critical paths) | +23 headless route tests for the 3 zero-coverage critical routes: billing/webhook (sig gate, idempotent free→paid welcome + cancel win-back, generic 500), mobile/entitlements (auth/env gating, server-side entitlement resolution), auth/signup (validation, captcha incl. fail-open, enumeration-safety, rate limit, no raw-error leak). All deps mocked; deterministic. 3 reviewers APPROVE. | PR #165 |
| 2026-06-28 | Performance (A3) | Parallelized independent DB writes via `Promise.all` on two core-flow completion hot paths: `search/route.ts` (eval insert + candidate update; session + room updates) and `analyze-apartment/route.ts` (per-room diagnosis insert + room status update). Distinct tables/rows, no data dependency; behaviour-preserving. Both reviewers APPROVE (write-independence verified). | PR #161 |
| 2026-06-28 | Correctness (G2/robustness) | Wrapped unguarded `await request.json()` in try/catch (400 "Invalid JSON", not an unhandled 500) on apartment-research, area-analysis, area-analysis/refine, products/evaluate-set. Mirrors PR #96. Both reviewers APPROVE. | PR #160 |
| 2026-06-28 | A11y (A2 / Track F) | Replaced empty `alt=""` with indexed, descriptive alt text on user room/reference photo thumbnails in 3 image grids (dashboard image manager, room setup grid, reference grid) — fixes a WCAG 1.1.1 failure. Both reviewers APPROVE. | PR #162 |
| 2026-06-28 | Artifact freshness (F5/D3) | Reconciled stale docs: standardized the Privacy/Support/Marketing URLs to the canonical `aptdesignerai.com` (pre-submission-checklist + welcome email; `ai.aptdesigner.app` bundle IDs untouched) and aligned the brand-kit iOS subtitle with the authoritative store-listing. Both reviewers APPROVE. | PR #163 |
| 2026-06-28 | Mobile (B6 — EAS) | `mobile/eas.json` (development/preview/production build profiles, `appVersionSource: remote`, preview+production submit profiles with Apple creds via `$EXPO_*` env — no secrets) + `mobile/app.config.ts` (dynamic config overlaying `app.json` so `extra.eas.projectId` reads from `EAS_PROJECT_ID` env). Closes the "EAS-ready" ticked-but-unbacked gap; **B6 → ticked**, B1 claim now backed. Verified: mobile tsc clean, `expo config` resolves projectId from env, eas.json parses, lint clean. Both reviewers APPROVE. | PR #149 |
| 2026-06-28 | Store readiness (F5/D3) | `docs/store-listing.md`: added Pro Annual ($399/yr, 32% saving, $33.25/mo) to both the Apple + Play pricing blocks — the live tier (Stripe/web/business-case) was invisible in the listing reviewers read. Grounded in the real price; arithmetic verified. Both reviewers APPROVE. | PR #150 |
| 2026-06-28 | Security (G — definer hardening) | Migration `024_harden_handle_new_user_search_path.sql`: `alter function public.handle_new_user() set search_path = ''` — pins the search_path on the SECURITY DEFINER signup-trigger (Supabase "Function Search Path Mutable" lint / privilege-escalation surface). Body already fully-qualifies `public.profiles`, so empty is safe + strictest; idempotent. Human-applied (PENDING_OPS). Both reviewers APPROVE. | PR #151 |
| 2026-06-28 | AI pipeline (correctness) | `lib/ai/deepseek.ts`: wrapped the bare `JSON.parse(tc.function.arguments)` for tool calls in try/catch — malformed model output now fails fast with a named, actionable error instead of an opaque SyntaxError (and is correctly non-retryable). Happy path byte-identical. Both reviewers APPROVE. | PR #152 |
| 2026-06-28 | Performance (A3) | `app/api/products/evaluate/route.ts`: collapsed 3 sequential, mutually-independent context fetches (project, latest diagnosis, sibling rooms) into one `Promise.all`, keyed by index (determinism-safe). ~3× fewer serial DB round-trips on the per-product scoring hot path. Behaviour-preserving. Both reviewers APPROVE. | PR #153 |
| 2026-06-28 | Mobile (billing UX) | `mobile/src/components/paywall-sheet.tsx`: fixed stale fallback paywall pricing ($79.99/$9.99 → real $399/yr $33.25/mo + $49/mo) and replaced the ungrounded "7-day free trial" with "free trial included" (fallback + live sublines). Live path still renders RC's real priceString. Removes a misleading-pricing store-acceptance risk. Both reviewers APPROVE. | PR #154 |
| 2026-06-28 | Growth (E7 — conversion send) | `lib/email/templates/lifecycle.ts` (`buildPaidWelcomeEmail1`) + `lib/email/types.ts` (`paid_welcome_1` stage) + `app/api/billing/webhook/route.ts`: "welcome to Pro" email fires once on a genuine free→paid activation (idempotent new→active guard; renewals/redelivery suppressed; fire-and-forget; dry-run safe). Symmetric to win-back. +3 tests. E7 stays partial (activation/habit triggers + analytics pulls + social live clients remain). Both reviewers APPROVE. | PR #155 |
| 2026-06-27 | Security (G6 — CORS) | `lib/security/cors.ts` + `lib/supabase/middleware.ts`: CORS allowlist (NEXT_PUBLIC_SITE_URL/APP_URL + localhost) reflected only for listed origins — never a wildcard — so disallowed cross-origin browser reads get no ACAO and are blocked; OPTIONS preflight on `/api/*` answered 204 pre-auth; additive (server-to-server callers send no Origin, unaffected). With CSP/HSTS/X-CTO already shipped, **G6 now complete → ticked**. 7 tests. Both reviewers APPROVE. | PR #140 |
| 2026-06-27 | Security (G5 — CAPTCHA, partial) | `lib/security/turnstile.ts` + `components/ui/turnstile.tsx` + waitlist route/form: Cloudflare Turnstile on the public waitlist form. Closed-but-inert (fails open with no secret; widget renders nothing with no site key) — form unchanged until the owner connects Cloudflare; when enabled, no valid token → 400; Cloudflare-unreachable fails open (rate limit still applies). Secret stays server-side. Signup captcha (Supabase-side, owner config) remains → **G5 stays unchecked**. 7 tests. Both reviewers APPROVE. | PR #141 |
| 2026-06-27 | Growth engine (E7.2 — welcome email) | `app/api/waitlist/confirm/route.ts` + `lib/email/templates/waitlist-welcome.ts`: wires the first lifecycle email into a real call site — a one-time welcome after waitlist double-opt-in confirmation. Fires at most once (token cleared on confirm), dry-run until RESEND_API_KEY, never breaks confirmation on send failure. Copy grounded in docs/email-welcome-sequence.md (no invented metrics). 11 tests. Both reviewers APPROVE. | PR #139 |
| 2026-06-27 | A11y (dashboard) | `app/dashboard/page.tsx`: associate three onboarding form controls with their labels (WCAG 1.3.1/4.1.2) — square-footage input, manual building-name input (caption `<p>`→`<label>`), per-room photos textarea — via htmlFor/id. No visual change. Both reviewers APPROVE. | PR #138 |
| 2026-06-27 | Test coverage (F2) | `__tests__/supabase/admin.test.ts`: cover `getAdminClient()` (previously zero tests) — null on missing env, no-persist/no-refresh client construction, singleton memoization, browser-context guard. doMock-after-resetModules for clean per-test isolation. No source change. Both reviewers APPROVE. | PR #137 |
| 2026-06-27 | Correctness (social queue) | `lib/social/queue.ts` + `lib/social/index.ts`: validate the TRIMMED body length (what is stored/sent) against the platform cap, not the raw input — whitespace-padded posts were wrongly rejected and the two paths disagreed on the authoritative length. Regression tests for enqueuePost + publishPost. Both reviewers APPROVE (re-review confirmed the sibling index.ts fix). | PR #136 |
| 2026-06-27 | Growth engine (E7.4 — churn metrics) | `lib/growth/metrics.ts`: added `cancelled_subscribers` (all-time, status=cancelled on pro+pro_annual) and `cancelled_30d` (recent churn, APPROXIMATE — keyed on the cancellation webhook's `updated_at`, no schema/webhook change) to the internal growth-metrics API. Honestly disclosed as approximate in the JSDoc, the response `notes`, and CONNECT.md Step 2. Test fake reworked to guard the status + updated_at filters. Both reviewers APPROVE. | PR #125 |
| 2026-06-27 | Security (G4 — user enumeration) | `lib/auth/signup-errors.ts` + `app/(auth)/signup/page.tsx`: signup no longer leaks "user already registered" — an already-registered address now shows the same neutral "check your email" screen a new signup gets (indistinguishable); only a genuinely new user fires `signup_complete`. 6 tests. Login lockout (other half of G4) deferred. Both reviewers APPROVE. | PR #124 |
| 2026-06-27 | Growth engine (E7.3 — social publishing queue) | `lib/social/*` (provider abstraction mirroring lib/email, dry-run by default) + `supabase/migrations/023_social_post_queue.sql` (admin-only, RLS no policy) + `app/api/internal/social-queue` (GET status / POST enqueue\|flush, INTERNAL_METRICS_TOKEN auth) + CONNECT.md Step 4. Status-guarded claim prevents double-post; nothing posts publicly without a per-channel credential. 15 tests. Reviewers: NaN limit guard + flush tests + honest docs added in fix cycle. | PR #123 |
| 2026-06-27 | Growth engine (E7.1 — waitlist double opt-in) | `supabase/migrations/022_waitlist_double_opt_in.sql` + `app/api/waitlist/*` + `/waitlist/confirmed` page + confirmation email (lib/email): a sign-up is stored PENDING with an unguessable single-use token and only counted once the emailed link is clicked — makes the long-claimed `double_opt_in: true` real. 10 tests. Reviewers: 64-char token regex + 5-min resend throttle + scoped public path added in fix cycle. | PR #122 |
| 2026-06-27 | Growth engine (E7.5 — owner connect runbook) | `docs/growth/CONNECT.md`: ~20-min owner setup runbook (capability status table — email/metrics/spend ceiling Built, social publishing honestly marked NOT built; step-by-step Resend + metrics-token + spend-cap connect; curl verify steps; human-only boundary). `.env.example`: documented `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `GROWTH_EMAIL_DRY_RUN`, `INTERNAL_METRICS_TOKEN`, `DAILY_PAID_CALL_LIMIT`. Two reviewers verified every documented var/behavior/field against the merged code. | PR #120 |
| 2026-06-27 | Security (G7 — paid-API spend ceiling) | `lib/utils/spend-limiter.ts`: per-user/UTC-day circuit breaker (`DAILY_PAID_CALL_LIMIT`, default 60) summing paid calls across ALL expensive endpoints — defense-in-depth over the per-route rate limiter. Wired into 12 paid routes (Gemini/Tavily/Browserbase/Maps) after each rate-limit check; excludes cheap places/photo + revenue billing/checkout. Discriminated-union result so the 429 helper can't be misused. 10 tests. Reviewers: discriminated union + zero/negative env tests added in fix cycle. | PR #119 |
| 2026-06-27 | Growth engine (E7.4 — funnel-metrics pull API) | `lib/growth/metrics.ts` + `app/api/internal/growth-metrics/route.ts`: internal read API returning REAL funnel counts (waitlist total + 7d, active subscribers across pro+pro_annual excluding one-time apartment tier, annual subscribers). Token-gated (`INTERNAL_METRICS_TOKEN`, HMAC timing-safe, 503-until-configured), IP rate-limited, middleware bypass. 8 tests. Reviewers: pro_annual + apartment-tier fix, length-safe token, status-filter test guard. | PR #118 |
| 2026-06-27 | Growth engine (E7.2 — email provider abstraction) | `lib/email/`: Resend REST provider (no SDK) behind one abstraction, dry-run default until `RESEND_API_KEY` supplied; `EmailStage` union mirrors the email-lifecycle docs; validation never throws; provider-error logging drops PII. 12 tests. Reviewers: dry-run consistency + replyTo validation + bounded logging in fix cycle. | PR #117 |
| 2026-06-27 | Security (G6 — CSP header) | `next.config.ts`: added `Content-Security-Policy` header covering all known origins — script-src (Stripe, Google Maps JS API, Vercel Analytics); img-src (Supabase storage, Google user photos, Maps tiles); connect-src (Supabase HTTPS + Realtime WebSocket, Gemini, Maps/Places, Stripe, RevenueCat); frame-src (Stripe + Google Maps embed iframes); worker-src self; object-src none; base-uri self; form-action self. Two reviewers caught missing `https://www.google.com` in frame-src (Maps iframes), `https://maps.googleapis.com` in script-src (Maps JS API), and missing `worker-src 'self'` — all fixed before merge. | PR #114 |
| 2026-06-27 | Artifacts (GROWTH_STATUS + preflight guard) | `docs/growth/GROWTH_STATUS.md`: corrected `engine_built: false` → `engine_built: true` (all E1-E6 track PRs merged; stale value was artifact from before those PRs). Updated stale prose example on line 62 to match. `scripts/preflight.sh`: added `isinstance(d.get("engine_built"), bool)` validation to GROWTH_STATUS parse block — rejects strings/ints that YAML would silently accept. | PR #113 |
| 2026-06-27 | A11y (icon-only buttons in list context) | `app/saved/page.tsx`: delete button (Trash2 icon) gets `aria-label={\`Delete ${design.title}\`}` — unique per card per WCAG 2.1 SC 4.1.2. `app/projects/[projectId]/rooms/[roomId]/mockups/page.tsx`: download button gets `aria-label={\`Download mockup from ${new Date(mockup.created_at).toLocaleDateString()}\`}` — unique per mockup card. Reviewer 1 correctly requested context-specific labels over generic ones; applied before merge. | PR #112 |
| 2026-06-27 | Security (G1/G2/G3 — rate limiting + error hygiene) | `lib/utils/rate-limiter.ts`: 7 new RATE_LIMITS constants for previously-unprotected expensive routes (`analyzeApartment` 5/hr, `apartmentResearch` 3/hr, `computerUseVerify` 2/hr, `billingCheckout` 10/hr, `userDelete` 3/day, `areaAnalysisRefineChat` 20/min, `areaAnalysisRefineFull` 5/5min). All 7 route handlers updated with per-user 429 + Retry-After guards after auth: `analyze-apartment`, `apartment-research`, `computer-use/product-verify`, `billing/checkout`, `user/delete`, `area-analysis/refine`, `area-analysis/refine-chat`. Error hygiene: 5 routes replaced raw LLM/Stripe/Supabase error messages in HTTP responses with generic client-facing strings (Stripe error, Supabase insert error, analysis pipeline error). Both reviewers flagged `user/delete` missing Retry-After and 3 remaining error leaks in refine/refine-chat; all fixed before merge. | PR #111 |
| 2026-06-26 | Docs (business case) | Pro Annual tier ($399/yr) added to `docs/BUSINESS_CASE.md` pricing table; ARR scenarios recomputed (conservative $46.2K, base $122.9K, optimistic $276.8K +23% across all). Pro Annual LTV comparison section added (Annual LTV $1,117 vs Monthly $490, +128%). YAML summary block updated `as_of: 2026-06-26`. | PR #107 |
| 2026-06-26 | Quality (F6 — preflight) | `scripts/preflight.sh`: 5-gate mechanical readiness script. Gate 1 runs full CI (tsc + tests + determinism + build + mobile tsc); Gate 2 checks 22 required files exist; Gate 3 counts unchecked DoD boxes in ROADMAP.md; Gate 4 runs 10 critical-path grep checks (`checkout.session.completed` in `lib/billing/stripe.ts`, `pro_annual`, `REVENUECAT_SECRET_KEY`, etc.); Gate 5 validates BUSINESS_CASE_SUMMARY YAML via Python. Exits non-zero (correct) while any DoD box is unchecked. | PR #106 |
| 2026-06-26 | Quality (F3 — eval suite) | `evals/__tests__/area-analysis.eval.test.ts`: live eval for area-analysis Pass A pipeline stage using `geminiProvider.chat()` directly with same model/prompt as `app/api/mobile/analyze/route.ts`. Two tests: quality assertions on warm living room image (all 8 fields present, palette ≥3 entries with warm term) and JSON mode regression guard. Completes the 5-eval coverage set (diagnosis + sourcing + grounding + refine + area-analysis). | PR #105 |
| 2026-06-26 | Monetization (C1 — annual billing) | Added `pro_annual` tier ($399/yr, "Save 32%") to Stripe billing: `BillingTier` type, `STRIPE_PRICE_IDS.pro_annual` (reads `STRIPE_PRICE_ID_PRO_ANNUAL` from env), `WebBillingStatus.tier` and cast updated to include `"pro_annual"`, `supabase/migrations/021_stripe_customers_annual_tier.sql` extends CHECK constraint. Upgrade page + pricing page show annual option. PENDING_OPS.md documents required env var and migration. | PR #98 |
| 2026-06-26 | Quality (F track — determinism) | `__tests__/integration/scoring-pipeline.test.ts`: replaced `Math.random()` calls with deterministic fixed values (`8.5` flat for high-score loop; `6.5 + (i % 5) * 0.1` cycling 6.5–6.9 for mid-score loop). Fixes AGENTS.md contract violation — `check:determinism` was untrustworthy while test inputs used non-seeded randomness. | PR #95 |
| 2026-06-26 | Security / Robustness (D track) | Wrapped `request.json()` in try/catch on all 14 unguarded POST/PATCH/DELETE API routes: `bundles`, `bundles/evaluate`, `products`, `products/[id]`, `products/evaluate`, `products/ingest`, `projects`, `projects/[id]`, `projects/[id]/floor-plan`, `rooms`, `rooms/[id]`, `rooms/[id]/images` (×2), `saved-designs`, `search`. Previously returned 500 on malformed JSON; now correctly returns 400. | PR #96 |
| 2026-06-26 | Performance / Safety (F track) | `app/api/identified-products/search/route.ts`: added `.order("brand").order("model").order("id").limit(500)` to the `product_image_embeddings` query. Previously an unbounded full table scan on every typeahead keystroke; 500-row limit with composite order makes the window bounded and fully deterministic. | PR #97 |
| 2026-06-26 | A11y / D track (store readiness) | Added `aria-label` to 5 icon-only `<Button variant="ghost" size="icon">` elements on 4 core pages (`dashboard`, `picks`, `saved/[id]`, `saved`). Fixes WCAG 2.1 SC 4.1.2 / axe `button-name` Level A violation — icon-only buttons had no accessible name. Required for App Store/Play Store accessibility review. | PR #93 |
| 2026-06-26 | Compliance / D track (store readiness) | `docs/app-privacy.md`: added Stripe, Google Maps/Places API, Browserbase, and DeepSeek to Apple App Privacy and Google Play Data Safety third-party processor tables. All four are active in the codebase; omitting them from the privacy disclosure is a direct App Store rejection criterion. | PR #94 |
| 2026-06-25 | Track A6 (Computer-Use security + quality) | `lib/agents/computer-use/agent-loop.ts` + `lib/ai/models.ts`: upgraded `MODELS.computerUse` to `"gemini-3.5-flash"`, rewrote agent loop for the GA built-in tool API (`computerUse: { environment: "ENVIRONMENT_BROWSER" }` replaces standalone model), enabled injection-safety safeguards. Provider-floors test updated. Clears A6 on both reliability (GA model) and security (adversarial training + auto-stop on injection). | PR #91 |
| 2026-06-25 | Quality (F track — critical-path coverage) | Added coverage for 4 previously-zero-coverage modules: `lib/billing/` (Stripe session creation, webhook parsing, tier validation), `lib/entitlements/web.ts` (fail-open behavior, tier checks), `lib/auth/ownership.ts` (room ownership guard), `lib/agents/computer-use/` (injection-safety invariants, model-pin test). 952 tests total (+76 from Run 27 baseline of 876). | PR #90 |
| 2026-06-25 | ROADMAP | ROADMAP.md: added A6 Computer-Use upgrade item as a phased Track A requirement. | PR #89 |
| 2026-06-25 | Quality (Track F4) | Playwright E2E + accessibility gate: `playwright.config.ts` (Chromium at `/opt/pw-browsers`; dev server auto-start; 1 retry in CI), `e2e/public-pages.spec.ts` (smoke tests — all public marketing pages must load and have a visible heading), `e2e/a11y.spec.ts` (axe-core WCAG 2.x scan of every public page; fails on critical or serious violations with human-readable node HTML summary). `package.json`: `@playwright/test@^1.61.1` + `@axe-core/playwright@^4.12.1` added as devDeps; `test:e2e` and `test:e2e:a11y` scripts. CI wiring recorded in PENDING_OPS.md (cannot write `.github/workflows/` in headless run). Advances F4. | PR #87 |
| 2026-06-25 | Docs | Replace boilerplate `create-next-app` README with AptDesignerAI product description: what it does, stack table, local dev commands (web + mobile), env var reference, pointers to ARCHITECTURE.md / AGENTS.md. | PR #86 |
| 2026-06-25 | A11y | Account deletion confirmation input was unaccessible: `<label>` had no `htmlFor` and `<input>` had no `id`. Screen readers announced the field without its label; clicking the label did not move focus. Fixed with `id="confirm-delete"` + `htmlFor="confirm-delete"`. WCAG 1.3.1 + 4.1.2. | PR #85 |
| 2026-06-25 | Security (RLS fix — migration 020) | `supabase/migrations/020_fix_saved_designs_rls_column_filter.sql`: migration 019's JWT-claim policy broke all share links because `app/api/shared/[token]/route.ts` uses a PostgREST column filter (`.eq("share_token", token)`) — not a JWT claim. Migration 020 drops the JWT-claim policy and replaces it with `USING (is_public = true AND share_token IS NOT NULL)`. The UNIQUE constraint on `share_token` (migration 015) ensures enumeration is still impossible. HUMAN-APPLIED; see PENDING_OPS.md for apply+verify instructions. | PR #84 |
| 2026-06-25 | Security (middleware public paths) | `lib/supabase/middleware.ts`: five classes of routes were missing from the public-path allowlist, causing them to be blocked behind cookie-auth. Fixed: (1) `/api/billing/webhook` — Stripe POSTs from its servers with no session cookie; route verifies `Stripe-Signature` itself; (2) `/api/shared/*` prefix — public share design links; route enforces `share_token + is_public` via RLS; (3) `/api/mobile/*` prefix — mobile clients send `Authorization: Bearer` token; each route calls `supabase.auth.getUser(token)` directly; (4) marketing pages `/pricing /faq /privacy /terms /support` added to `PUBLIC_PATHS`; (5) `/guides` with prefix match added via `PUBLIC_PATH_PREFIXES` to cover sub-routes. Two independent reviewers both raised issues (mobile API bypass, /guides sub-routes) that were incorporated before merge. | PR #83 |
| 2026-06-25 | Security (RLS hardening) | `supabase/migrations/019_fix_saved_designs_rls.sql`: drops the overly-permissive migration-015 policy that allowed unauthenticated PostgREST callers to enumerate all `is_public=true` rows without the share token. Replacement policy requires `share_token = current_setting('request.jwt.claims', true)::json->>'share_token'` — a row is only returned when the caller proves knowledge of the token in their JWT. Two variants documented (JWT-claim vs column-filter). HUMAN-APPLIED; see PENDING_OPS.md for apply+verify instructions. | PR #78 |
| 2026-06-25 | Quality (Track F2) | `vitest.config.ts`: adds `coverage.thresholds` block (statements=25, branches=19, functions=30, lines=25). Current coverage ≈36/28/40/36 clears all thresholds with headroom. Gate prevents accidental coverage regressions without requiring immediate improvement. Advances F2 checkbox. | PR #77 |
| 2026-06-25 | Quality (Track F1) | Drive `npx eslint .` to zero warnings — 29 files, 55 additions/34 deletions. Global `@typescript-eslint/no-unused-vars` config with `argsIgnorePattern`/`varsIgnorePattern: "^_"`. Mobile `eslint.config.mjs` syntax fix (named export). 24 `@next/next/no-img-element` suppressions for Supabase CDN, external retailer, and `blob:` URLs. Dead code removed: `HISTORY_LIMIT`, `mathItemMap`+`mathItem`, `ALL_PRICE_TIERS`, 8 unused imports. `react-hooks/exhaustive-deps` fix in `dashboard/page.tsx` (missing `locationCoords?.lat/lng`). Two independent reviewers APPROVE. Advances F1 checkbox. | PR #76 |
| 2026-06-25 | Eval suite (Track A5) | Live eval tests for sourcing relevance and photo-grounding pipeline stages. `evals/__tests__/sourcing.eval.test.ts`: exercises `scoreProduct()` with warm beige linen sofa + oak/brass coffee table against warm Scandinavian context; asserts `verdict ≠ "no"` and `final_item_score ≥ 6`. `evals/__tests__/grounding.eval.test.ts`: exercises `verifyWhatShouldGoAgainstPhotos()` — positive (sofa kept) and contrastive (gas range stove dropped, sofa control kept). Both gate on `RUN_EVALS=1`; 876 tests passing, 6 skipped. Reviewer A REQUEST_CHANGES (silent-pass guard + threshold at `"no"` boundary) — both fixed before commit. Reviewer B APPROVE. CI job for `RUN_EVALS=1` recorded in PENDING_OPS.md (cannot write `.github/workflows/` in headless run). | PR #73 |
| 2026-06-25 | Eval suite (Track A5) | Live eval tests for room-diagnosis pipeline. New gold fixture `living-room-warm-sofa.json` (broad 10-term palette OR gate). Updated `studio-living-keep-brass-lamp.json`: real Unsplash URL, removed `mustKeep` (wrong observational signal) and `minValidationConfidence` (dishonest — diagnosis produces no confidence score). New `evals/__tests__/diagnosis.eval.test.ts`: calls `runRoomDiagnosis` end-to-end with `flattenDiagnosisOutput()` adapter; per-test `it.skipIf(!evalsEnabled())`; `mustNotDrop`-only for keepItems compliance. Fixed `refine.eval.test.ts` confidence-threshold test to use inline fixture (decoupled from gold case). Two reviewers APPROVE. 876 tests passing. | PR #68 |
| 2026-06-24 | Mobile Share (Track E6/B5) | `POST /api/mobile/saved-designs/[id]/share`: Bearer-JWT auth, idempotent share token generation (randomBytes(16)), rate-limited (20 req/min), 0-row UPDATE guard via `.select("id")`. `saved.tsx`: native Share sheet from each DesignCard using React Native's built-in Share API with design title in copy. No new deps. Reviewer A (round 1) caught missing rate limit, missing 0-row UPDATE detection, unused `title` param — all fixed. Both reviewers APPROVE. | PR #65 |
| 2026-06-24 | Marketing (Track E6) | `docs/email-lifecycle.md`: 6 post-signup lifecycle sequences — A-series (activation, Days 0/3/7), B-series (habit formation after first analysis), C-series (upgrade conversion on paywall hit), D-series (paid re-engagement), E-series (win-back churned Pro), F-series (share trigger 1h after first public share). All event-driven; suppression rules included. Two reviewers APPROVE. | PR #64 |
| 2026-06-24 | Marketing (Track E6) | `docs/press-kit.md`: Product Hunt launch package (58-char tagline, 203-char description, first maker comment), email templates for design bloggers + tech journalists, app fact sheet (verified claims only), boilerplate in 25/50/100-word variants, media asset directory, launch day checklist, 3 A/B landing page headline variants with testing guidance. Two reviewers APPROVE. | PR #63 |
| 2026-06-24 | Marketing (Track E6) | `docs/content-calendar.md`: 30-day launch content calendar (D-7 to D+30) covering X/Twitter, Instagram (stories/reels/carousels), TikTok scripts, Reddit AMAs, and email triggers. Builds on existing social-drafts.md via cross-references. Ongoing weekly cadence table. Reviewer A caught imprecise sofa-scale tip in Day -2 TikTok — tightened to match Day 21 phrasing. Both reviewers APPROVE. | PR #62 |
| 2026-06-24 | Business Case (Definition of Done) | `docs/BUSINESS_CASE.md`: bottoms-up financial model with 3 scenarios — Conservative ($38K ARR), Base ($100K ARR), Optimistic ($225K ARR). Blended CPI ($4.70×60%+$3.70×40%=$4.30) derived inline. 97–99% gross margin confirmed ($0.0006/analysis on Gemini 2.5 Flash Lite). Formula factors labeled (0.25=Day-30 retention, 0.40=Pro mix). Reviewer A required 2 cycles: round 1 flagged CPI mismatch, unlabeled factors, 50% organic justification, Scenario B sensitivity arithmetic error — all fixed. Reviewer B APPROVE after first pass. | PR #61 |
| 2026-06-24 | Marketing / Analytics (Track E5) | Analytics scaffolding: installed `@vercel/analytics`, added `<Analytics />` to root layout for automatic page-view capture across all routes. `lib/analytics.ts`: typed `FunnelEvent` union + SSR-safe `trackEvent()` wrapper. 7 funnel events wired at correct call sites: `signup_complete` (signup form), `analysis_started`/`analysis_complete` (dashboard handleAnalyze), `design_saved` (focus page handleSaveDesign), `upgrade_page_view` (client island on upgrade page), `checkout_started` (checkout button before Stripe redirect), `checkout_complete` (client island on success page). `docs/analytics.md`: full event reference table. Reviewer A approved (no PII, SSR guard correct, event timing correct). Reviewer B initially blocked (4 events dead-code, no docs) — both fixed before final approval. | PR #58 |
| 2026-06-24 | Native Mobile (Track B3) | Push notifications + deep links + mobile ESLint gate fix: `expo-notifications@56.0.18` + app.json plugin (accent `#b4501e`, default channel). `use-push-notifications.ts`: Device.isDevice guard, Android channel setup, idempotent permission request, dual-path EAS project ID resolution, token persisted to AsyncStorage. `_layout.tsx`: `usePushNotifications(session?.user.id)` wired. `+not-found.tsx`: expo-router fallback for unknown deep-link routes. `app.json`: `ios.associatedDomains: ["applinks:aptdesignerai.ai"]` for iOS Universal Links (AASA file is human-applied). Android intentFilters deferred (custom scheme `aptdesignerai://` sufficient). `eslint.config.mjs`: `flat` → `flat.js` (fixes broken ESM directory import); disables `react-hooks/set-state-in-effect` (4 intentional RN init patterns). Reviewer A caught token persistence missing, Device.isDevice guard missing, Android intentFilters too broad, URL injection comment needed — all fixed. | PR #56 |
| 2026-06-24 | Monetization / Web (Track C1) | Web entitlement gate: `FREE_SAVE_LIMIT_WEB = 3` constant added to `lib/entitlements/web.ts`. `app/api/saved-designs/route.ts` POST path: count existing saves, call `hasProEntitlementWeb()` on breach, return 403 `{subscription_required: true}`. Re-saves of already-saved rooms (UPDATE path) always allowed. Soft/best-effort limit — count+insert race at boundary documented in comment (same semantics as mobile gate, PR #43). Reviewer A caught race; addressed by documenting as intentional soft cap. Reviewer B approved. | PR #52 |
| 2026-06-24 | Store-readiness / Stability (Track D4) | Three D4 stability additions: `app/global-error.tsx` root-layout fallback error boundary (`<html>`+`<body>`, plain inline styles — Tailwind unavailable when root layout crashes), typed props with `error.digest`. `app/projects/[projectId]/rooms/[roomId]/focus/error.tsx` scoped boundary for focus page (highest-risk route — background vision, search, mockup streams run concurrently). `docs/pre-submission-checklist.md`: 10-section human-runnable checklist (core journey, required URLs, permission strings, debug content, billing, store assets, privacy, migrations, EAS build, Human Core). Reviewer A mis-reviewed pre-existing `app/error.tsx` instead of the new file (false-positive flags); actual file correct. Reviewer B approved. | PR #53 |
| 2026-06-24 | Marketing / SEO (Track E3) | SEO guides hub + 3 long-form design articles: `app/guides/page.tsx` hub with warm-editorial card layout. `app/guides/color-palette-guide/page.tsx`: 5 sections on light direction, fixed finishes, 60-30-10, colour at scale, how AI reads palettes (5 min). `app/guides/ai-vs-professional-design/page.tsx`: 4 sections comparing AI vs professional designers — honest, balanced take (4 min). `app/guides/material-coherence/page.tsx`: 5 sections on warm/cool temperature, wood species (max 2), metal finish families, soft/hard balance, AI scoring (5 min). FAQ: 2 new Pricing items (commercial scope, Apartment vs Pro). Support: "Design guides" quick-link card. Reviewer A caught 1 unescaped apostrophe (`AI's` → `AI&rsquo;s`); fixed before push. | PR #54 |
| 2026-06-24 | Monetization / Web (Track C1) | Stripe web billing: `supabase/migrations/018_stripe_customers.sql` (RLS — authenticated users SELECT own row only; writes service-role only). `lib/billing/stripe.ts` — `createCheckoutSession` (one-time/subscription mode via tier), `constructWebhookEvent`, `extractBillingInfoFromEvent` (checkout.session.completed + subscription.updated/deleted; Stripe API version 2026-05-27.dahlia). `lib/entitlements/web.ts` — `hasProEntitlementWeb(userId)` via Supabase admin client; fail-open on missing credentials. `POST /api/billing/checkout` — creates Stripe session for authed user (uses `NEXT_PUBLIC_APP_URL` for redirects — not attacker-controllable Origin header). `POST /api/billing/webhook` — verifies Stripe signature, upserts stripe_customers. `/billing/upgrade`, `/billing/checkout-success`, `/billing/checkout-cancel` pages. Pricing page paid-tier CTAs wired to `/billing/upgrade?tier=...`. Reviewer A caught Origin-header open-redirect (→ fixed to NEXT_PUBLIC_APP_URL) and `billing_cycle_anchor` semantically wrong for period_end (→ null; status is primary gate). | PR #50 |
| 2026-06-24 | Store-readiness (Track E3) | `/support` page: server component fixes the 404 that App Store listing links to. MarketingHeader + MarketingFooter, email contact card (`hello@aptdesigner.app`), 4 topic quick-links, account deletion note (App Store guideline). Metadata: `title: "Support — AptDesigner"`. | PR #48 |
| 2026-06-24 | Marketing (Track E4) | `docs/email-welcome-sequence.md`: 4-email waitlist nurture sequence (Day 0 value prop, Day 3 educational, Day 7 preview + EARLY30 hint, Launch day live links). `docs/social-drafts.md`: X/Twitter 6-tweet thread + 2 standalone, Instagram 3 carousels, TikTok 2 video scripts, Reddit 3 community posts. All content grounded in real app features; no invented metrics or fake testimonials. | PR #49 |
| 2026-06-24 | Native Mobile (Track B4) | Design-bar polish — View-based indicators in `results.tsx`: 6 px filled dots replace `✓`/`✕` Unicode glyphs (accent-warm for keep, destructive for remove). Rotated border chevron in `room-type.tsx` replaces `›` ThemedText. Both eliminate iOS emoji ambiguity per VISION.md bar. | PR #46 |
| 2026-06-24 | Native Mobile (Track B4) | Replace Expo boilerplate Explore tab with Design Principles educational content: 5 Collapsibles (Colour & natural light, Proportion & furniture scale, Layering materials & textures, Negative space, Cohesion across rooms). Removed all template-only imports. Content grounded in how the AI pipeline analyses rooms. | PR #47 |
| 2026-06-24 | Brand (Track E2) | Brand kit: `docs/brand-kit.md` (colour palette light/dark tokens, typography scale, spacing tokens matching theme.ts, brand voice Do/Don't, app icon export sizes, social/OG dimensions, "what NOT to do" list) + `public/wordmark.svg` (AptDesigner+AI tspan wordmark, #141211 text / #b4501e accent, no fragile x-offset). Two-reviewer cycle: Reviewer A caught spacing table off-by-one (three=12→16 etc), wrong dark accentForeground (#faf9f7→#141211), duplicate OG row (630 vs 628), SVG x=242 hardcode. Reviewer B caught spacing table and accentForeground. All fixed before merge. | PR #44 (merged) |
| 2026-06-24 | Monetization / Server (Track C4) | Server-side entitlement gate: `lib/entitlements/server.ts` (`hasProEntitlement()` calls RC REST API with `REVENUECAT_SECRET_KEY`, 5s timeout, fail-open on outage/missing key, 404→false). `/api/mobile/entitlements` upgraded from `tier: "free"` stub to real RC check. `/api/mobile/saved-designs` gates at `FREE_SAVE_LIMIT = 3` server-side (count via authed Supabase client, RLS enforced). Two-reviewer cycle: Reviewer A caught `X-Platform: "stripe"` (wrong for mobile subscribers — removed) and `!RC_SECRET_KEY → return false` (silently blocks paying subscribers on misconfiguration — changed to fail-open + console.error). All fixed before merge. | PR #43 (merged) |
| 2026-06-24 | Monetization / Mobile (Track C2/C3) | RevenueCat SDK integration: `mobile/src/lib/rc-init.ts` (shared singleton `initRC()` with single `_configured` flag — prevents double-configure). `_layout.tsx` configures RC on mount + syncs identity (`Purchases.logIn/logOut` with Supabase user UUID). `use-entitlements.ts` hook exposes `isPro` / `refresh()`. `paywall-sheet.tsx` upgraded from stub to live RC: fetches Offerings, selectable pricing cards, `purchasePackage`, `restorePurchases`, user-cancel detection (`{userCancelled: true}`), `purchasing` state disables CTAs. `results.tsx`: `canSave = isPro || freeQuotaOk`, `refreshEntitlements()` on purchase success. Two-reviewer cycle: Reviewer A caught double-configure bug (two independent module-level flags) — fixed by extracting shared singleton. | PR #42 (merged) |
| 2026-06-24 | Native Mobile (Track B2) | B2 write path: `POST /api/mobile/saved-designs` (new route) — Bearer-JWT auth via `supabase.auth.getUser`, room_type allowlist + analysis shape validation + thumbnail_url SSRF guard (https on own Supabase host), 10/min rate limit, insert via authed client with Bearer in global.headers (RLS-safe), title auto-derived `"{style_name} · {Room Label}"`, snapshot stored as `{ mobile_analysis, metadata: { source: "mobile" } }`. `results.tsx`: stores `publicUrl` in state, `saveDesign()` hook, `SaveState` machine (idle→saving→saved|save_error), Save Design button (primary) + Back to Home (bordered secondary). No new LLM calls; 876 web tests passing. | PR #37 (auto-merge enabled) |
| 2026-06-24 | Native Mobile (Track B2) / CI gate | PR #35 mobile CI gate fix: `SkeletonCard` + `DesignCard` prop type changed from `(typeof Colors)['light']` (single literal) to `(typeof Colors)[keyof typeof Colors]` (correct union) — `Colors[scheme]` returns `Colors['light'] | Colors['dark']` which is not assignable to the single literal type. Also resolved `run16/bookkeeping` (PR #36) merge conflict with PR #34 (both prepend to IMPROVEMENT_LOG + loop-memory) by rebasing onto current default branch. | PR #35 unblocked; PR #36 rebased and re-triggered |
| 2026-06-24 | Native Mobile (Track B2) | Saved designs screen — replaced static stub with real Supabase-backed data fetch. `use-saved-designs.ts` hook: `tick`-based reload, session guard via `getSession()`, `cancelled` flag in IIFE prevents setState after unmount, `room_type: string | null` (schema-accurate). `saved.tsx`: 3 skeleton card placeholders (ROADMAP "skeletons" req), error + retry state, empty state, `DesignCard` with expo-image thumbnail + room-type badge + relative time, no emoji, warm-editorial design tokens. Two-reviewer cycle: Reviewer A caught auth race + room_type nullability + relativeTime NaN + setState-after-unmount — all fixed. | PR #35 (merged) |
| 2026-06-24 | Native Mobile (Track B2) / CI gate | Mobile CI gate fix: added `"defaultSemiBold"` variant to `ThemedTextProps` union + `StyleSheet` entry (16px / lineHeight 24 / fontWeight '600'). Unblocked failing mobile `tsc --noEmit` gate on PR #33 (B2 mobile UX flow) and PR #34 (run 15 bookkeeping) — both had inherited the same bug. Reviewer A caught `fontWeight: 600` (numeric) → corrected to `'600'` (string) to match codebase convention. | PRs #33 and #34 unblocked (merged) |
| 2026-06-24 | Native Mobile (Track B2) | Mobile UX flow: room type picker screen (6 options), photo.tsx routes to room-type instead of results, results.tsx full rewrite — uploads image to Supabase Storage via FormData REST (correct MIME type per extension), calls POST /api/mobile/analyze, stage machine (uploading → analyzing → done/error), renders all 8 AI analysis fields (style_name, summary, design_direction, palette chips, materials+textures chips, what_works, what_should_go). Back-nav data loss fixed (peek not consume). Retry + "Pick a Photo" error paths. PENDING_OPS.md updated with EXPO_PUBLIC_API_URL and room-photos bucket setup. Two independent reviewers, 2-cycle review. | PR #33 (auto-merge enabled) |
| 2026-06-24 | Native Mobile / Backend (Track B2) | POST /api/mobile/analyze: mobile-specific room analysis endpoint. Bearer JWT auth (supabase.auth.getUser — no cookies), SSRF guard (image_url must be https on own Supabase Storage host), room_type allowlist (10 types), try/catch on request.json + withCostLedger, array field validation on AI response. Runs Pass A room understanding (summary, style_name, design_direction, palette, materials, textures, what_works, what_should_go) via geminiProvider with thinkingFor("area_analysis") + selectModel("area_analysis") + DETERMINISTIC_SEED + withCostLedger + recordUsage. Rate limited to 3 req/5 min per user. Two independent reviewers, 2-cycle review (Reviewer A caught SSRF + 4 other issues). | PR #32 (auto-merge enabled) |
| 2026-06-24 | Native Mobile (Track B2) | Supabase auth foundation: login + signup screens (warm-editorial design system), `useSession` hook using `onAuthStateChange`-only pattern (eliminates getSession race condition), auth gate in root layout (loading→null, session→AppTabs, no session→Login/Signup), sign-out in home header. AsyncStorage for session persistence (SecureStore 2048-byte limit exceeded by Supabase sessions). Reviewers caught 3 bugs (getSession race, auto-confirm signup success gate, missing sign-out path) — all fixed before merge. | PR #28 (auto-merge enabled) |
| 2026-06-24 | Native Mobile / Store-readiness (Track B3/D3) | app.json brand config: fixed all Expo template defaults blocking store submission (name/slug/scheme "mobile"→"aptdesignerai", bundleIdentifier + android.package = "ai.aptdesigner.app", removed invalid ios.icon path, supportsTablet: true, warm-editorial splash colors replacing Expo blue). AnimatedSplashOverlay: removed hardcoded `#208AEF`, added `useColorScheme()` + inline backgroundColor (#faf9f7 light / #141211 dark). | PR #29 (auto-merge enabled) |
| 2026-06-24 | Store-readiness (Track D2/D3) | Staged App Privacy labels (Apple App Privacy + Google Play Data Safety) and store listing copy with ASO. Third-party disclosures: Google Gemini API, Supabase, Tavily Search API (product search queries — no PII). Pricing copy corrected to 3-tier model (Explore free / Apartment $29 one-time / Pro $49/month). ASO competition estimates labeled as unverified. Reviewer caught Tavily omission + pricing mismatch + unqualified ASO claims — all fixed before merge. | PR #30 (auto-merge enabled) |
| 2026-06-24 | Native Mobile (Track B2) | Real photo selection via expo-image-picker: gallery + camera picker, live 4:3 preview, `canAskAgain` permission handling with `Linking.openSettings()` on permanent denial, Android `getPendingResultAsync` recovery on mount, `mediaTypes` restricted to images (prevents silent video-URI failure). `photo-session.ts` module-level store avoids `content://` URI encoding corruption through expo-router params. Two independent reviewers, 2-cycle review. TypeScript clean; 876 tests passing. | PR #26 (auto-merge enabled) |
| 2026-06-24 | Store-readiness / Compliance (Track D1) | In-app account deletion for Apple 5.1.1(v): `DELETE /api/user/delete` uses service-role admin client to cascade-delete the auth user (profiles → projects → rooms → saved_designs all ON DELETE CASCADE). `/account` page with two-step typed confirmation ("delete my account"). "Account settings" link in desktop dropdown + mobile drawer. Zero new LLM calls; 876 tests passing. | PR #23 (pending CI) |
| 2026-06-24 | Marketing engine (Track E1) | Waitlist landing page for iOS/Android launch: `/waitlist` server component (MarketingHeader/Footer, hero, 4-perk cards, bottom CTA), `WaitlistForm` client component (5 UX states, no redirect), `POST /api/waitlist` (email validation + 254-char cap, IP rate-limit 5 req/15 min, admin client insert, friendly duplicate handling), `supabase/migrations/017_waitlist.sql` (UNIQUE email, RLS enabled/no policy). Middleware exemptions added for both routes. | PR #22 (pending CI) |
| 2026-06-24 | Native Mobile (Track B1) | Scaffold Expo app under `/mobile`: Expo 56 + expo-router navigation, design system ported (warm-editorial palette + light/dark modes), core journey screens (Dashboard, Photo, Results, Saved) as stubs ready for B2 integration. Uses native React Native components (Pressable, ScrollView, SafeAreaView), NOT a thin web wrapper. TypeScript/ESLint/tests all passing (876 web tests, mobile TS clean). Clears ROADMAP Track B1 | PR #15 (merged) |
| 2026-06-23 | UI / Design quality + Store-readiness | Fix VISION.md design bar violation in signup page: replace `🏠` emoji with Lucide `Home` icon (matches login panel pattern); add Terms of Service + Privacy Policy consent line below the signup form (App Store requirement for paid launch) | PR #11 (auto-merge enabled) |
| 2026-06-23 | Latency / Performance | Parallelize room overview count queries (`candidate_products`, `product_bundles`, `room_mockups`) with `Promise.all` — eliminates 2 stacked sequential server-side round-trips on the room journey hub page, cutting TTFB by ~40ms per visit | PR #10 (auto-merge enabled) |
| 2026-06-23 | UI / Design quality | Replace emoji iconography (🏠🛋️🍳🛏️🚿) in dashboard room-select and photo-upload steps with Lucide icon components (Home, Sofa, UtensilsCrossed, BedDouble, Bath): fixes explicit VISION.md design bar violation ("emoji used as iconography") in the primary onboarding flow | PR #9 (auto-merge enabled) |
| 2026-06-23 | Latency / Performance | Remove dead blocking `GET /api/analyze-apartment` fetch from dashboard `loadExisting()`: `apartmentSummary` state was never read in any render path; removing the fetch eliminates one Supabase round-trip that blocked `setLoading(false)` for every returning user with diagnosed rooms | PR #8 (auto-merge enabled) |
| 2026-06-23 | Latency / Performance | Parallelize per-room image fetches in dashboard `loadExisting()`: convert sequential `for` loop to `Promise.all` — 3–4× faster dashboard load for returning users with 3–4 rooms; also fix pre-existing ESLint unescaped-apostrophe error | PR #7 (auto-merge enabled) |
| 2026-06-23 | Feature / Social / SEO | Add OG + Twitter Card meta tags to `/shared/[token]`: convert page from client-side fetch to server component with `generateMetadata()` (dynamic title/description/image per design); `not-found.tsx` for HTTP 404; `loading.tsx` for navigation state; zero new LLM calls | PR #6 (auto-merge enabled) |
| 2026-06-23 | Feature / Retention | Add public share links for saved designs: PATCH `/api/saved-designs/[id]` generates a 128-bit share token; public `GET /api/shared/[token]` endpoint (no auth); public `/shared/[token]` editorial page with CTA; share toggle UI in saved design detail page; DB migration for real Supabase | PR #5 (auto-merge enabled) |
| 2026-06-23 | Engineering quality | Add 66 tests for `product-math.ts` + `material-math.ts`: 6-axis product scoring (scale/palette/material/value/proportion/lifestyle), wood coherence, metal coherence, soft-hard ratio, distribution balance, cross-room constraints | PR #3 (auto-merge enabled) |
| 2026-06-23 | Engineering quality | Add 53 tests for `set-math.ts` + `bundle-math.ts`: cross-product coherence, material conflicts, duplicate detection, tier differentiation, spatial feasibility, room completeness, price coherence | PR #2 (auto-merge enabled) |
| 2026-06-23 | Engineering quality | Add 55 tests for `color-math.ts`: CIEDE2000 reference pairs (Sharma 2005) + palette harmony + per-item fit integration | PR #1 (auto-merge enabled) |
